# NiloV Catering — project context (auto-loaded by Hermes)

Premium catering landing site. Stack (verified, do NOT change without asking):
- Next.js 16 + React 19 + Tailwind 4 + TypeScript
- GSAP, framer-motion, lenis (smooth scroll), zod, shadcn/ui
- Package manager: pnpm
- node_modules/.next are safe to delete & rebuild

Brand & behavior rules (from user corrections):
- Do NOT build the site yet — user studies blueprints/skills first, does NOT want 1:1 replication.
- Improve on provided docs, treat as a base, not a template to copy.
- User is highly cost-sensitive: keep free models, never switch to paid without explicit ask.
- User distrusts agent self-assessment — enforce separate critic + objective metrics (Lighthouse/CWV/axe) before claiming quality.
- When delegating to user, give exact URL + button/field + exact text.

Working dir: ~/Documents/hermes/catering-site
