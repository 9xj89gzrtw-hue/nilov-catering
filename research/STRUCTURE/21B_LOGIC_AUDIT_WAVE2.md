# 21B — LOGIC AUDIT (LogicAuditor) · ПЕРЕПРОВЕРКА ПОСЛЕ ВОЛНЫ 1

**Роль:** независимый критик-субагент (др. модель, НЕ автор). Настроен на занижение.
**Дата:** 2026-07-19. **Объект:** `04_BLOCKS`, `07_CALCULATOR_SPEC`, `08_CONSTRUCTOR_SPEC` + `21_LOGIC_AUDIT` (был балл 5).
**Метод:** кросс-чтение трёх файлов + цитатная сверка каждого закрытого бага + web_search по бренду/прайсу.
**ОБЯЗАТЕЛЬНЫЙ ИНТЕРНЕТ:** выполнен (см. R1/R2).

---

## ИТОГОВЫЙ БАЛЛ: 7 / 10  (был 5)

**Почему поднялся с 5:** Все 6 числовых/логических противоречий из `21_LOGIC_AUDIT` §2 (КР-1…КР-6) и тупики воронки §3 **реально закрыты** и согласованы цитатами во всех трёх файлах. Математика калькулятора и конструктора теперь детерминированно идентична.

**Почему НЕ 10 (на занижение):**
- Остаток балла упирается в **внешнюю неверифицируемость** (бренд-claim'ы и расхождение канона с живым прайсом) — не ломает логику расчёта, но блокирует прод-релиз.
- Один **мелкий резидуал логики** (R4: расхождение числа карточек форматов 07 vs 08 + нет входа для mobile-furshet в калькуляторе).

---

## ПЕРЕПРОВЕРКА ЗАКРЫТЫХ БАГОВ (цитаты)

| # | Баг | Статус | Док-во (цитата) |
|---|---|---|---|
| 1 | **Люкс = 5350 везде** (КР-1) | ✅ ЗАКРЫТ | `07` §Цены: «Фуршет \| … \| 5 350»; `04` §Formula (330): «Фуршет — … luxury **5 350**»; `04` PackageGrid (764): «Фуршет: … Люкс **5 350**». Нигде нет `6500`. savings (337) считает от `pricePerGuest['luxury']` per-формат (фуршет 5350 / банкет 6970). |
| 2 | **calcTotal едина** (КР-2) | ✅ ЗАКРЫТ | `07` полный TS `calcTotal(guests,format,tier,addons,opts)`; скидки gamma+early внутри. `04` (333): «калькулятор (07) И конструктор (08) ОБЯЗАНЫ вызывать одну и ту же calcTotal». `08` §Расчёт (31): «ЕДИНАЯ calcTotal … из 07 … импорт из lib/pricing.ts. Оба инструмента считают ОДИНАКОВО». |
| 3 | **ADDONS единый** (КР-3) | ✅ ЗАКРЫТ | `07` `ADDONS` (118-129): пирамида 7000/бармен 6000/фонтан 5000/торт 3000/сомелье +350/шоу +800/мастер +600/эко +50/видео 5000/флористика 0. `08` шаг4 (48): «из единого ADDONS (07): пирамида 7000… флористика (бесплатно при свадьбе)» — 1:1. |
| 4 | **MIN_GUESTS по формату** (КР-4) | ✅ ЗАКРЫТ | `07` (39-45): `MIN_GUESTS = {furshet:20, banket:15, 'coffee-break':10, 'mobile-furshet':10, 'chef-at-home':10}`. `04` EventTypeSelector (147): «мин. по формату тянется из единого MIN_GUESTS в 07». `08` шаг2 (46): «Валидация per-format через MIN_GUESTS (07)». |
| 5 | **qty в формуле** (КР-5) | ✅ ЗАКРЫТ | `07` custom: `base = …reduce((s,it)=> s + it.pricePerGuest * it.qty * guests, 0)`. `04` (335): «qty обязательный множитель … (совпадает с 08)». `08` (34): «qty обязателен … item.total = i.pricePerGuest * i.qty * guests». |
| 6 | **Тупик 10–19** | ✅ ЗАКРЫТ | `07` §Edge (159): инлайн-нотис для 10–19 (фуршет/банкет MIN>10) → «выберите Кофе-брейк (от 10) или Chef-at-home. Либо увеличьте до 20+». «Нотис не блокирует ввод». `08` Edge#1 (112) — то же. |
| 7 | **Chef-at-home НЕ тупик** (КР/B11) | ✅ ЗАКРЫТ | `07` §Edge (161-166): добавлен в FormatSelector; `chefTotal = hourlyRate*hours + …`; база «от 2 500 ₽/час (уточняется)» + «Свяжитесь с менеджером» → /api/contact с контекстом. `08` §Расчёт (39) + Edge#10 (121) — то же. |
| 8 | **CTA >500 унифицирован** (B8) | ✅ ЗАКРЫТ | `04` (324), `07` (160), `08` (113): везде «**Оставить заявку**» → `/api/contact`. Нет «позвонить»/«индивидуальный расчёт» как расходящихся фраз (поиск по 04: 0 совпадений). |
| 9 | **Slugs унифицированы** (КР-6/B5/B6) | ✅ ЗАКРЫТ | `02_IA` §«Каноничные slug'и» (60-72) — ЕДИНЫЙ RU-словарь `/events/svadba·korporativ·chastnoe·chef-at-home`. `04` EventTypeSelector (149): «Свадьба→/events/svadba · Корпоратив→/events/korporativ · Частное→/events/chastnoe · Chef-at-home→/events/chef-at-home» — совпадает. SeasonalModule (530): `ctaHref` → `/plan/calculator?tier=premium`. В 04 нет EN-slugs (`/events/wedding|corporate|private` — 0 совпадений). |

---

## ВЕРДИКТ

Логика цен и воронка **внутренне согласованы** после Волны 1: Люкс=5350 везде, единая `calcTotal` в 07/08, единый `ADDONS`, per-формат `MIN_GUESTS`, обязательный `qty`, тупик 10–19 и Chef-at-home закрыты, маршруты/CTA унифицированы. Математический риск рассинхрона устранён.

**Блокаторы прод-релиза (вне математики):** неверифицированные бренд-claim'ы и расхождение канона меню с живым прайсом (требуют подтверждения заказчика, не ломают расчёт). Один мелкий резидуал в UI-слое (R4).

---

## ОСТАТОЧНЫЙ BUG-LIST

**P2 (до прод-релиза — фактчек/риск, НЕ логика):**
- **R1 [B9/B10/B12]** Бренд-claim'ы НЕ подтверждены вебом (проверено 2026-07-19): «19 лет/с 2007», «3000+ событий», клиенты Эрмитаж/Мариинский/Armani/Forbes/Газпром, награды — самоотчёт. Домен `nilov-catering.ru` **НЕ индексируется** (найден только `interfood-catering.ru` + VK + vercel-demo). Компания реальна (ИП Нилов Дмитрий Игоревич, бренд NiloV Catering, тел +7(812)919-59-11 — совпадает со spec), но история/клиенты — риск фейк-репутации. Закрыть письменным подтверждением заказчика.
- **R2 [BUG-F2]** Канон меню (фуршет 2 450 / кофе-брейк 950 / банкет 4 470) **противоречит живому сайту** (фуршет+кофе-брейк **от 390**, корпоратив 2 450). Внутренняя консистентность spec НЕ нарушена (оба инструмента берут канон), но перед продом сверить с прайсом заказчика и при необходимости поменять канон в `07`.

**P3 (мелочи логики/UI):**
- **R4** `07` FormatSelector = **4 карточки** (Фуршет/Банкет/Кофе-брейк/Chef-at-home); `08` шаг1 = **5** (Фуршет/Банкет/Кофе-брейк/**Мобильный фуршет**/Chef-at-home). При этом `07` `PRICE_PER_GUEST` и `MIN_GUESTS` **содержат** `mobile-furshet`, но входа для него в калькуляторе нет → data определены, UI-точка отсутствует. Унифицировать список форматов между 07 и 08.
- **R5** `03_JOURNEYS` (20): «или звонок менеджеру» — терминологический дрейф от канона «Оставить заявку»; не ломает инструменты, но привести к единой формулировке.

**НЕ в зоне этого аудита:** Perf/a11y-доказательства (<200ms/CLS<0.1) — относятся к этапу сборки, не к логике цен.

---

*Итог: с 5→7. Математика цен/воронка восстановлена и согласована; балл сдерживают только внешняя неверифицируемость claim'ов (R1/R2) и мелкий R4.*
