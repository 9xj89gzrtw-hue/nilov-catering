# 26 — VIDEO ARCHIVE TASK (куда засунуть видео заказчика с мероприятий)

**Датировано:** 2026-07-18. **Источник требования:** пользователь — «у заказчика очень
много разных видео с мероприятий, надо придумать куда это можно будет засунуть».

**База:** `17_MEDIA_DIRECTION.md` §8 (6 мест + правила встраивания, подтверждено вебом:
kometmedia event-recap→sales, wiserreview Wall of Love, brandtotable behind-scenes,
Rutube Showcase / self-host).

## ЧТО СДЕЛАТЬ (после волны 1 исполнителей, когда 04_BLOCKS.md стабилизируется)

Исполнитель дописывает в `04_BLOCKS.md` по разделу 8 из `17`:

1. **Новый блок `EventsRecap`** (добавить в библиотеку блоков 04):
   - Лента хайлайтов (reel-style 15–45с) с фильтром по типу события (свадьба/корп/
     частное/шеф) + площадке (Эрмитаж/Мариинский/др. 🟡 — пример-категории, не подтверждённые клиенты) + дате.
   - Каждый клип открывается в модалке (полный recap 2–3 мин ИЛИ рил).
   - Источник: Rutube Showcase (эмбед, не self-host MP4 — LCP) ИЛИ self-host — Vimeo ЗАПРЕЩЁН в РФ (`04_BLOCKS:317`, `04` блок 27 `EventsRecap`).
   - Страница `/events/recap` + короткая лента на главной (после GalleryTeaser).
   - Таксономия ЕДИНАЯ с GalleryTeaser/Testimonials («по типу события»).
   - a11y: play-btn + poster при reduced-motion, aria-label клипа, focus-trap модалки.

2. **Блок 14 `EventHero`** — добавить video-recap вариант: ротация по типу
   (Свадьба→своё видео, Корпоратив→своё) поверх/вместо фото. Ссылка на §8 п.2.

3. **Блок 9 `TestimonialsCarousel`** — добавить video-variant (Wall of Love):
   короткие видео-слова гостей поверх EventAtmosphere. Ссылка на §8 п.3.

4. **Блок 18 `ShowCookingGrid`** — video из архива реальных событий (не постановочный
   loop). Ссылка на §8 п.5.

5. **Блок 19 `GalleryMasonry`** — 20–30% плиток = video-items **(parallax только на 1 hero-стрип, НЕ per-image; play on click)**.
   Ссылка на §8 п.6.

6. **Блоки 8 `WhyUs` / 26 `TrustProof`** — вставка behind-scenes 60с (сборка/сервировка/
   шеф). Ссылка на §8 п.4.

**Синхрон с волной 1:** НЕ править параллельно с исполнителями 1–3 (конфликт 04).
Запускать исполнителя 6 ТОЛЬКО после возврата волны 1.

**Критерий готовности:** в `04` есть `EventsRecap` + video-варианты в 9/14/18/19/8/26,
все со ссылкой на `17` §8, с a11y/reduced-motion/LCP-правилами. Потом прогон
MenuUXCritic/структурные критики по добавленному.
