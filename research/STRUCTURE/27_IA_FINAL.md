# 27 — ФИНАЛ: КРИТИК IA (InformationArchitect, перепроверка после Волн 1+2А)

**Дата:** 2026-07-19. **Роль:** InformationArchitect (независимый, настроен на занижение).
**Объект:** 02_IA.md · 03_JOURNEYS.md · 04_BLOCKS.md (~1100+ строк) · 16_BLOCK_BENCHMARKS.md.
**Ресёрч:** web_search 2026 (Muffin Group, Colorlib, DreamHost, freedom251 — подтверждают паттерны).

---

## 1. БАЛЛ ЗА INFORMATION ARCHITECTURE: 8 / 10

**Был 6 в Ит.1 → 8.** Рост на +2 за счёт устранения класса дефектов «битые/осиротевшие роуты».

**Почему не 9–10 (занижение):**
- Концепт и иерархия — сильные (shallow, event-first, 5+CTA, дедупликация 22→~10).
- Но остались 3–4 **минорных** недоработки не-перво-рода (H/I/K/L) — не битые ссылки, а тонкости контента/документации. Они не ломают сборку, но тянут на 8, не 9.

---

## 2. ПЕРЕПРОВЕРКА ПО ЦИТАТАМ (закрытые баги)

| Баг | Требование | Статус | Цитата-доказательство |
|---|---|---|---|
| A | Единый RU-URL-словарь | ✅ ЗАКРЫТ | `04:149-150` «Свадьба→`/events/svadba` · Корпоратив→`/events/korporativ` · Частное→`/events/chastnoe` · Chef-at-home→`/events/chef-at-home`»; `03:18,23,28,32` — все RU; `02:65` канон зафиксирован. EN-слугов в роутах НЕТ. |
| A2 | singular/plural | ✅ ЗАКРЫТ | Роут-биндинг RU (см. A). `wedding/corporate/private` остались только как **имена ассетов** (`04:151`) и `eventType`-енум данных (`04:363,1128`) — это НЕ URL, битых ссылок нет. |
| B | `MenuPreview` → `/menu/{format}` | ✅ ЗАКРЫТ | `04:190-191` «Куда ведёт CTA: `Смотреть меню →` → **`/menu/{format}`** … НЕ в `/menu/{collection}` — такого роута НЕТ (закрывает баг B)». |
| C | `/about#cert` + `/certificates` | ✅ ЗАКРЫТ | `04:29` футер «Помощь»: **Сертификаты** (`/certificates` — отдельная страница, блок CertBlock; ранее вела на `/why-us#cert`, что ломало… бага C)». `/about` → `/why-us` в навигации. |
| D | `/seasonal` в навигации | ✅ ЗАКРЫТ | `04:18` шапка: «…`Сезонное(/seasonal)`…» + `04:11` AnnouncementBar + футер. Не осирот. |
| E | `/team` в шапке | ✅ ЗАКРЫТ | `04:18` шапка: «`Команда(/team)`». |
| F | Breadcrumbs задействованы | ✅ ЗАКРЫТ | `04:21` «Breadcrumbs … задействован на ВСЕХ глубоких роутах — `/events/*`, `/menu/*`, `/plan/*`, `/blog/*`». |
| G | MobileBottomNav состав | ✅ ЗАКРЫТ | `04:20` «ровно **5** ключевых ссылок … `События`·`Меню`·`Галерея`·`Почему мы`·`Спланировать` … `/team` и `/seasonal` достижимы через бургер». |
| J | SEO-каннибализация меню | ✅ ЗАКРЫТА (снижен риск) | `02:139-157` «Каноничная иерархия меню» — 4 пути развёдены по интенту; `/seasonal` «НЕ дублирует … живёт отдельно». |

**Доп. проверки из ТЗ:**
- `/menu/{format}` — `/menu/furshet`·`/menu/banquet`·`/menu/coffee-break`·`/menu/catalog`·`/menu/show-cooking` — все в sitemap `02:23-28` ✅
- `/delivery` + `/certificates` + `/reviews` + `/thank-you` + `/404` — добавлены (`02:52-56`; `04:21,25,26,352,353,997,1022,1076,1090`) ✅
- Битых ссылок по grep: **не найдено** (EN-остатки — только ассеты/енум, не роуты) ✅

---

## 3. ОСТАТОЧНЫЙ BUG-LIST (минорный, не перво-рода)

| # | Тип | Проблема | Где | Серьёзность |
|---|---|---|---|---|
| H | Тонкая страница | **`/plan` (хаб)** описан в `02` как «хаб инструментов», но в `04_BLOCKS` НЕТ отдельного блока-спецы хаба (что выбрать — калькулятор или конструктор, «когда что»). Риск thin-page при сборке. | `02:38-41`; `04` (нет «БЛОК N — PlanHub») | НИЗКАЯ |
| I | Документация | **`/events/chef-at-home`** ведёт на «персональное предложение», минуя `/plan/calculator` — намеренно, НО в `03` сценарий [4] это не помечено явно как «исключение из воронки» (фикс #10 из Ит.1 не доведён до явной пометки). | `03:31-33` | НИЗКАЯ |
| K | Навигация | **`/faq`** НЕ в шапке (там 8 пунктов: События·Меню·Галерея·Почему мы·Команда·Отзывы·Сезонное·Спланировать). Достижим через футер «Помощь» (`04:29`) + FAQTeaser→`/faq`. Приемлемо, но не в шапке. | `04:18,29` | НИЗКАЯ |
| L | Контент-дубль | Цифры/стори (18 лет, 3000, су-вид, фермеры) дублируются: `AwardsStrip`+`WhyUs`+`/why-us`+`TrustProof`. Не битая ссылка, смысловой оверлап. | `04` блоки 3/8/57 | КОСМЕТИКА (принято) |

---

## 4. СВЕРКА С ТОПАМИ 2026 (web_search)

- **Colorlib (2026)** — «event category navigation» (Свадьба/Корпоратив/Частное как первичная навигация). У NiloV `/events/*` = ✅ эталон.
- **Muffin Group (2026)** — «sticky navigation keeps menu and CTA visible». У NiloV sticky header `04:17` + `StickyMobileCTA` = ✅.
- **DreamHost (2026)** — sticky-меню идеально «когда CTA всегда видна». У NiloV = ✅.
- **Нового/упущенного не нашёл:** IA уже покрывает event-category nav, sticky CTA, deep footer, breadcrumbs, 301-редиректы, llms.txt. Доп. страницы из Ит.1 (`/areas`, `/how-it-works`, `/press`) — опциональны, НЕ критичны для структуры; `/delivery` частично закрывает `/areas`.

---

## 5. ВЕРДИКТ

Архитектура **документально консистентна и конкурентоспособна на 2026**. Все критические
битые/осиротевшие роуты (A/B/C/D/E/F/G/J) **закрыты цитатно**; единый RU-URL-словарь
зафиксирован и соблюдается во всех трёх документах; битых ссылок по grep нет.

Остаток — 4 минорных пункта (H/I/K/L), не ломающих сборку:
- **H** — перед сборкой дописать контент/спеку хаба `/plan` (чтобы не thin-page);
- **I** — пометить chef-at-home как явное исключение в `03`;
- **K** — `/faq` оставить в футере (OK) или вынести в `/plan`;
- **L** — дубль стори принять как сознательный (разные блоки = разные контексты).

**Итоговый балл: 8/10** (прогноз Ит.1 «после фикса — 8» выполнен). Для 9 нужна только доработка H.
Готово к сборке (Волна 3А: видео-архив `/events/recap` уже заложен в `04` БЛОК 27).
