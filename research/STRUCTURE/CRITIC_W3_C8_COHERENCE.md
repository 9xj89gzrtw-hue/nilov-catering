# CRITIC W3 · C8 — Когерентность и сквозные противоречия между файлами (ось 8/9)

**Дата:** 2026-07-20 · **Критик:** независимый субагент #8 (не автор, настроен на занижение)
**Метод:** проверка ПО ДИСКУ (grep + read_file по всей папке `STRUCTURE/`, 93 .md). Старым вердиктам не доверял — все проверки прогнаны вживую.
**Объект:** взаимная согласованность набора; фокус — cross-file contradictions.
**Прочитаны целиком:** `22_COHERENCE_REPORT`, `47_COHERENCE_AUDIT`, `39_CANON_INDEX`; выборочно/через grep — `14_GAP_ANALYSIS`, `19_BLOCK_CRITICS`, `20_CRITIC_RESULTS`, `19B_CRITIC_REPORT`, `19B_ITER2`, `21_LOGIC_AUDIT`, `21B_LOGIC_AUDIT_WAVE2` + весь набор для grep-проверок (a)–(e).

---

## БАЛЛ: **6 / 10 → REWORK**

Ядро канона (`01`/`06`/`39`) внутренне сильное и согласованное, но живой grep находит **реальные сквозные противоречия**, а один из самих аудит-файлов (`47_COHERENCE_AUDIT`) содержит ложное самоуспокаивающее утверждение. Аудит когерентности, который сам некогерентен, — это блокер для оси.

---

## РЕЗУЛЬТАТЫ 5 ОБЯЗАТЕЛЬНЫХ ПРОВЕРОК (grep, вживую)

| # | Проверка | Вердикт | Доказательство |
|---|---|---|---|
| (a) | Цвета ivory `#FAF7F2` / ink `#1C1815` / gold `#B08D57` едины | **PASS** | Совпадают в `01:35,37,39`, `06:22,23,28,43`, `04:26–47`, `39:15`, `29`, `31`, `43`, `05`. Ядровая тройка нигде не расходится. Историч. дрейф cream `#F2ECE3`↔`#F2EDE4` и warm-grey `#6B625A`↔`#6B635B` (RC-1) — по `22:202` ЗАКРЫТ (Волна 4Б); live-grep `#F2EDE4`/`#6B635B` находит их **только внутри самого отчёта `22`** как описание прошлого дефекта, не в канон-файлах. ✅ |
| (b) | Cormorant везде, Playfair = 0 в целевых файлах | **FAIL** | grep `Playfair` → **~30 живых вхождений как АКТИВНОГО шрифта**: `36_DESIGN_RUBRIC:18` (замор. рубрика!), `36_A` (29,40,71,85,137,212,274…), `36_B` (27,77,114,165,181,221,235…), `36_C` (24,91,160,249…), `09_HERO:25,41,78`, `10:10,13,22`, `11:10,18`, `16:22`, `13:21`, `19_BLOCK_CRITICS:17`, `25_MENU_UX_CRITIC:50`. В `01`/`06`/`39`/`00`/`02`/`04`/`44` — корректно «НЕ Playfair»/Cormorant. |
| (c) | Юр-лицо ИП Нилов, «Интерфуд» = 0 | **FAIL (1 нарушение)** | `21B_LOGIC_AUDIT_WAVE2:47` называет оператора «ИП Нилов Д.И. **«Интерфуд Кейтеринг»**» — запрещённое legacy-имя (AGENTS.md). Остальные вхождения корректны: `04:780` и `33_UXSIM_GOSHA` — конкурент в контексте, `04:2088` — guard «НЕ Интерфуд», `45:41` — пометка ребрендинга. |
| (d) | Цена кофе 390₽, «950» = 0 | **FAIL (внутр. противоречие BUG-F2)** | Канон spec держит **950 ₽** (`07:21,80`, `04:390,632,1001,1469,1554`, `10:30,41`) с флагом 🟡 `pending-verification` + рефут-нотой «живой сайт NiloV: от 390 ₽». Но в UX-симуляциях `33_UXSIM_DARYA:14,22,67`, `33_UXSIM_MARINA:12,13,51` «950 ₽» стоит **без 🟡 и без рефут-ноты** — исполнитель может взять 950 как факт. `45:16` и `FACTCHECK_REPORT:36` фиксируют 950 как ❌ REFUTED. |
| (e) | Роспотребнадзор = регулятор, не клиент | **FAIL (1 нарушение)** | `04_BLOCKS.md:1164` (TrustProof, item 10) перечисляет `Роспотребнадзор (сертификация)` в списке **«Клиенты:»** рядом с Эрмитаж/Armani — прямо противоречит `04:522` (тот же файл, где он КОРРЕКТНО исключён) и `45:16` (REFUTED). Также stale в `13:23` и `16:103`. Везде ещё (`02:39,181`, `04:139,522`, `19:30`, `29:62`, `45`) — корректно как регулятор. |

---

## СЧЁТ РЕАЛЬНЫХ СКВОЗНЫХ ПРОТИВОРЕЧИЙ: **5 значимых** (2 HIGH, 2 MEDIUM, 1 residual-кластер)

1. **[HIGH] Playfair не вычищен + `47` лжёт, что вычищен.** `47_COHERENCE_AUDIT:17` заявляет «C2–C5 … ✅ вычищен везде», а grep даёт ~30 живых Playfair (весь набор `36_*`, `09`/`10`/`11`, `16`, `13`, `19`, `25`). Хуже: `47:15` ложно цитирует `06_TOKENS:76` как «Playfair» — на деле строка 76 = Cormorant. Аудитор выдумал self-contradiction, которого нет, и «закрыл» реальный, которого не заметил.
2. **[HIGH] Роспотребнадзор как «клиент» в `04:1164`** ↔ `04:522`/`45:16`. Внутрифайловое + межфайловое противоречие, риск ложного соц.-доказательства/претензии госоргана.
3. **[MEDIUM] «Интерфуд Кейтеринг» как юр-имя в `21B:47`** ↔ AGENTS.md + `04:2088`. Запрещённое legacy-название использовано как факт.
4. **[MEDIUM] Цена 950 (канон) vs 390 (живой сайт), BUG-F2.** Внутренне spec-консистентен (оба инструмента берут 950), но противоречит реальности; в `DARYA`/`MARINA` 950 без флага. Не сверено с прайсом заказчика.
5. **[RESIDUAL, LOW→MED] Незакрытые контракты из `22`/`47`:** slug-словарь EN-enum↔RU-route (RC-4, `22:187,208`), лейбл CTA «Спланировать» vs «Спланировать событие» (RC-3), Hero-CTA route расхождение `/plan` vs `/plan/calculator` (`47` R4). Помечены «закрыты», но mapper и лейбл всё ещё расходятся по файлам.

**Позитив (для баланса):** светлая ДНК соблюдена ВЕЗДЕ (тёмных фонов нет), цветовая тройка едина, тарифная модель `economy…luxury` унифицирована (`22` C3 ✅), дубли trust-блоков свёрнуты (C12 ✅), GSAP-мусор убран (C10 ✅), Люкс=5350 приведён (C2 ✅). Ядро канона `01`/`06`/`39` — реально ≥8.

---

## БАГ-ЛИСТ ДЛЯ AUTHOR

| # | Severity | Локация | Баг | Fix |
|---|---|---|---|---|
| C8-1 | HIGH | `47_COHERENCE_AUDIT:15,17` | Ложь «Playfair вычищен везде» + неверная цитата `06:76` | Исправить: Playfair жив в `36_*`/`09`/`10`/`11`/`16`/`13`/`19`/`25`; строка `06:76` = Cormorant. Снять статус «✅ вычищен». |
| C8-2 | HIGH | `36_DESIGN_RUBRIC:18`, `36_A/B/C`, `09`, `10`, `11`, `16:22`, `13:21`, `19:17`, `25:50` | Playfair как активный шрифт вопреки канону Cormorant (`01:58`,`06:76,106`) | Заменить Playfair→Cormorant во всех целевых файлах; в рубрике порог оси 3 переписать на Cormorant. |
| C8-3 | HIGH | `04_BLOCKS.md:1164` | Роспотребнадзор в списке «Клиенты» | Удалить из клиентов; при нужде — перенести в `CertBlock` как регулятора/сертификацию. |
| C8-4 | MED | `21B_LOGIC_AUDIT_WAVE2:47` | «ИП Нилов Д.И. «Интерфуд Кейтеринг»» — запрещённое имя | Заменить на «ИП Нилов Дмитрий Игоревич (бренд NiloV Catering)». |
| C8-5 | MED | `33_UXSIM_DARYA:14,22,67`, `33_UXSIM_MARINA:12,13,51` | «950 ₽» без 🟡/рефут-ноты (живой сайт 390₽) | Добавить сноску «950 = канон spec 🟡 pending; живой сайт 390 ₽, см. `04:390`» или пометить «что видит пользователь». Сверить канон `07` с прайсом заказчика до прода. |
| C8-6 | LOW | `13:23`, `16:103` | Stale: Роспотребнадзор/«19 лет» в бенчмарках | Убрать Роспотребнадзор, «19 лет»→«18 лет 🟡». |
| C8-7 | LOW | `47` R4 / `22` RC-3,RC-4 | CTA-лейбл + EN/RU route↔enum маппер расходятся | Единый лейбл «Спланировать событие»; описать явный `eventType`↔slug mapper. |

---

## ОБОСНОВАНИЕ БАЛЛА
- Ядро (цвета, светлая ДНК, тарифы, типографика в канон-файлах) когерентно → тянет к 8.
- Но проверка (b) провалена (~30 Playfair), (c)/(e) — по 1 нарушению в аудит-файлах, (d) — незакрытый BUG-F2, а сам `47_COHERENCE_AUDIT` содержит ложное «вычищен везде» → −2.
- ≥2 HIGH cross-file противоречия + недостоверный аудит-файл ⇒ по правилу самокритики (≥3 крит. или ложный self-report) ось не может быть PASS.

## ВЕРДИКТ: **REWORK** (6/10)
Порог PASS (≥8) не достигнут. Обязательны к закрытию до PASS: C8-1, C8-2, C8-3 (все HIGH). C8-4/C8-5 — до прод-релиза.
