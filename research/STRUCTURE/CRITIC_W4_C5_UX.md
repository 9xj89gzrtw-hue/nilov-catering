# CRITIC_W4_C5_UX — ПОВТОРНЫЙ критик #5 (UX, персоны, устройства, ось 5/9)

**Дата:** 2026-07-20 (итерация W4, после правок Sticky-CTA / волн 1–11)
**Роль:** независимый критик-субагент (НЕ автор `04_BLOCKS`/`32_UX_PERSONAS`/симуляций). Установка — занижение.
**Объект:** `32_UX_PERSONAS.md`, `34_UX_SUMMARY.md`, `43_NAV_SPEC.md`, `04_BLOCKS.md`, `29_DEVICECRITIC.md` — сверка ПО ДИСКУ (read_file/grep), НЕ по старому `CRITIC_W2_C5_UX.md`.
**Регрессионная гипотеза задания:** стал ли консистентным навигационный конфликт между `43_NAV_SPEC` и `04_BLOCKS` (состав `MobileBottomNav` + роуты) ПОСЛЕ правок Sticky-CTA.
**Стадия:** спека/структура (сборка НЕ начата — `components/layout/MobileBottomNav.tsx` на диске ВСЁ ЕЩЁ legacy 2-пункта: `Меню` / `Калькулятор(#calculator)` / `Звонок`, см. file:1-46). → объективные метрики (Lighthouse/axe/CWV) не замерены, Perf/a11y корректно потолочены на 7 (skill self-critic, SPEC-stage).

---

## 1. ОЦЕНКА ОСИ 5 (UX / персоны / устройства)

**Итоговый балл оси: 8.0 / 10** (заниженно, критик).

| # | Под-критерий | Балл | Обоснование (file:line, по диску) |
|---|---|---|---|
| 1 | Покрытие ключевых персон (a) | **10** | 30/30 персон `32_UX_PERSONAS.md:21-57` → 30 файлов `33_UXSIM_*.md` на диске (сверка имён: Аня…Гоша — 30/30, `34_UX_SUMMARY.md:13-14`). Без изменений с W2. |
| 2 | Покрытие типов устройств (d) | **10** | Mobile (Дима), Tablet (Катя), Desktop (Борис), Accessibility/SR (Анна) + `29_DEVICECRITIC.md` (все устройства). 4/4 класса покрыты. |
| 3 | Сезонность New Year — только нояб–дек, НЕ постоянно (b) | **8** | `04_BLOCKS.md:1224` `newyear`=нояб–дек; `:114,:1238` механизм `seasonalConfig` + evergreen-лендинг «планируй заранее» (НЕ постоянный НГ-блок). Баг B-ZH закрыт Волной 8. Корректно. |
| 4 | AnnouncementBar — помечен ли dismissible (c) | **8** | `04_BLOCKS.md:112` `dismissible?:boolean` (по умол. да); `:114` high-season override `dismissible:false`/`per-visit`. Логика целостна. |
| 5 | Нет противоречий `43_NAV_SPEC` ↔ `04_BLOCKS` по `MobileBottomNav`/роутам (e) — **РЕГРЕССИЯ** | **7** | C5-1 (конфликт состава 5-пунктового bottom-nav) **ЗАКРЫТ** CoherenceChecker C13 (`04_BLOCKS.md:197`): *«5-й пункт = Главная, не Почему мы, унифицировано с `43_NAV_SPEC` §3»* → обе спецы теперь несут `{Главная, События, Меню, Галерея, Спланировать}`. Sticky-CTA-правки (`:159-188`, B2–B4, B13) консистентны: primary StickyMobileCTA=`/plan`, OR-контракт (ровно 1 из {Sticky, BottomNav}), базовый href совпадает с Hero/MobileBottomNav/C9. **ОДНАКО:** (i) `04_BLOCKS.md:124` (проза SiteHeader mobile) ВСЁ ЕЩЁ пишет «Почему мы» в 5-ке bottom-nav — stale-описание, формально противоречит C13 (не блокирует сборку, но мусор в спеце); (ii) C5-2 (§7 `43_NAV_SPEC:155-158` НЕ перечисляет `/plan/calculator`,`/plan/constructor`,`/partners`,`/subscribe`,`/media-kit`,`/account/orders`) **НЕ закрыт**; (iii) C5-3 (`43_NAV_SPEC:105` `grep "/constructor"→0`) **НЕ закрыт** — false-positive на легитимный `/plan/constructor`. |
| 6 | Аудит-трейл исправлений в симуляциях (консистентность) | **7** | `33_UXSIM_GOSHA.md:17` по-прежнему цитирует «MobileBottomNav (5 пунктов, в т.ч. „Почему мы“)» — теперь ПРОТИВОРЕЧИТ утверждённому C13 (5-й = Главная). `33_UXSIM_ZHANNA.md` без resolved-баннера Волны 8 (C5-5 не закрыт). Трейл неоднороден. |

**Среднее по оси:** (10+10+8+8+7+7)/6 = **8.33** → критик занижает до **8.0**: конфликт состава bottom-nav реально закрыт и Sticky-CTA консистентны (подъём с W2), НО (1) §7 `43_NAV_SPEC` неполон по роутам, (2) grep-гейт `:105` сломан, (3) stale-упоминание «Почему мы» в `04_BLOCKS:124` и в `33_UXSIM_GOSHA.md` остались, (4) стадия = спека без замеров CWV/axe (по skill ≤8 без proof).

---

## 2. БАГ-ЛИСТ (id / severity / status / file:line / суть / фикс)

**C5-1 · HIGH → ✅ RESOLVED · 43_NAV_SPEC.md:54-60 vs 04_BLOCKS.md:124/197**
Конфликт состава `MobileBottomNav` (ровно 5) ЗАКРЫТ через CoherenceChecker C13: `04_BLOCKS.md:197` фиксирует канон = `Главная(/) · События · Меню · Галерея · Спланировать(/plan)` и прямо пишет «унифицировано с `43_NAV_SPEC` §3». Обе спецы теперь согласованы. Sticky-CTA-правки подтверждают тот же набор (`04_BLOCKS.md:159-188`).
**Остаточный шум:** `04_BLOCKS.md:124` (проза мобильного SiteHeader) ВСЁ ЕЩЁ перечисляет «Почему мы» в 5-ке — противоречит C13. Не блокирует сборку (B3 `:197` — контракт), но требует правки формулировки для чистоты.
**Фикс (тривиальный):** в `04_BLOCKS.md:124` заменить «Почему мы(/why-us)» на «Главная(/)» (либо убрать перечисление, т.к. контракт B3 `:197` — единственный источник).

**C5-2 · MEDIUM → ⚠️ PARTIAL · 43_NAV_SPEC.md:155-158**
§7 «Покрытие роутов» НЕ перечисляет `/plan/calculator`, `/plan/constructor`, `/partners` (AgencyCabinet), `/subscribe` (RecurringOrder), `/media-kit` (MediaKit), `/account/orders` (Петя/Сергей) — добавлены Волной 11.
**Смягчение:** `41_BUILD_CHECKLIST.md:96` полно перечисляет эти роуты как часть канона (со ссылкой на Волну 11/Петю/Сергея) →「источник истины」для роутов фактически `41`/`02_IA`, риск пропуска сборщиком снижен. Но `43_NAV_SPEC` (заявлен как «единый источник истины для навигации», строка 3) по-прежнему расходится с полным каноном.
**Фикс:** добавить перечисленные роуты в §7 `43_NAV_SPEC` (или явно сослаться на `41_BUILD_CHECKLIST` Шаг 4 как на полный список).

**C5-3 · MEDIUM → ❌ OPEN · 43_NAV_SPEC.md:105**
Гейт `grep -rn "/constructor" app/ components/ lib/ → 0` **некорректен**: совпадёт с легитимным `/plan/constructor` (используется везде в симуляциях: `ARTYOM:35`, `SVETA:18`, `PETYA:79`, `DARYA:58`), давая ложно-положительные «404» при проверке. **Не исправлено с W2.**
**Фикс:** искать ТОЛЬКО standalone `href="/constructor"` (слово-граница), а не подстроку `/constructor`.

**C5-4 · INFO → ✅ N/A (без изменений)**
На диске 30 `33_UXSIM_*.md` (по числу персон), не 34. Расхождение в формулировке задачи, не в работе. 30/30.

**C5-5 · LOW → ❌ OPEN · 33_UXSIM_ZHANNA.md:13,25,40**
Симуляция Жанны НЕ несёт resolved-баннера Волны 8 (ZH1–ZH8 закрыты в `04_BLOCKS.md:114,1238-1244`). Трейл неоднороден (Дима `:31` цитирует волны, Жанна — нет).
**Фикс:** добавить в начало `33_UXSIM_ZHANNA.md` строку-статус.

**C5-6 · MEDIUM (кросс-срезовый) → ❌ OPEN · BUG-F2**
Цены тарифов (`pending-verification`, канон 950/2450 ₽ vs живой сайт 390 ₽) НЕ подтверждены заказчиком → бьёт достоверность КАЖДОГО live-числа в калькуляторе/конструкторе во всех симуляциях. Вне зоны навигационного конфликта, но блокирует цель ≥9 (`34_UX_SUMMARY.md:86 §4`).

**C5-7 · LOW → ❌ OPEN · 29_TRUST_SEG_CRITIC.md (позиционирование)**
Премиум-лексемы/логотипы 🟡 рассогласованы с волей «средний, НЕ премиум» (T1–T12). Влияет на UX среднего сегмента (Жанна/Павел/Гоша). Не блокирует ось 5, но влияет на итоговый UX-балл персон.

**НОВЫЙ C5-8 · LOW · 33_UXSIM_GOSHA.md:17 (регрессия трейла)**
Симуляция Гоши цитирует «MobileBottomNav (5 пунктов, в т.ч. „Почему мы“)» — теперь ПРОТИВОРЕЧИТ утверждённому C13 (`04_BLOCKS.md:197`, 5-й = Главная). Симуляция не обновлена под финальный состав → аудитор, читающий только файл Гоши, увидит устаревший состав.
**Фикс:** в `33_UXSIM_GOSHA.md:17` заменить «в т.ч. „Почему мы“» на «в т.ч. „Главная“» (либо убрать перечисление).

---

## 3. ПРОВЕРКА ПУНКТОВ ЗАДАНИЯ (a–e)

- **(a) Покрытие персон** — ✅ PASS. 30/30 (`32_UX_PERSONAS` ↔ `33_UXSIM_*`).
- **(b) Сезонность New Year** — ✅ PASS. `04_BLOCKS.md:1224` newyear=нояб–дек; B-ZH закрыт Волной 8.
- **(c) AnnouncementBar dismissible** — ✅ PASS. `:112,:114` корректно (default да; high-season override).
- **(d) Типы устройств** — ✅ PASS. Mobile/Tablet/Desktop/Accessibility + `29_DEVICECRITIC`.
- **(e) Противоречия маршрутов `43_NAV_SPEC` ↔ `04_BLOCKS`** — ✅ **УЛУЧШЕНО (регрессия цели задания):**
  - Состав `MobileBottomNav` — **КОНФЛИКТ ЗАКРЫТ** (C5-1, CoherenceChecker C13): обе спецы теперь `{Главная, События, Меню, Галерея, Спланировать}`, Sticky-CTA консистентны (primary=`/plan`, OR-контракт).
  - Остаточные противоречия: §7 `43_NAV_SPEC` неполон по роутам (C5-2, partial), grep-гейт `:105` сломан (C5-3), stale «Почему мы» в `04_BLOCKS:124` (C5-1 residual) и в `33_UXSIM_GOSHA.md:17` (C5-8).

---

## 4. ВЕРДИКТ

**PASS** (повышение с REWORK при W2).

**Обоснование:**
- **Регрессионная гипотеза задания ВЫПОЛНЕНА:** навигационный конфликт по составу `MobileBottomNav` между `43_NAV_SPEC` и `04_BLOCKS` **стал консистентным** после правок Sticky-CTA — через CoherenceChecker C13 (`04_BLOCKS.md:197`), две спецы теперь несут идентичный 5-пунктовый набор, а Sticky-CTA-правки (`04_BLOCKS:159-188`, B2–B4, B13) НЕ внесли новых расхождений (primary `/plan`, OR-контракт, совпадение с Hero/MobileBottomNav/C9).
- Сильные стороны сохранены: 30/30 персон, 4/4 устройства, корректная сезонная логика, AnnouncementBar dismissible.
- Балл оси поднят **7.0 → 8.0**.
- **Почему не ≥9:** (1) §7 `43_NAV_SPEC` неполон по роутам Волны 11 (C5-2, хотя покрыт `41_BUILD_CHECKLIST`); (2) grep-гейт `:105` сломан (C5-3); (3) остаточный мусор «Почему мы» в `04_BLOCKS:124` + `33_UXSIM_GOSHA.md:17` (C5-1 residual / C5-8); (4) BUG-F2 цены (C5-6) и позиционирование (C5-7) открыты; (5) стадия = спека, без замеров Lighthouse/axe/CWV (по skill self-critic SPEC-stage не даёт ≥8 без proof → потолок 8.0 корректен и не завышен).
- **Критических (блокирующих сборку целиком) багов по оси 5 нет** → FAIL не обоснован. C5-3 (grep-гейт) — единственный, что может дать ложно-положительный фейл на гейте сборки, но он изолирован и легко правится.

**Что закрыть до сборки (низкий порядок, НЕ блокирует PASS оси):** C5-3 (поправить grep-гейт `43_NAV_SPEC:105`), C5-2 (дописать роуты Волны 11 в §7), C5-1 residual + C5-8 (убрать «Почему мы» из `04_BLOCKS:124` и `33_UXSIM_GOSHA.md:17`), C5-5 (banner Жанны). Кросс-срезовые C5-6 (BUG-F2 цены) и C5-7 (позиционирование) — отдельные оси, блокируют итоговый ≥9 после сборки.

**Оценка оси 5: 8.0 / 10.** Рост +1.0 относительно W2 (7.0) за счёт закрытия навигационного конфликта состава bottom-nav и консистентности Sticky-CTA.
