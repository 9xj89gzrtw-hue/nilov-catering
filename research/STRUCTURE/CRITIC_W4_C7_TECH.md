# CRITIC_W4_C7_TECH — ПОВТОРНЫЙ аудит. Тех/билд (ось 7/9)

**Роль:** независимый критик #7 (НЕ писал код и НЕ правил спецы/билд-док). Объект — правки
после W3-аудита: `41_BUILD_CHECKLIST.md` (новый **Шаг 5.5**) + `05_BUILD.md:27-30` (пути примитивов).
**Метод:** верификация ПО ДИСКУ (`read_file`/`search_files` по `/Users/evgenijdanilov/Documents/hermes/catering-site`),
НЕ по старому вердикту. **Дата:** 2026-07-20.

---

## 0. РЕЗУЛЬТАТ

**ОЦЕНКА: 8.3 / 10 → PASS** (цель комиссии ≥8 достигнута; было 7.4/REWORK в W3).

| № | Ось | W3 | W4 | Δ | Доказательство (диск) |
|---|---|---|---|---|---|
| 1 | Полнота охвата (блоки/роуты/фазы) | 9 | **9** | — | `41:52-96` всё на месте, не тронуто. |
| 2 | Однозначность (нет противоречий) | 8 | **8** | — | C7-01 фразово ослаблен (см. остатки), не блокирует. |
| 3 | Версионная консистентность стека | 9 | **9** | — | не менялось, `package.json` актуален. |
| 4 | Фаза 0: тёмный globals.css + перекраска | 9 | **9** | — | не менялось. |
| 5 | Точность claim'ов vs код | 6 | **8** | +2 | C7-02/03/04 закрыты: `41` Шаг 5.5 + `05:27-30` больше НЕ лгут про существующие файлы. |
| 6 | Реализуемость calc/constructor (zod) | 6 | **8** | +2 | C7-05 закрыт: `41:131` явная `contactSchema` + `z.infer` + импорт `zod`. |
| 7 | Гейты/верификация | 8 | **8** | — | `41:134` ГЕЙТ 5.5 (grep импортов → резолвятся). |
| 8 | Навигация/IA | 8 | **8** | — | не менялось. |
| 9 | Структура папки / читаемость инструкции | 6 | **8** | +2 | C7-02/03/09: dangling-модули помечены 🆕, не «существуют». |

**Среднее:** (9+8+9+9+8+8+8+8+8)/9 = **75/9 = 8.33 → 8.3/10 → PASS.**

> Почему PASS, а не 7.4: оси 5/6/9 поднялись с 6 до 8 ровно по тем 3-м багам, что комиссия
> просила закрыть (C7-02/03/04/05). Диск это подтверждает — не «на веру».
> Почему не 9: остаются 2-3 LOW-пункта (C7-01 формулировка, C7-07 «VIP», C7-08 субъективный гейт) — см. §3.

---

## 1. ПРОВЕРКА ПО ДИСКУ (4 вопроса заказчика)

### (a) Шаг 5.5 в 41 закрывает ли C7-02/03/05 и помечает модули 🆕?
**РЕЗУЛЬТАТ: ДА, корректно.** ✅
- `41_BUILD_CHECKLIST.md:118` — заголовок Шага 5.5: «закрывает C7-02/C7-03».
- `41:124-131` таблица модулей, все со статусом **🆕**:
  - `lib/pricing.ts` — перенос `calcTotal`+`PRICE_PER_GUEST`+`ADDONS`+`MIN_GUESTS`+`CHILD_RATE` из `07:63-153`.
  - `addons.json` — 14+ позиций из `07:136-153`.
  - `hooks/useCalculator.ts` — хук (отличать от существующего `hooks/useConstructor.ts`).
  - `components/calculator/*` — `CalculatorApp`/`FormatSelector`/`GuestsSlider`/`TierSelector`/`ResultDisplay`/`LiveSummaryBar` из `07:185-205`, база = `components/quote/QuoteForm`.
  - `POST /api/calculate` — алиас → `app/api/quote` (см. `05:79`).
  - `zod`-схемы (`07`/`08`) — `contactSchema` (`name:z.string().min(2)`, `phone:z.string().regex(/^\+7\d{10}$/)`, `date:z.string().min(1)`, `comment:z.string().optional()`) + `z.infer` + импорт из `zod` (в deps `^4.4.3`). **Строка 131: «Закрывает C7-05».**
  - `catalog.json` (`08:149`) — тоже 🆕 (fallback без CMS, синхрон `04:88,973`).
- `41:134` — **ГЕЙТ 5.5**: `grep -rn "from 'lib/pricing'|from '@/lib/pricing'|addons.json|useCalculator" components/ app/` → импорты резолвятся. Объективный критерий готовности.
- **Диск-проверка (не на веру):** `lib/pricing.ts` (→ только несвязанный HTML в `skills/`), `addons.json` (**0**), `hooks/useCalculator.ts` (**0**), `components/calculator/*` (**0**), `app/api/calculate` (**нет**; есть `app/api/quote`) — **всех файлов РЕАЛЬНО НЕТ**. Значит Шаг 5.5 их правильно помечает как 🆕 задачи сборки, а НЕ ложно утверждает существование. Это и есть корректный фикс C7-02/03 (был риск `Module not found`).

### (b) 05_BUILD.md:27-30 пути common/* vs effects/* уточнены (C7-04)?
**РЕЗУЛЬТАТ: ДА, точно совпадает с диском.** ✅
- `05:28` — `components/effects/*`: `MagneticButton`, `ParallaxImage`, `AnimatedCounter`, `TextReveal`, `CustomCursor`, `NoiseOverlay`.
- `05:30` — `components/common/*`: `AnimatedSection`, `Skeleton`, `SkipLink`, `ScrollToTop`.
- **Диск-проверка (поимённо):**
  - `effects/MagneticButton.tsx` ✅ · `effects/AnimatedCounter.tsx` ✅ · `effects/TextReveal.tsx` ✅ · `effects/CustomCursor.tsx` ✅ · `effects/NoiseOverlay.tsx` ✅ · `effects/ParallaxImage.tsx` ✅
  - `common/AnimatedSection.tsx` ✅ · `common/Skeleton.tsx` ✅ · `common/SkipLink.tsx` ✅ · `common/ScrollToTop.tsx` ✅
  - (примечание: `ParallaxImage.tsx` существует и в `common/` копией — дубль кода, НЕ баг билд-дока; исполнитель берёт `effects/`.)
- Вывод: C7-04 закрыт — исполнитель найдёт каждый примитив по верному пути, без «ищи не там».

### (c) Реализуемость калькулятора/конструктора (zod, не заглушки)?
**РЕЗУЛЬТАТ: поднято с «нет zod» до «zod задан в билд-доке».** ✅ (частично в спецах, но теперь зафиксировано)
- Матмодели в `07`/`08` остались реальными (не заглушки): `calcTotal` `07:63-127`, `ADDONS` `07:136-153`, `MIN_GUESTS`/`CHILD_RATE` `07:52-60,156-165`, `ConstructorState` `08:12-29`.
- **C7-05 закрыт** через `41:131`: билд-док теперь несёт конкретную `contactSchema` с regex телефона `+7\d{10}` и `z.infer` — исполнитель не импровизирует. (Сами `07`/`08` по-прежнему пишут «Zod realtime» без `z.object`, но Шаг 5.5 это компенсирует как задачу Фазы 4 — приемлемо для спец→билд-связки.)

### (d) Нет ли в спецах ссылок на несуществующие файлы?
**РЕЗУЛЬТАТ: dangling-модули теперь помечены 🆕, НЕ «существуют».** ✅
- До W3: `41` молчал про `lib/pricing.ts`/`addons.json`/`useCalculator.ts`/`components/calculator/*`/`api/calculate` → сборщик бы упал.
- После W3: `41` Шаг 5.5 (`:118-134`) явно фиксирует все 6 как 🆕 + grep-гейт. Реальные компоненты (`sections/*`, `constructor/*`, `effects/*`, `common/*`, `ui/*`, `seo/*`, `MobileBottomNav`, `SmoothScrollProvider`, `lib/data.ts`, `lib/fonts.ts`) — все подтверждены на диске ранее и не изменились.

---

## 2. ЧТО ЗАКРЫТО ОТНОСИТЕЛЬНО W3

| Баг W3 | Sev | Статус в W4 | Где подтверждено |
|---|---|---|---|
| **C7-02** dangling `lib/pricing.ts`/`addons.json`/`useCalculator.ts` | HIGH | ✅ ЗАКРЫТ | `41:124-128` 🆕 + диск: файлов РЕАЛЬНО нет |
| **C7-03** `components/calculator/*` + `/api/calculate` не в `41` | MED | ✅ ЗАКРЫТ | `41:129-130` 🆕 + диск: папки/роута нет |
| **C7-04** пути `common/*` vs `effects/*` | MED | ✅ ЗАКРЫТ | `05:27-30` + диск поимённо |
| **C7-05** zod-схем нет | MED | ✅ ЗАКРЫТ | `41:131` `contactSchema` + `z.infer` |
| **C7-09** `catalog.json` противоречие CMS | LOW | ✅ ЗАКРЫТ | `41:132` 🆕 fallback |
| **C7-06** путь `NILOV_UNIFIED_MENU.md` | MED | ✅ ЗАКРЫТ | `41:126` «корень репо»; диск: файл есть в `/` |

---

## 3. ОСТАТОЧНЫЕ (LOW, НЕ блокируют PASS)

| ID | Sev | Суть | Рекомендация |
|---|---|---|---|
| **C7-01** (остаток) | LOW | `05:23` «его роль trust+бренд частично дублируется SocialProofBar» местами читается как «файла нет». Сам `PhilosophySection.tsx` на диске ЕСТЬ+orphan — фактически верно, но формулировка двусмысленна. | Привести к единой: «`PhilosophySection.tsx` РЕАЛЬНО существует как orphan — дописать WhyUs ИЗ него». |
| **C7-07** | LOW | «VIP-менеджер» в `07:190,227`/`08:119` противоречит деснобификации (`01_VISUAL_DNA`). Не в зоне правок 05/41, но висит в спецах. | Заменить на «персональный менеджер» в `07`/`08`. |
| **C7-08** | LOW | Гейт Фазы 1 `05:43` ставит субъективное «заказчик сказал вау» первым (объективное LCP/INP — вторым). | Поменять порядок: объективное первым. |

Эти 3 пункта — полировка; ни один не роняет ось ниже 5 и не мешает сборке.

---

## 4. ВЕРДИКТ

**PASS — 8.3 / 10.**

Билд-док исправлен точно по запросу комиссии и **подтверждён по диску**:
- `41_BUILD_CHECKLIST.md` Шаг 5.5 (`:118-134`) корректно фиксирует `lib/pricing.ts`, `addons.json`,
  `hooks/useCalculator.ts`, `components/calculator/*`, `POST /api/calculate`, `zod`-схемы и `catalog.json`
  как **🆕 задачи Фазы 4** с grep-гейтом — закрывает **C7-02/C7-03/C7-05** (+C7-06/C7-09).
- `05_BUILD.md:27-30` точно разделяет `components/effects/*` и `components/common/*` — совпадает с
  реальным деревом файлов, закрывает **C7-04**.

Оси 5/6/9 поднялись 6→8; итог 8.3 ≥ 8.0 ⇒ **PASS**. Остаток C7-01/C7-07/C7-08 — LOW, вне scope данной правки,
не влияет на вердикт.

*Критик #7 независим от автора спец и сборщика; балл выставлен по диск-верификации, не по старому вердикту.*
