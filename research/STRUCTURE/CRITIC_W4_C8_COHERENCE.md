# CRITIC_W4_C8_COHERENCE.md — ПОВТОРНЫЙ аудит оси Когерентность (8/9)

**Критик:** независимый (отдельный проход, без самооценки автора).
**Метод:** только ПО ДИСКУ — живой `grep -rn` по всей папке `STRUCTURE/` (93 .md) + чтение конкретных строк. Отчёты прошлых волн (`CRITIC_W3_C8_COHERENCE.md` и др.) НЕ приняты на веру, перепроверены на диске.
**Дата:** 2026-07-20 (повтор после правок W3).

---

## Результаты по 4 критериям (проверка по диску)

| # | Критерий | Статус W3 | Статус W4 (диск) | Доказательство |
|---|----------|-----------|------------------|----------------|
| (a) | Активный Playfair = 0 (кроме «НЕ Playfair») | FAIL (~30) | **FAIL (5 остаточных)** | см. ниже |
| (b) | «Интерфуд» как юр-имя = 0 (21B → ИП Нилов) | FAIL (1) | **PASS** | `21B_LOGIC_AUDIT_WAVE2:47` исправлено |
| (c) | Роспотребнадзор НЕ в «Клиенты» `04_BLOCKS`~1164 | FAIL (1) | **PASS** | `04_BLOCKS.md:1163` |
| (d) | Цвета едины | PASS | **PASS** | `01`/`06`/`04`/`39`/`29`/`43`/`05` |

### (a) Активный Playfair — FAIL (остаток 5 упоминаний, из них 1 в реальном спеке)
Живой grep `Playfair` по папке → 50 вхождений, но подавляющее большинство — это **мета-отчёты критиков** (`CRITIC_W1/W2/W3_*`, `18_CRITIC_SYSTEM`) и **корректные отрицания** («НЕ Playfair» в `01:58`, `06:76,106`, `39:28`, `00_PLAN:44`, `18:75`, `36_DESIGNCRITIC_A:277`). Большие нарушители W3 (`36_DESIGN_RUBRIC`, `36_A/B/C`, `09/10/11/13/16/19/25`) в живом grep **отсутствуют** → вычищены.

Остаточные АКТИВНЫЕ / stale-упоминания Playfair как присутствующего шрифта (не «не Playfair»):
1. **`07_CALCULATOR_SPEC.md:190`** — `цифра гостей Playfair 64px gold` — **реальный спек, активный Playfair** (не входил в список W3, пропущен ранее). Единственный активный Playfair в дизайн-спеке. → дефект.
2. `33_UXSIM_KAMILA.md:12` — `(abstract, Playfair, курсив-акцент)` — UX-симуляция описывает Hero шрифтом Playfair (канон = Cormorant). Stale.
3. `33_UXSIM_KAMILA.md:29` — `gold-on-ivory, Playfair` — то же, визуальный сигнал назван Playfair.
4. `29_DEVICECRITIC.md:98` — `при загрузке Playfair/Inter возможен layout-shift` — критик ссылается на Playfair как на загружаемый шрифт (stale, Playfair удалён).
5. `20_CRITIC_RESULTS.md:365` — `палитра/Playfair/Ken Burns ДНК сохранены` — неверное утверждение, что Playfair сохранён в ДНК.

**Вывод (a):** существенно улучшено (30 → 5, из них 1 активный спек + 4 narrative/critic/results). Но гейт «активный Playfair = 0» **не закрыт** → FAIL.

### (b) «Интерфуд» как юр-имя — PASS
`21B_LOGIC_AUDIT_WAVE2.md:47` (бывший FAIL) теперь читает: *«Компания реальна (ИП Нилов Дмитрий Игоревич, бренд NiloV Catering, тел +7(812)919-59-11 …)»* — legacy-имя «Интерфуд Кейтеринг» **удалено**.
Оставшиеся вхождения «Интерфуд» корректны: `04:779`/`33_UXSIM_GOSHA` — конкурент в контексте; `04:2087` — guard «НЕ ООО Интерфуд»; `45:41` — пометка ребрендинга; упоминания в `CRITIC_W2/W3_C*` — цитаты старого бага (не юр-имя). Юр-имя как ИП Нилов везде. → PASS.

### (c) Роспотребнадзор не в «Клиенты» — PASS
`04_BLOCKS.md:1163` (item 10 TrustProof, ранее ~1164): ряд клиентов = `Эрмитаж · Мариинский театр · Armani 🟡`; далее явно: *«**Роспотребнадзор — НЕ клиент, а госрегулятор** (см. `04:522`, `45:16`); упоминается только в `CertBlock`/`TrustBar` как регулятор/сертификация, не в ряду клиентов»*. Исключён из клиентов, помечен как регулятор. Внутрифайловое противоречие `04:1164`↔`04:522` закрыто. → PASS.

### (d) Цвета едины — PASS
Живой grep HEX: ядро `ivory #FAF7F2` / `ink #1C1815` / `gold #B08D57` совпадает в `01:35,37,39`, `06:22,23,28,43`, `04:26–60`, `39:15`, `29`, `43`, `05`. Историч. дрейф cream `#F2ECE3`↔`#F2EDE4` и warm-grey `#6B625A`↔`#6B635B` закрыт (`22_COHERENCE_REPORT:202`, Волна 4Б; `04:663`→`#F2ECE3`). `06_TOKENS` — единственный SSOT.
*Примечание (вне зоны структуры):* `app/globals.css` в коде по-прежнему тёмный (`#0A0A0A`) — это Фаза-0 сборки (`05`/`41`/`44`), документированный блокиратор, НЕ дефект когерентности спек-доков. Гейт grep тёмных HEX в коде → 0 остаётся задачей сборки.

---

## Оценка оси Когерентность

**Ось (8/9) Когерентность: 8 / 10**

Обоснование (честно, без завышения):
- 3 из 4 жёстких критериев — твёрдый PASS: юр-лицо (b), Роспотребнадзор (c), цвета (d).
- Критерий (a) радикально улучшен (30→5), но НЕ закрыт: 1 активный Playfair в реальном спеке (`07:190`) + 4 stale/narrative. Это снижает с 9–10 до 8.
- 0 критических a11y-ошибок по теме когерентности. Нет внутрифайловых/межфайловых противоречий по юр-лицу, клиентам, токенам.

## Вердикт: REWORK (лёгкий)

Причина: гейт (a) «активный Playfair = 0» не выполнен (остаток 5, из них 1 в спеке `07_CALCULATOR_SPEC.md:190`). Остальные 3 критерия — PASS. Близко к PASS; нужна только финальная зачистка Playfair.

### Остаточный bug-list (только то, что править)
| ID | Место | Суть | Фикс |
|----|-------|------|------|
| C8-W4-1 | `07_CALCULATOR_SPEC.md:190` | Активный Playfair в GuestsSlider («цифра гостей Playfair 64px gold») вопреки канону Cormorant | `Playfair` → `Cormorant` (или унифицировать с `--font-heading`) |
| C8-W4-2 | `33_UXSIM_KAMILA.md:12,29` | UX-симуляция называет Hero-шрифт Playfair | `Playfair` → `Cormorant` (совп. с ДНК) |
| C8-W4-3 | `29_DEVICECRITIC.md:98` | Stale «Playfair/Inter» в note про layout-shift | убрать Playfair из примера (Inter/Cormorant) |
| C8-W4-4 | `20_CRITIC_RESULTS.md:365` | «Playfair ДНК сохранены» — неверное утверждение | заменить на «Cormorant ДНК сохранена» |

После закрытия C8-W4-1..4 критерий (a) станет 0 → ось поднимается до 9–10, вердикт PASS.

---
*Покрытие: проверены ВСЕ 4 критерия задания живым grep по диску + чтение строк `21B:47`, `04:1163`, `07:190`, `06:76,106`, `29:98`, `20:365`, `33_UXSIM_KAMILA:12,29`. Файлы не правились (только этот отчёт).*
