# CRITIC W5 — C4 БЛОКИ (ось 4/9) — ФИНАЛЬНЫЙ повторный аудит

> Режим: независимый критик (читаю ТОЛЬКО диск, не верю CRITIC_W4_*.md).
> Источники на диске: `04_BLOCKS.md`, `06_TOKENS.md:181`.

## Проверяемые утверждения (по диску)

### (a) 04_BLOCKS:1441 — CTASection Primary → `/plan` (НЕ `/plan/calculator`)
**СТАТУС: PASS**
Диск, строка 1441:
```
- Primary (MagneticButton gold): «Спланировать событие» → `/plan`.
```
Ровно `/plan`. Вхождений `/plan/calculator` в блоке CTASection нет (Secondary — это телефон/WhatsApp/Telegram, см. 1442–1443). Один primary CTA, роут корректен.

### (b) 04_BLOCKS:1437 — overline = `text-gold-text` (НЕ `text-gold`, WCAG-AA 4.54:1)
**СТАТУС: PASS**
Диск, строка 1437:
```
`SectionHeading` overline `text-2xs uppercase text-gold-text` + заголовок ...
```
Используется `text-gold-text`, НЕ `text-gold`.

Кросс-проверка с SSOT `06_TOKENS.md:181`:
```
| `SectionHeading` | overline `text-2xs uppercase` **`text-gold-text`** (`#8A6D3B` = 4.54:1 ✓ AA)
  — [Волна 12, B7] было `text-gold` (2.89:1 FAIL); title `font-heading text-3xl text-foreground` |
```
Совпадает: `#8A6D3B` = 4.54:1 ≥ 4.5:1 (WCAG AA для мелкого текста) → порог пройден. Дефект устранён.

### (c) Глобально нет `text-gold` как текстового класса в 04_BLOCKS
**СТАТУС: PASS**
Поиск подстроки `text-gold` по всему файлу `04_BLOCKS.md` (2194 строки) → **1 совпадение**,
и это `text-gold-text` (строка 1437). Голых вхождений `text-gold` (без `-text`),
то есть класса с контрастом 2.89:1 FAIL, в файле нет. Утверждение выполнено.

## Оценка оси 4/9 (Блоки)

| Критерий | Оценка |
|---|---|
| Точность маршрутизации CTA (a) | 10/10 |
| Контраст overline / токены (b) | 10/10 |
| Отсутствие FAIL-контрастных классов (c) | 10/10 |
| Консистентность с SSOT `06_TOKENS` | 10/10 |

**Итоговая оценка оси: 9/10**
(минус 1 за ограниченный охват — проверены целевые 3 дефекта по задаче; полный
бенчмарк всех ~12 блоков в этой итерации не производился, но целевые правки подтверждены
на диске и согласованы с токен-SSOT).

## Вердикт
**PASS**

Все три целевых регрессионных утверждения подтверждены непосредственно по содержимому
файлов на диске (`04_BLOCKS.md` + `06_TOKENS.md:181`):
- CTA Primary ведёт на `/plan` ✓
- overline использует `text-gold-text` (#8A6D3B, 4.54:1, WCAG AA) ✓
- в `04_BLOCKS` отсутствует голый класс `text-gold` (2.89:1 FAIL) ✓

Ось 4 (Блоки) = **9/10**. Критических дефектов по проверенному объёму нет.
