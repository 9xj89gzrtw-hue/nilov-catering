# CRITIC W7 — Ось C1 (Визуал / Типографика) — ФИНАЛЬНЫЙ прогон

**Критик:** независимый (другая модель, не автор). Работа ПО ДИСКУ (живой grep/read), старым CRITIC_W*.md не верю.
**Скоуп:** весь набор STRUCTURE (116 .md). Обязательно прочитаны/проверены: `01_VISUAL_DNA`, `06_TOKENS`, `04_BLOCKS`, `07_CALCULATOR_SPEC`, `09/10/11` storyboards, + сплошной grep по всей папке.

---

## Резюме вердикта

| Критерий | Результат | Доказательство |
|---|---|---|
| (1) Шрифт заголовков = Cormorant, активный Playfair = 0 в ТАРГЕТ-спеках | **PASS** | Все дизайн-спеки чисты; residual только в 3 narrative/UX-файлах |
| (2) Светлая ДНК (ivory/ink/gold), нет residual тёмных hex в таргет-спеках | **PASS** | `#0A0A0A` в спеках — только legacy-описание («было»/«отменён»/OVERRIDE) |
| (3) Contrast-токены (`text-gold-text` #8A6D3B 4.54:1 AA) применены | **PASS** | `06:44,181`, применено `04:1437`, `10:10`, `11:10` |
| (4) Единая type-scale | **PASS** | `06:82–104` единая rem-clamp, один `--text-h1` для всех Hero |

**Оценка оси C1: 8.5 / 10 → ВЕРДИКТ: PASS**

---

## Подробно по критериям

### (1) Cormorant vs Playfair — PASS
- **ТАРГЕТ-канон чист:** `01_VISUAL_DNA:58` Cormorant (300–700, italic-акценты, кириллица); `06_TOKENS:76,106` `--font-heading:"Cormorant"... НЕ Playfair`; `39_CANON_INDEX:28`, `00_PLAN:44`, `18_CRITIC_SYSTEM:75` — все с корректным отрицанием «НЕ Playfair».
- **Storyboards чисты:** `09_HERO:25,41,78` Cormorant; `10:10,13,22` Cormorant; `11:10,18` Cormorant. Playfair отсутствует.
- **`07_CALCULATOR_SPEC:190` ИСПРАВЛЕН:** теперь «цифра гостей **Cormorant** 64px gold» (дефект C8-W4-1 закрыт). Grep Playfair по `07` → 0.
- **`16_BLOCK_BENCHMARKS`, `13`, `36_*`, `19`, `25`** — Playfair больше НЕ встречается (grep по глобу → 0). Крупные нарушители прошлых волн вычищены.
- Все хиты в `CRITIC_W1/W2/W3/W4_*` — мета-отчёты о самом слове, не спека.

### (2) Светлая ДНК — PASS
- `06_TOKENS:22,23,43` — `#FAF7F2` / `#1C1815` / `#B08D57`, светлая система полностью.
- `01_VISUAL_DNA:35,37,39` — ivory/ink/gold заданы канонически.
- Все `#0A0A0A` в спек-доках — ЛЕГАСИ-контекст: `01:3` («OVERRIDE blueprint… заложил тёмный»), `04:16,26,64,71` («было #0A0A0A» / «НЕ оставлять хардкод»), `00:18,36` («отменён»), `39:17` («legacy v1»). Активных тёмных таргет-значений нет.
- Тёмный `app/globals.css` (код) — документированный Фаза-0 build-блокиратор (`44`, `31`, `05`), НЕ дефект спек-доков.

### (3) Contrast-токены — PASS
- `06:44` `--color-gold-text: #8A6D3B` = 4.54:1 ✓ AA (с явным предупреждением, что `#B08D57` как текст = 2.89:1 FAIL, только заливка).
- Дисциплина применения: `06:181` SectionHeading overline `text-gold-text`; `04:1437`, `10:10`, `11:10` — overline gold-text #8A6D3B. Цены = ink (`06:180` PriceTag 16.5:1 AAA). Ink-на-золоте CTA = 5.7:1 (`06:29,174`).

### (4) Type-scale — PASS
- `06:82–104` единая rem-clamp шкала (модуль 1.333); `--text-h1 = clamp(2.5rem,5vw,4.5rem)` — ЕДИНЫЙ для Home Hero / EventHero / подстраниц; удалены конфликтующие px-токены `--text-4xl:59px`, `--text-hero:76px` (вторая «истина» устранена).

---

## Список RESIDUAL-проблем (file:line)

Все — вне ТАРГЕТ-дизайн-спеков (narrative/UX-sim/results); НЕ блокируют PASS, но остаются stale-упоминаниями удалённого Playfair.

| # | file:line | Проблема | Тяжесть |
|---|---|---|---|
| R1 | `33_UXSIM_KAMILA.md:12` | Hero описан «(abstract, **Playfair**, курсив-акцент)» — канон = Cormorant. Без отрицания | LOW (stale narrative) |
| R2 | `33_UXSIM_KAMILA.md:29` | «gold-on-ivory, **Playfair**» — визуальный сигнал назван Playfair | LOW (stale narrative) |
| R3 | `29_DEVICECRITIC.md:98` | «при загрузке **Playfair**/Inter возможен layout-shift» — ссылка на удалённый шрифт | LOW (stale critic note) |
| R4 | `20_CRITIC_RESULTS.md:365` | «палитра/**Playfair**/Ken Burns ДНК сохранены» — ЛОЖНОЕ утверждение (в ДНК Cormorant, не Playfair) | LOW-MED (неверный факт в results) |

**Рекомендация правок (не критиком):** R1–R4 — заменить `Playfair` → `Cormorant`. R4 наиболее важен (утверждает несуществующий факт). Дизайн-спеки трогать не нужно — они уже канонично чисты.

---

## Обоснование балла 8.5

Все 4 гейта критерия закрыты в ТАРГЕТ-спеках: единый Cormorant, светлая ДНК, WCAG-AA gold-text 4.54:1 с корректной дисциплиной заливка-vs-текст, единая clamp-шкала с устранённой дупликацией. Минус 1.5 — за 4 residual stale-Playfair в narrative/UX/results-файлах (в т.ч. 1 фактически неверное «Playfair ДНК сохранены» в `20:365`), которые засоряют когерентность набора, хотя и не входят в дизайн-канон. Порог PASS (≥8) уверенно взят.
