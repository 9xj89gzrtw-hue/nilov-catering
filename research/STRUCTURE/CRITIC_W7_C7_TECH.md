# CRITIC_W7_C7_TECH — ФИНАЛЬНЫЙ аудит. Тех/Билд (ось C7)

**Роль:** независимый критик #7 (НЕ писал код и НЕ правил спецы/билд-док). Объект — ВЕСЬ набор
STRUCTURE-файлов (115+ .md), фокус на осях: (1) dangling-модули без пометки 🆕; (2) zod;
(3) консистентность стека; (4) противоречия API-роутов. НЕ доверял старым вердиктам —
проверял **по диску** (`package.json`, реальная ФС репозитория, grep по `research/STRUCTURE/*`).
**Дата:** 2026-07-20.

---

## 0. РЕЗУЛЬТАТ

**ОЦЕНКА ОСИ C7: 8.0 / 10 → PASS (на нижней границе порога).**

| Проверка | Статус |
|---|---|
| (1) dangling-модули без 🆕 (`lib/pricing.ts`, `hooks/useCalculator.ts`, `addons.json`, `catalog.json`, `lib/video.ts`) | ⚠️ 4/5 закрыты; **`lib/video.ts` — висящая ссылка БЕЗ 🆕** (BUG-T1) |
| (2) zod-схемы в калькуляторе/конструкторе | ⚠️ только `contactSchema` в билд-доке; доменные схемы calc/constructor отсутствуют (BUG-T3) |
| (3) стек Next16 + framer-motion + lenis + zod консистентен | ✅ `package.json` совпадает 1:1; НО `AGENTS.md` лжёт про GSAP (BUG-T2) |
| (4) противоречия `/api/calculate` vs `/api/quote` | ✅ примирены, логически чисто |

> **Почему PASS, а не REWORK, но на границе:** тяжёлые баги прошлых итераций закрыты —
> 4 из 5 критических dangling-модулей помечены 🆕 в `41` Шаг 5.5, `contactSchema` задана,
> стек в `package.json` точно совпадает, `/api/calculate`⇄`/api/quote` примирены. Но ровно
> ОДИН из 5 проверяемых модулей (`lib/video.ts`) остаётся висящей ссылкой БЕЗ 🆕 (тот самый
> failure-mode, ради которого ось C7 и существует), плюс `AGENTS.md` противоречит сам себе
> по GSAP, плюс доменные zod-схемы не дописаны. Без закрытия BUG-T1/T2/T3 на след. итерации
> ось уходит в REWORK.

---

## 1. ПРОВЕРКА ПО ДИСКУ (4 вопроса заказчика)

### (1) Ссылки на несуществующие модули без пометки 🆕

**Метод:** `grep` по `research/STRUCTURE/*` + проверка ФС репозитория `ls`/`find`.

| Модуль | Где ссылается | На диске? | Помечен 🆕? | Вердикт |
|---|---|---|---|---|
| `lib/pricing.ts` | `04:84,1008`; `07:129`; `08:33` | ❌ нет | ✅ `41:126` | OK (C7-02 закрыт) |
| `hooks/useCalculator.ts` | `04:1846`; `07:212` | ❌ нет | ✅ `41:128` | OK (C7-02 закрыт) |
| `addons.json` | `07:133`; `41:127` | ❌ нет | ✅ `41:127` | OK (C7-02 закрыт) |
| `catalog.json` | `04:88,971,972`; `08:149` | ❌ нет | ✅ `41:132` (fallback) | OK (C7-09 закрыт) |
| `lib/video.ts` | `04:321` (VideoProvider/VideoRef, 6+ блоков) | ❌ нет | ❌ **НИГДЕ** | 🔴 **BUG-T1** |

**Диск-факт (не на веру):** `lib/pricing.ts`, `addons.json`, `hooks/useCalculator.ts`,
`catalog.json`, `lib/video.ts` — **реально отсутствуют** в репозитории. `hooks/useConstructor.ts`
**есть** (5815 байт) — корректно противопоставлен `useCalculator` в `41:128`.
Grep-гейт `41:134` проверяет только `lib/pricing|addons.json|useCalculator` → `lib/video.ts`
им выпадает и остаётся «висящим».

**Вывод (1):** 4/5 закрыты корректно (C7-02/03/09). `lib/video.ts` — единственная
незакрытая висящая ссылка (совпадает с BUG-1 из `CRITIC_W7_C4_BLOCKS.md:134-141`, который
**до сих пор не устранён** в `41`).

### (2) zod-схемы в калькуляторе/конструкторе

- `41:131` фиксирует `contactSchema` (`name:z.string().min(2)`, `phone:z.string().regex(/^\+7\d{10}$/)`,
  `date:z.string().min(1)`, `comment:z.string().optional()`) + `z.infer` + импорт из `zod`.
  → контактная форма (шаг 6 конструктора) покрыта. C7-05 закрыт **на уровне билд-дока**. ✅
- **ПРОБЕЛ:** доменные схемы для самого расчёта отсутствуют:
  - в `07`/`08` нет `z.object` для входов калькулятора/конструктора (guests 10–500,
    `format` enum, `tier` enum, выбор addon, гвард `MIN_GUESTS`);
  - `calcTotal`/`PRICE_PER_GUEST`/`ADDONS` — сырые TS-типы, НЕ zod;
  - `08:53` пишет «Zod realtime» **и** упоминает `z.infer`, тогда как `41:131` называет
    zod-схемы 🆕-задачей → **внутреннее противоречие**: `08` имплицирует zod уже в использовании,
    `41` — что его ещё предстоит добавить.
- **Вывод (2):** контактная zod есть; доменная zod для calc/constructor — нет (BUG-T3).

### (3) Консистентность стека Next16 + framer-motion + lenis + zod

**Реальный `package.json` (репозиторий):**
`next:16.2.10`, `react/react-dom:19.2.4`, `framer-motion:^12.42.2`, `lenis:^1.3.25`,
`zod:^4.4.3`, `shadcn:^4.13.0`, `zustand:^5.0.14`, `@react-pdf/renderer:^4.5.1`,
`@hookform/resolvers:^5.4.0`, `react-hook-form:^7.81.0`. **gsap — ОТСУТСТВУЕТ.**

- Совпадает 1:1 с `AGENTS.md` (кроме GSAP, см. ниже) и `15_REPO_AUDIT.md:9-11`. Устаревших
  версий нет. ✅
- `05_BUILD.md:35-36,61,91` + `06_TOKENS.md:148` + `15_REPO_AUDIT.md:13` единогласно:
  **GSAP НЕ в deps → всё на Framer Motion**. Решение сборкоспособно. ✅
- 🔴 **ПРОТИВОРЕЧИЕ в каноническом `AGENTS.md:19`:** там в «Verified stack» перечислен
  **`GSAP (animations)`** — прямо конфликтует с запретом GSAP во всех билд-доках и с
  реальным отсутствием gsap в `package.json`. Любой агент, читающий `AGENTS.md` первым
  (как предписано), получит ложный сигнал «GSAP в стеке». (BUG-T2)
- `37_MOTIONCRITIC_*` импортирует `framer-motion ^12.42.2` — консистентно. ✅

**Вывод (3):** версии в `package.json` консистентны; но `AGENTS.md` содержит устаревшую
ложную запись про GSAP → документальная рассинхронизация стека.

### (4) Противоречия `/api/calculate` vs `/api/quote`

- `07:211`: «State: `hooks/useCalculator.ts` … API `POST /api/calculate` дублирует расчёт для валидации.»
- `04:1822-1828` **явно примиряет**: локальный клиент-расчёт <50ms — ИСТОЧНИК ИСТИНЫ,
  `POST /api/calculate` — **ОПЦИОНАЛЬНЫЙ** server-side endpoint только для кросс-проверки +
  лида, вызывается **асинхронно/неблокирующе** с offline-фолбэком.
- `05:79`: репо уже имеет `/api/quote`; `/api/calculate` → сделать **алиас** ИЛИ переименовать
  логику в quote-роуте. `41:130` + `02_IA:168`: `/api/calculate` = 🆕 алиас → `app/api/quote`.
- **Диск-факт:** `app/api/quote/route.ts` **есть** (POST, пока stub: логирует + `{success:true}`).
  `app/api/calculate` — **нет** (корректно: будет алиасом). Гrep `api/calculate` по `app/|components/|lib/` → 0.
- 📝 Замечание: `05:79` «переименовать логику в quote-роуте» мoot — `/api/quote` уже stub без
  calc-логики, алиас тривиален. Не дефект, задокументировано для полноты (BUG-T5, INFO).

**Вывод (4):** дублирование `/api/calculate`/`/api/quote` **чисто примирено**, логического
противоречия нет. Роут-карта корректна.

---

## 2. BUG-LIST (file:line)

| ID | Sev | Суть | file:line |
|---|---|---|---|
| **BUG-T1** | MED | `lib/video.ts` (`VideoProvider`/`VideoRef`, ЕДИНАЯ точка свопа видео-провайдера, зависит 6+ блоков) сослана в `04:321` с полным кодом, НО **НЕ помечена 🆕** ни в `41` Шаг 5.5, ни где-либо ещё; на диске файла нет. Grep-гейт `41:134` её не ловит → риск «Module not found» при сборке. | `04_BLOCKS.md:321`; `41_BUILD_CHECKLIST.md:118-134` (нет строки) |
| **BUG-T2** | MED | `AGENTS.md:19` перечисляет `GSAP (animations)` в «Verified stack», прямо противоречит `05_BUILD.md:35-36,61,91` + `06_TOKENS.md:148` + `15_REPO_AUDIT.md:13` (GSAP НЕ в deps) и реальному `package.json` (gsap отсутствует). Ложный сигнал для агента. | `AGENTS.md:19` |
| **BUG-T3** | MED | Доменные zod-схемы calc/constructor отсутствуют: `41:131` даёт только `contactSchema`; в `07`/`08` нет `z.object` для входов (guests/format/tier/addons/`MIN_GUESTS`); `08:53` пишет «Zod realtime»+`z.infer`, а `41:131` называет zod 🆕-задачей → внутреннее противоречие статуса zod. | `07_CALCULATOR_SPEC.md:247`; `08_CONSTRUCTOR_SPEC.md:53`; `41_BUILD_CHECKLIST.md:131` |
| **BUG-T4** | LOW | Первые ссылки `lib/pricing.ts` (`04:84`) и `catalog.json` (`04:88`) даны в CMS-секции **без соседней 🆕**; маркер 🆕 живёт только в `41:126/132`. Сборщик, читающий `04` сверху вниз, может принять модули за существующие. | `04_BLOCKS.md:84`; `04_BLOCKS.md:88` |
| **BUG-T5** | INFO | `05:79` «переименовать логику в quote-роуте» — `/api/quote` уже stub без calc-логики, алиас тривиален. Не дефект. | `05_BUILD.md:79` |

---

## 3. РАЗБИВКА ОЦЕНКИ ПО СУБ-ОСЯМ

| Суб-ось | 0–10 | Обоснование |
|---|---|---|
| Целостность ссылок на модули (🆕 или валидный путь) | 7 | 4/5 закрыты; `lib/video.ts` висящий без 🆕 (BUG-T1) + первые ссылки `04` без 🆕 (BUG-T4) |
| zod-схемы (реальны, не заглушки) | 7 | `contactSchema` есть; доменные calc/constructor схемы нет (BUG-T3) |
| Версионная консистентность стека | 8 | `package.json`==AGENTS/15; но `AGENTS.md:19` лжёт про GSAP (BUG-T2) |
| Консистентность API-роутов | 9 | `/api/calculate`⇄`/api/quote` чисто примирены, диск подтверждает |
| Действие билд-дока (🆕/гейты) | 8 | Шаг 5.5 хорош, но grep-гейт не ловит `lib/video.ts` |
| Внутренняя консистентность 04↔07↔08↔41 | 8 | единая `calcTotal`, тарифы, роуты; нюанс zod-статуса (BUG-T3) |
| Реальность репо vs доки | 8 | диск совпадает с утверждениями; `lib/video.ts`/`useCalculator`/`pricing`/`addons`/`catalog` подтверждённо отсутствуют (ожидаемо 🆕) |

**Голос (округление до целого по оси C7): 8.0 / 10 → PASS** (на нижней границе;
суб-оси усреднённо ~7.9, округлено вверх до порога с явной оговоркой).

---

## 4. ВЕРДИКТ

**PASS — 8.0 / 10 (пограничный).** Билд-док прошёл тяжёлые итерации W3→W4 и закрыл
критические dangling-модули (`lib/pricing.ts`/`addons.json`/`useCalculator.ts`/`catalog.json`
→ 🆕 в `41` Шаг 5.5), задал `contactSchema`, согласовал версии и примирил `/api/calculate`⇄`/api/quote`.
Однако три резидуальных дефекта остаются именно в зоне ответственности оси C7:

1. **BUG-T1** — `lib/video.ts` ВИСИТ без 🆕 (тот же failure-mode, что и C7-02, но не закрыт).
2. **BUG-T2** — `AGENTS.md` противоречит стеку по GSAP.
3. **BUG-T3** — доменные zod-схемы calc/constructor не дописаны.

**MUST-FIX ПЕРЕД СБОРКОЙ (иначе след. итерация → REWORK):**
- добавить строку `lib/video.ts` (🆕) в `41_BUILD_CHECKLIST.md` Шаг 5.5 + в grep-гейт `41:134`;
- убрать `GSAP` из `AGENTS.md:19` (или пометить «не используется»);
- дописать `z.object` для calc/constructor-входов в `07`/`08` (или расширить `41:131`),
  устранив противоречие статуса zod между `08:53` и `41:131`.

*Критик #7 независим от автора спец и сборщика; балл выставлен по диск-верификации, не по старым вердиктам (W3/W4/W7_C4 учтены, но перепроверены заново).*
