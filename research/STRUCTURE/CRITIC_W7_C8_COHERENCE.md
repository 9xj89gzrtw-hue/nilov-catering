# CRITIC_W7 — Ось C8: КОГЕРЕНТНОСТЬ (независимый критик)

**Дата:** 2026-07-20 · **Критик:** отд. модель (не автор) · **Метод:** только диск, живой grep по `STRUCTURE/`.
**Объём:** весь набор STRUCTURE-файлов (121 .md). Обязательно прочитаны: `00_PLAN`, `01_VISUAL_DNA`, `04_BLOCKS`, `06_TOKENS`, `39_CANON_INDEX`, `41_BUILD_CHECKLIST`, `43_NAV_SPEC` + перекрёстные проверки `02_IA`, `09/10/11/13/16`, `18_CRITIC_SYSTEM`, `22_COHERENCE_REPORT`, `33_UXSIM_*`, `36_DESIGNCRITIC_*`, `40_BUILDDOC_CRITIC`, `44_CODEREALITY_AUDIT`, прошлые C8 (`CRITIC_W1/W3/W4_C8`).

---

## ИТОГОВЫЙ ВЕРДИКТ

| Метрика | Значение |
|---|---|
| **Ось C8 (Когерентность)** | **8 / 10** |
| **Вердикт** | **PASS** (≥8) |
| Блокирующих противоречий | 0 (критичных, ломающих сборку) |
| Наследственных/локальных дефектов | 4 (см. ниже, все правятся в спецах, не в коде) |

> Когерентность документ-набора **существенно выросла** по сравнению с ранними волнами. Флагманские оси — **шрифт** и **светлая палитра** — теперь едины по всем целевым файлам. Остаточные дефекты локальны и не ломают сборку, но требуют финальной зачистки в `04_BLOCKS` и унификации имён trust-блока/канона роутов.

---

## ОЦЕНКА ПО 5 КРИТЕРИЯМ ЗАДАЧИ

### (1) Cormorant везде, Playfair = 0 (кроме отрицаний) — **PASS (9/10)**

Живой grep `Playfair` по папке STRUCTURE (свежий прогон):
- Дизайн/спец-файлы `01/02/04/06/07/09/10/11/13/16/19/25` → **0 вхождений Playfair**.
- `36_DESIGNCRITIC_A_FOUNDATION.md:277` — единственное упоминание, и то **отрицание** («менее избитый display-сериф, не Playfair») → корректно.
- `07_CALCULATOR_SPEC.md` → **0** (претензия `CRITIC_W4_C8` C8-W4-1 «`07:190` Playfair 64px» **устарела** — исправлено).
- `33_UXSIM_KAMILA`, `29_DEVICECRITIC:98`, `20_CRITIC_RESULTS:365` → **0 Playfair** (претензии W4 C8-W4-2/3/4 **устарели**).
- `Cormorant` присутствует как канон в `00_PLAN:35,44`, `01:58`, `02_IA:184`, `06:76,106`, `39:28`, `43:128`, `04` (H1 Hero), `09/10/11` сториборды — **везде**.

⚠️ **Предупреждение методологии:** `CRITIC_W1_C1_VISUAL` (37+ Playfair) и `CRITIC_W4_C8_COHERENCE` (5 остатков) опирались на **устаревшее состояние** — их greps были сделаны до зачистки волн 14–15. На текущем диске активного Playfair в спецах **нет**.

### (2) Светлая ДНК (ivory/ink/gold) везде — **PASS (8/10, с локальным дефектом)**

- Канон светлой темы един: `00_PLAN:17`, `01:1-4` (OVERRIDE blueprint §1.8), `06:1`, `39` КАНОН №1, `43:114`. HEX-ядро совпадает: `ivory #FAF7F2` / `ink #1C1815` / `gold #B08D57` в `01:35,37,39` ↔ `06:22,23,28,43` ↔ `39:15` ↔ `43:114`.
- Тёмный `#0A0A0A`/`#F5F0EB`/`#722F37`/`#C9A96E` встречается **только** в описании legacy-кода `app/globals.css` (`04:15-17`, `15_REPO_AUDIT:19-21`, `44:37`) — это задокументированный блокиратор Фазы-0 сборки, **НЕ цель спеки** (`39` КАНОН №1, `41` Шаг 1 grep-гейт). Не считается дефектом когерентности спец-доков.
- **ДЕФЕКТ (внутриспековый):** встроенный в `04_BLOCKS` «исправляющий» сниппет кода (блок B1, строки 22–64) **противоречит SSOT `06_TOKENS`** на 2 значениях, которые `06` уже исправил по a11y:
  - `04:33` `--color-primary-foreground: #FFFFFF` (белый на золоте, 3.09:1 FAIL) ↔ `06:29` **должен быть `#1C1815`** (ink, 5.7:1 ✓ AA, Волна 12 B3).
  - `04:37` и `04:60` `--color-muted-foreground / --color-ink-soft: #6B625A` ↔ `06:33,59` **должен быть `#4A423B`** (поднят до AAA, Волна 3 V6).
  - `04:64` `.btn-primary { color: #FAF7F2 }` (белый текст на gold) ↔ `06 §5`/`01 §7` (ink на gold обязателен).
  - Это «копипаста» старых значений в инструкцию по фиксу — реальная рассогласованность спец↔SSOT. См. **Противоречия #1–#3**.

### (3) Имена блоков кросс-док (04/39/41/43) согласованы — **PASS (7.5/10, нейтрально-месиво)**

- `39_CANON_INDEX` + `41_BUILD_CHECKLIST` — надёжный мост `04`-имя ↔ код-файл. Основные блоки (`HeroSection`, `MenuPreview`, `GalleryTeaser`, `TestimonialsCarousel`, `ProcessSteps`, `CTASection`, `FAQTeaser`) согласованы.
- **Мести trust-блоков:** `TrustBar` (04 бл.2) / `AwardsStrip→TrustProof` (04 бл.3/57) / `SocialProofBar` (код, в который влиты TrustBar+Awards) / `TrustProof variant:'facts'` — 4 имени для одного логического proof-ряда (`22_COHERENCE_REPORT:83` это прямо отмечает). `39:53-55` и `41:55-56` объясняют merge, но единого каноничного имени в навигации/спеке нет — риск дублей при сборке (`40_BUILDDOC_CRITIC` BUG-F).
- `WhyUs` = `PhilosophySection.tsx` (существует, НЕ заимпортирован в page) — `41:65` корректно помечает как ♻️ orphan-файл. `40_BUILDDOC` BUG-B («PhilosophySection нет в коде») **устарел**: свежий grep по `05_BUILD.md` → 0 упоминаний, в `41` он описан как существующий.

### (4) Маршруты кросс-док согласованы — **PASS (7.5/10, с разрывом канона)**

- **Первичная навигация ПОЛНОСТЬЮ согласована** во всех файлах: `/events` `/menu` `/gallery` `/why-us` `/team` `/reviews` `/seasonal` `/plan` (+ `/plan/helper`, `/help/formats`, `/allergens`) — совпадают в `04:119,133-135`, `39:36-40`, `41:78-96`, `43 §1/§4/§7`, `02_IA:39-53,115-166`. Единый CTA-глагол C9 «Спланировать событие»→`/plan` согласован (`04:365,368`, `43 §2`, `41:44`).
- **Разрыв канона роутов между 41 и 43:** `41:96` декларирует «ПОЛНЫЙ КАНОН» из `02_IA` с расширенными роутами `/partners`, `/subscribe`, `/media-kit`, `/accessibility`, `/en`, `/careers`, `/account/orders` (плюс `/venues`, `/tasting`). Но `43_NAV_SPEC` §4 (футер — «единый источник истины для навигации») **не экспонирует НИ ОДИН из этих роутов** (только Навигация/Помощь/Информация из базового набора). `04` футер (`:132-141`) тоже их не содержит.

### (5) Нет осиротевших блоков — **PASS (7.5/10)**

- `TastingBooking` (`04:2138`) и `VenueList` (`04:2150`) **ОПРЕДЕЛЕНЫ** → претензия `CRITIC_W7_C9_GAPS` C9-1/C9-2 («данглинг роуты / блок отсутствует в 04») **ЛОЖНАЯ/устарелая** (подтверждено `CRITIC_W7_C3_IA:49`).
- **Осиротевшие роуты на уровне спецы:** `/partners` (B2B, Волна 11), `/subscribe` (Сергей), `/media-kit` (Юлия), `/accessibility` (WCAG-заявление), `/account/orders` (Петя P1/P2), `/en`, `/careers` объявлены каноничными в `41:96` (от `02_IA`), НО **не имеют блок-спеки в `04`** и не выведены в `43` навигацию. Это висячие роуты на уровне документации (блок не описан → сборщик не знает контракт).
- В коде (`44_CODEREALITY_AUDIT`) `PhilosophySection`/`WhyUs` — orphan-файл (есть, не заимпортирован) — это проблема кода, не спец-когерентности (закрывается сборкой по `41:65`).

---

## СПИСОК ПРОТИВОРЕЧИЙ (file:line)

| # | Файл:строка | Противоречие | Связь с каноном | Серьёзность |
|---|---|---|---|---|
| **1** | `04_BLOCKS.md:33` | `--color-primary-foreground: #FFFFFF` (белый текст на gold) | против `06_TOKENS.md:29` (`#1C1815` ink, 5.7:1 ✓ AA; белый=3.09:1 FAIL) | СРЕДНЯЯ (a11y, внутриспековая) |
| **2** | `04_BLOCKS.md:37` и `:60` | `--color-muted-foreground` / `--color-ink-soft: #6B625A` | против `06_TOKENS.md:33,59` (`#4A423B`, поднят до AAA, Волна 3 V6) | СРЕДНЯЯ (контраст sub/microcopy) |
| **3** | `04_BLOCKS.md:64` | `.btn-primary { color: #FAF7F2 }` — белый на gold | против `06 §5` / `01 §7` (ink на gold обязателен) | СРЕДНЯЯ (дубликат #1 на уровне CSS-правила) |
| **4** | `04_BLOCKS.md:273,545,679,855,1166` vs `39:53-55`, `41:55-56` | 4 имени trust-блока (`TrustBar`/`AwardsStrip`/`TrustProof`/`SocialProofBar`) без единого каноничного | `39`/`41` объясняют merge, но canonical-имя в навигации не зафиксирован | НИЗКАЯ (нейминг, риск дубля) |
| **5** | `41_BUILD_CHECKLIST.md:96` vs `43_NAV_SPEC.md §4` | Расширенный канон роутов (`/partners`,`/subscribe`,`/media-kit`,`/accessibility`,`/account/orders`,`/en`,`/careers`) не экспонирован в навигации `43` и футере `04` | `43` — «единый источник истины для навигации», но покрывает только базовый набор | НИЗКАЯ (роут-канон расходится) |
| **6** | `41_BUILD_CHECKLIST.md:96` + `02_IA.md:66,69` vs `04_BLOCKS.md` (блоки) | Роуты `/partners`,`/subscribe`,`/media-kit`,`/accessibility`,`/account/orders`,`/en`,`/careers` объявлены каноничными, но **блок-спеки в `04` отсутствуют** | осиротевшие роуты на уровне документации | НИЗКАЯ (висячие блоки) |

**НЕ подтверждено (ложные тревоги прошлых критиков, проверено свежим grep):**
- `CRITIC_W7_C9_GAPS:89-109` — `TastingBooking`/`VenueList` НЕ данглинг (есть в `04:2138,2150`).
- `CRITIC_W4_C8_COHERENCE:22-27` — Playfair в `07:190`/`33_UXSIM_KAMILA`/`29_DEVICECRITIC:98`/`20_CRITIC_RESULTS:365` — все очищены (0 вхождений).
- `CRITIC_W1_C1_VISUAL:8,31` — 37+ Playfair в целевых файлах — устарело, сейчас 0.

---

## РЕКОМЕНДАЦИИ (финальная зачистка перед сборкой)

1. **`04_BLOCKS.md` блок B1 (строки 22–64):** привести встроенный CSS-фикс к SSOT `06_TOKENS` — `primary-foreground`→`#1C1815`, `muted-foreground`/`ink-soft`→`#4A423B`, `.btn-primary` цвет→ink. Устраняет #1–#3.
2. **Trust-блок:** зафиксировать ОДНО каноничное имя (рекомендую `TrustProof` как логический, `SocialProofBar` как код-файл) и использовать его во всех спецах/навигации (#4).
3. **Роут-канон:** либо сократить «ПОЛНЫЙ КАНОН» `41:96` до базового набора `43`, либо добавить расширенные роуты в `43 §4` футер и написать недостающие блок-спеки в `04` (#5, #6).
4. (Вне зоны C8, но для полноты) `app/globals.css` тёмный — закрывается сборкой по `41` Шаг 1 (grep-гейт тёмных HEX → 0).

---

## ЗАКЛЮЧЕНИЕ

Ось C8 = **8/10, PASS**. Набор STRUCTURE-документов когерентен на флагманских осях (Cormorant-шрифт един, светлая ivory/ink/gold-ДНК — канон везде, первичные роуты согласованы во всех 7 ключевых файлах). Документация опережает код (это по замыслу, `41` Шаг 0 дисклеймер). Остаточные дефекты — локальное рассогласование токенов в «фикс»-сниппете `04` против SSOT `06`, месиво имён trust-блока и расхождение канона роутов 41↔43 с несколькими висячими блок-спеками — **все не блокирующие, все правятся в спецах**. Ранние критики (W1/W4) завышали дефекты по устаревшим данным; на текущем диске Порог PASS (≥8) пройден.
