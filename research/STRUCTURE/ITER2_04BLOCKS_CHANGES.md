# Отчёт Итерация 2 — правки `04_BLOCKS.md` (структура NiloV Catering)

Редактировался ТОЛЬКО файл `research/STRUCTURE/04_BLOCKS.md`. Код и прочие `.md` не тронуты.

## P0-1 (контраст gold как текст)
- **Результат: файл уже соответствует токенам `06_TOKENS`.** Единственное появление utility-класса золотого текста — `text-gold-text` (line 1479, блок 12 CTASection, overline), что и есть токен #8A6D3B (4.54:1 AA). Все остальные упоминания `gold` — заливки/рамки/фокус-кольца (`--color-gold`, `box-shadow` gold, `focus-visible 2px gold`, `MagneticButton gold`, `bg-gold-tint`) — по инструкции НЕ менялись. Доп. `text-gold` как текстовый класс в файле отсутствует. Правка не требовалась.

## P0-3 (фиктивный AI)
- line 210: публичный брендинг `AIConcierge` FAB → `Консьерж` (FAB). Добавлена внутренняя пометка: **«AI-слой — в резерве, модель не подключена»**; публично — «Консьерж»/«Помощник» (детерминированный wizard, НЕ «AI»).
- line 942: то же в `БЛОК 15.8 HelpersWizard` («резервный» `AIConcierge` FAB → FAB `Консьерж` + пометка AI-слоя в резерве).
- Противоречие «14:17 vs 08:136»: в самом `04_BLOCKS.md` явных ссылок `14:17`/`08:136` нет (номера — из других файлов свода). Внутри `04` оба места, где фигурировал AI-брендинг, приведены к единому решению (Помощник/Консьерж, wizard без генеративного AI). Локальное противоречие в `04` устранено.

## P0-6 (факт-гейт)
- line 88: добавлено **ПРАВИЛО FACT-GATE** (обязательно для `Testimonial`/`Review`/`TrustProofItem`/`AwardsStrip`/`Dish`/`Event`/`ClientLogo`): поле `status: 'verified' | 'pending'`; элементы `pending` НЕ рендерятся как голый факт (только при `verified` ИЛИ с дисклеймером «на проверке»).
- line 95 (`Dish`), line 99 (`Review`), line 107 (`TrustProofItem`+`ClientLogo`): добавлено обязательное `status: 'verified' | 'pending'`.
- line 397 и line 503: кофе-брейк «950» оставлен, но с пометкой **статус: `pending` — точная цена подтверждается при расчёте (на сайте указано 390 ₽)** (🟡 pending).
- line 534 (TrustBar) и line 1175 (TrustProof п.10): клиенты Эрмитаж/Armani/Газпром/Forbes помечены `status:'pending'` + дисклеймер «на проверке / пример из архива» (Роспотребнадзор как госрегулятор исключён).
- Унификация «18/19 лет» → **«18 лет (с 2007 года)»**: line 460 (FACTCHECK) и line 384 (Hero Sub).

## P0-8 (Hero 1 CTA)
- line 377 (intro Hero): зафиксирован канон — ровно **1 primary «Спланировать событие» → /plan + 1 secondary ghost «Смотреть форматы» → /events**; ценовые якоря и календарь брони вынесены ниже фолда.
- line 390–391: secondary CTA «Смотреть меню» → **«Смотреть форматы» → /events** (было /menu); прописаны `ctaPrimary.href="/plan"`, `ctaSecondary.href="/events"`.
- lines 502–507: добавлен нижний блок **`HeroExtras`** (НИЖЕ фолда) — туда перенесены ценовые якоря (М1), календарь брони (O1), Helper-CTA (G7), экспресс-пресеты, микро-подпись (K7).
- line 393: «ЦЕНА ВЫШЕ ФОЛДА» → «ЦЕНА — НИЖЕ ФОЛДА (блок `HeroExtras`)».
- line 438: «ПРОВЕРИТЬ ДАТУ ВЫШЕ ФОЛДА» → «НИЖЕ ФОЛДА (блок `HeroExtras`)».

## Оставшиеся противоречия
1. **14:17 vs 08:136** — номера ссылаются на другие файлы свода (не `04_BLOCKS`); внутри `04` AI-брендинг унифицирован, но источник противоречия вне зоны правки этого файла — требует сверки `14_*`, `08_*` по всему своду отдельной итерацией.
2. **Год основания 18/19** — внутри `04` унифицировано («18 лет (с 2007 года)»), но итоговое число всё ещё ждёт подтверждения у заказчика (TODO ПЕРЕД ПРОДОМ п.1), как и цены кофе-брейка 950 vs 390 ₽. Это намеренные 🟡 `pending`, а не ошибка правки.
3. **Волновые подпункты Hero** (G7/M1/O1/K7) по-прежнему описаны внутри секции Hero с пометками «закрывает …», но теперь явно переадресованы в `HeroExtras`; двойное описание (здесь + в `HeroExtras`) сохранено для трассируемости волн, не конфликтует.
