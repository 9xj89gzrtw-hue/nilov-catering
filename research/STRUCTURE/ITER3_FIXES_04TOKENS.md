# ITER3_FIXES_04TOKENS.md — Что исправлено (Итерация 3, ИСПРАВЛЯЮЩИЙ редактор)

**Дата:** 2026-07-20
**Скоуп:** ТОЛЬКО структура — `research/STRUCTURE/04_BLOCKS.md` и `research/STRUCTURE/06_TOKENS.md`. Код (`app/`, `components/`) НЕ трогался. Отчёт по file:line (номера строк — текущее состояние файлов после правок).

**Файлы изменены:**
- `04_BLOCKS.md` (BRAND BUG-1..4, TRUST, A11Y B2/B3/B7/B8/B10)
- `06_TOKENS.md` (A11Y B1/B3 — токены)
- Создан этот отчёт `ITER3_FIXES_04TOKENS.md`

---

## BRAND — ось (ITER3_BRAND BUG-1..4)

### BUG-1 (публичный лейбл «Премиум» → «Максимальный»)
Слово «Премиум» убрано изо ВСЕХ публичных лейблов тарифов (внутренний `tier:'premium'|'luxury'` в TS-типах оставлен — это НЕ публичная строка, по критике не баг). Заменено на нейтральное «Максимальный», синхронизировано во всех местах:

- `04_BLOCKS.md:106` — модель `Package.name`: «Эконом / Стандарт / Расширенный / **Максимальный**»
- `04_BLOCKS.md:991` — `PackageGrid` публичные лейблы: «Эконом / Стандарт / Расширенный / **Максимальный**»
- `04_BLOCKS.md:993` — «ЕДИНАЯ ТАБЛИЦА ТАРИФОВ … те же 4 публичных лейбла (Эконом/Стандарт/Расширенный/**Максимальный**)»
- `04_BLOCKS.md:1018` — `pricePerGuest` ПУБЛИЧНЫЕ лейблы карточек: «Эконом / Стандарт / Расширенный / **Максимальный**»
- `04_BLOCKS.md:1026` — Dirty-check: «верхний тариф `luxury` (публично «**Максимальный**»)»
- `04_BLOCKS.md:1509` — FAQTeaser: «банкет «**Максимальный**» — верхний тариф»
- `04_BLOCKS.md:1593-1596` — `PackageGrid` цены: Фуршет/Банкет/Кофе-брейк — ПУБЛИЧНЫЕ лейблы «Эконом / Стандарт / Расширенный / **Максимальный**» + чек-лист «**Максимальный** — 12 канапе…» (бывш. «Премиум»)
- `04_BLOCKS.md:1861-1862` — «Фуршет **Максимальный** = 4 350» / «Названия пакетов: … **Максимальный**» (внутренний `luxury` = публично «Максимальный»)
- `04_BLOCKS.md:653` — таблица минимальных чеков: «Банкет | **Максимальный** | от 6 970 ₽»
- `04_BLOCKS.md:1226` — BlogPost case-заголовок: «меню **Максимальный** за 5 дней»

### BUG-4 (JSON-LD «Фуршет Премиум» → «Фуршет»)
- `04_BLOCKS.md:1321` — schema.org `Product.name` было `"Фуршет Премиум"` → `"Фуршет"` (публичная микроразметка больше не несёт запрещённое слово; grep-gate `премиум` в публичных строках → 0).

### BUG-2 (фейк-лого статусных брендов → generic «нам доверяют», status:'pending')
Узнаваемые бренды (Эрмитаж/Armani/Газпром/Forbes/Мариинский) убраны из рядов «клиентов» — заменены на анонимные плейсхолдеры с дисклеймером «на проверке» и `status:'pending'`:

- `04_BLOCKS.md:531` — `TrustBar` marquee: «набор анонимных силуэтов-плейсхолдеров «нам доверяют» 🟡, статус `pending` — конкретные бренды НЕ называются до подтверждённых договоров»
- `04_BLOCKS.md:540` — Копирайт/данные TrustBar: `Корпоратив «А»` · `Свадьба «Б»` · `Лофт-площадка` · `Бизнес-центр` · `Загородный клуб` 🟡 (статус `'pending'`, дисклеймер «на проверке»)
- `04_BLOCKS.md:556` — aria-label клиента: «Клиент: нам доверяют 🟡»
- `04_BLOCKS.md:551` — медиа-ассеты: `public/demo/logos/{client-a,client-b,client-c,wedding-d,...}.svg` (убраны `hermitage/maryinsky/armani/gazprom`)
- `04_BLOCKS.md:1182` — TrustProof п.10: «Клиенты: `Корпоратив «А»` … `Загородный клуб` 🟡 *(статус `'pending'` — анонимные плейсхолдеры «нам доверяют», НЕ называем конкретные бренды (Эрмитаж/Armani/Газпром/Forbes)…)*» + подпись «от семейного ужина до банкета на 500 гостей — один стандарт качества»

### BUG-3 («приём в Эрмитаже» как доказательство → убрано)
- `04_BLOCKS.md:535` — GROUNDING-ПОДПИСЬ TrustBar: «От семейного ужина до **банкета на 500 гостей** — один стандарт качества» (убрана музейно-дворцовая отсылка «приём в Эрмитаже»)
- `04_BLOCKS.md:548` — «приземляющий» ряд: убрана фраза «Элит-клиенты … остаются как доказательство потолка качества»; заменено на «НЕ называем статусных клиентов … как доказательство потолка качества … держим анонимные плейсхолдеры «нам доверяют»»
- `04_BLOCKS.md:1182` / `04_BLOCKS.md:1184` — подпись и Волна 10: «от семейного ужина на 10 до банкета на 500 — один стандарт качества» (убрано «приёма в Эрмитаже»)

> Примечание: оставшиеся упоминания Эрмитаж/Armani/Газпром/Forbes в `04` теперь ТОЛЬКО в негативном/политико-правильном контексте (grep-gate `04:102`, модель `Event` `04:118` с `status:'pending'`, FACTCHECK-статус `04:473` «НЕ называем», TrustProof `04:1198` «до подписанных договоров НЕ называем») и как ДЕМО-данные слотов (площадки-примеры, помеченные 🟡/DEMO). Это НЕ нарушает бан-лист `40_BRAND_VOICE §3`.

---

## TRUST — ось (ITER3_TRUST)

### TRUST-1 (реальный механизм fact-gate, не только проза)
`04_BLOCKS.md:88-103` — FACT-GATE переписан из декларации в проверяемый контракт:
- Добавлено **значение по умолчанию** `status` = `'pending'` (fail-closed, НЕ fail-open): «отсутствие/пустой статус трактуется как `'pending'`».
- Добавлен **обязательный render-guard (псевдокод)** `renderFactItem(item)` с ветками: `verified` → рендер; `pending` + `disclaimer` → рендер с видимым дисклеймером; иначе `return null`.
- Добавлен **CI-assert**: prod-снапшоты `TrustProof`/`Hero`/`WhyUs` не содержат числовой/годовой claim без видимого `disclaimer` или `status:'verified'`; grep-gate на `премиум|Armani|Газпром|Эрмитаж` в публичных строках → 0.
- Условие 2 гейта уточнено: дисклеймер должен быть **ВИДИМ пользователю**, НЕ только редакторская пометка 🟡.

Поле `status` добавлено во ВСЕ модели, перечисленные в гейте (раньше было только у `Dish`/`Review`/`TrustProofItem`/`ClientLogo`):
- `04_BLOCKS.md:118` — **`Event`** — добавлена модель с **`status: 'verified' | 'pending'`**
- `04_BLOCKS.md:119` — **`Testimonial`** — добавлена модель с **`status: 'verified' | 'pending'`**
- `04_BLOCKS.md:120` — **`AwardsStrip`** — добавлена модель (объединена в `TrustProof`) с **`status: 'verified' | 'pending'`**
- `Review` (`04:99`) и `ClientLogo` (`04:107`) уже несли `status` — оставлено.

### TRUST-2 (Hero «3000 событий» — видимый дисклеймер)
- `04_BLOCKS.md:402` — Sub Hero: «Более 3000 событий **(на проверке)** за 18 лет (с 2007 года) 🟡 …» (пометка «(на проверке)» теперь ПРЯМО в видимом тексте + 🟡, оба условия гейта выполнены).

### TRUST-3 (460: не цитировать FACTCHECK как подтверждение)
- `04_BLOCKS.md:473` — СТАТУС FACTCHECK переписан: убраны ✅ и «ПОДТВЕРЖДЕНО живым сайтом»; заголовок «🟡 БЕЗ ПОДТВЕРЖДЕНИЯ»; явно указано, что FACTCHECK_REPORT (стр. 19, 51) помечает «3000+/3500+» как ❓ no-data и «ссылка на отчёт FactChecker невоспроизводима»; публиковать ТОЛЬКО как «3 500+ 🟡» со статусом `pending`.

> Не правил (вне явных инструкций задачи, но смежно): вложенный «фактически 3 500+» в TrustProof `04:1180` и цена «950» `04:397/503` остались — они уже несут 🟡/pending-дисклеймер, и задача прямо требовала трогать только 389 и 460. Если нужно убрать «3 500+» из дисклеймера — отдельная правка.

---

## A11Y — ось (ITER3_A11Y B1-B11)

### 06_TOKENS.md — токены (B1/B3)
- `06_TOKENS.md:38` — `--color-ring: #B08D57` → **`#8A6D3B`** (gold-text). Focus-ring на ivory = **4.537:1 ≥ 3:1** (WCAG 1.4.11 ✅); на secondary = 4.129:1 ✅. Закрывает B3/B4/B5/B6 (кольцо тиражируется через `ring-ring`/`focus-visible:ring-2` на все формы/кнопки).
- `06_TOKENS.md:44` — `--color-gold-text` дополнен ограничением: на `secondary` (#F2ECE3) = 4.13:1 < AA, НЕ использовать на secondary-фоне.
- `06_TOKENS.md:45` — **НОВЫЙ токен** `--color-gold-text-on-secondary: #6E5631` (на secondary = **5.885:1 ✅ AA**, на ivory = 6.467:1). Для золотого текста на cream-секциях.

### 04_BLOCKS.md — использование токенов (B2/B7/B8/B10)
- `04_BLOCKS.md:1495` — CTASection overline: `text-gold-text` → **`text-gold-text-on-secondary`** (#6E5631, 5.885:1 на secondary, закрывает B2).
- `04_BLOCKS.md:179` — `ScrollProgress`: `color?: def gold` → **`def var(--color-ring)`** (#8A6D3B, 4.537:1 ≥ 3:1), явно «НЕ чистый `--color-gold` #B08D57 (2.893:1 < 3:1)» (закрывает B7).
- `04_BLOCKS.md:1602` — PackageGrid «Popular» frame: `--color-gold` 2px → **`--color-gold-text`** 2px (#8A6D3B, на secondary 4.129:1 ≥ 3:1; было #B08D57 = 2.632:1 FAIL) (закрывает B8).
- `04_BLOCKS.md:144` — кнопка звонка: золотой текст номера → **`var(--color-gold-text)`** (#8A6D3B, 4.537:1 на ivory ≥ AA; было «золотой/тёмный» двусмысленно); иконка телефона → цвет `var(--color-gold-text)` #8A6D3B (на ivory 4.537:1 ≥ 3:1 по 1.4.11), НЕ `#B08D57` (2.89:1 FAIL), либо иконка внутри gold-заливки (закрывает B10).

> B9 (декор-рамка логотипов 1px) — не тронута: по отчёту это info/low (декоративная рамка, не обязана быть ≥3:1), и задача явно перечисляла ScrollProgress/Popular-frame/иконку телефона. B11 — ОК по отчёту, оставлено.

---

## Проверка
- Grep по `04_BLOCKS.md` на публичный «Премиум»: остался ТОЛЬКО во внутренних `tier:'premium'` (TS-типы, не публичная строка) и в негативных упоминаниях брендов в политике/ДЕМО-данных — не нарушает бан-лист.
- Grep по `04_BLOCKS.md` на `Эрмитаж|Armani|Газпром|Forbes` в рядах «клиентов»: заменено на анонимные плейсхолдеры; оставшиеся — в негативном/ДЕМО контексте с 🟡/`pending`.
- `06_TOKENS.md`: `--color-ring` = #8A6D3B, добавлен `--color-gold-text-on-secondary` = #6E5631.

## Код
НЕ редактировался (задача: править ТОЛЬКО структуру `research/STRUCTURE/`).
