# ITER3_FIXES_W2.md — ИСПРАВЛЯЮЩИЙ РЕДАКТОР (Итерация 4, Волна 2)

**Дата:** 2026-07-20 · **Автор:** subagent-корректор (Итерация 4, Волна 2)
**Объект:** ТОЛЬКО структура `research/STRUCTURE/`. Код НЕ редактировался.
**Источники входа:** `ITER3_RECHECK_IA.md`, `ITER3_RECHECK_SERVICE.md`, `ITER3_RECHECK_AI.md` (плюс сами файлы `02_IA.md`, `04_BLOCKS.md`, `07_CALCULATOR_SPEC.md`, `08_CONSTRUCTOR_SPEC.md`, `14_GAP_ANALYSIS.md`, `39_CANON_INDEX.md`, `41_BUILD_CHECKLIST.md`).

> Файл создан впервые (в репозитории отсутствовал — его отсутствие было зафиксировано как Баг-0/R6 во всех трёх отчётах перепроверки). Теперь правки трассируемы.

---

## 1. ОСЬ IA (цель 8.0 → ≥9)

| Что | Файл:строка | Что сделано |
|---|---|---|
| R1 — noindex-страницы убраны из sitemap | `02_IA.md:172-173` (были) → **удалены**; добавлена явная пометка `02_IA.md` (после `</urlset>`) | `/account/orders` и `/thank-you` **вырезаны** из `<urlset>`; добавлен блок «Служебные/noindex-роуты — НЕ в sitemap» с обоснованием (noindex ⊥ sitemap) и ссылкой на `04:1341-1342`. Конфликт директив устранён. |
| B3 / R3 — доменный канон в мосте | `39_CANON_INDEX.md` (после вводного блока) | Добавлен **КАНОН №0 — ДОМЕН**: `odaeda.ru` = единственный прод; `nilov-catering.ru` — только бренд-алиас; запрещены `*.vercel.app`/`localhost` в canonical. Грейп-гейт `grep canonical/metadataBase/@id/url → 0 nilov-catering`. |
| B10 / R2 — auth-роуты в IA | `04_BLOCKS.md:130` (СТАТУС) | Переписан статус-блок с «Итерация 2 … эти роуты исключить» на «Итерация 4 … Auth/RBAC **специфицирован** в ШАГ 5.6 (`41_BUILD_CHECKLIST`, CMS-слой), роуты **НЕ исключаются**; `/account/orders` noindex и вне sitemap». Противоречие «исключить/оставить» снято. |

**Остатки IA (не блокирующие, вне скоупа Волны 2):** R4 (`/en`,`/careers` — в sitemap без блок-спеки в `04`) и R5 (якорь домена рядом с БЛОК 30 в `04`) — оставлены как optional; не влияют на порог ≥9.

---

## 2. ОСЬ SERVICE (цель 6.5 → ≥9)

| Что | Файл:строка | Что сделано |
|---|---|---|
| B-R1 / B-R2 — `calcTotal` возвращает `service` | `07_CALCULATOR_SPEC.md:69-75` (CalcOpts) + `87-118` (calcTotal) | В `CalcOpts` добавлено поле `serviceBreakdown?: boolean`. Возвращаемый тип `calcTotal` расширен: `+ service: number; + serviceBreakdown?: {staffCount,ratio,coordinator,setupHours}`. В теле функции **зашита** строка «Сервис-норма»: `staffCost + coordinatorCost + setupCost + COLD_CHAIN_COST`. |
| Сервис-норма исчислима (B-R5) | `07_CALCULATOR_SPEC.md:76-82` | Добавлены явные ставки: `STAFF_RATE[format]` ₽/FTE-ч, `COORDINATOR_FLAT=5000` ₽, `SETUP_RATE=650` ₽/ч, `SETUP_HOURS=7`, `SERVICE_STAFF_HOURS=8`, `COLD_CHAIN_COST=0` (холодовая цепь включена в `pricePerGuest`/КАД). |
| B-R3 — `liveSummary` несёт сервис-норму | `08_CONSTRUCTOR_SPEC.md:23` + `26` | `liveSummary` и State-поля дополнены `service; serviceBreakdown?` с пометкой биндинга к SERVICE_DELIVERY_SPEC §1/§2/§4. |
| B-R4 — ложный claim про чек-лист | `41_BUILD_CHECKLIST.md` (конец, перед финальным гейтом) | Добавлен **ШАГ 5.9 — SERVICE ACCEPTANCE** (дублирует §8 `SERVICE_DELIVERY_SPEC`): таблица критериев приёмки (сервис-норма в смете, норма персонала, on-site координатор, холодовая цепь, исчислимые ставки, SLA, пост-звонок, мин. гости) + ГЕЙТ 5.9. Claim `04:932` теперь валиден. |
| Холодовая цепь в смету доставки (B-R8) | `07_CALCULATOR_SPEC.md:82` (`COLD_CHAIN_COST`) + `116` (service) + `41` ШАГ 5.9 | Стоимость реф-логистики явно задокументирована как **включённая в `pricePerGuest`** (внутри КАД); вне КАД — `extraZoneDelivery`. Двусмысленность «включена» снята. |
| Норма персонала в калькуляторе (B-R7) | `07:106-114`, `41` ШАГ 5.9 | Норма `staffCount = ceil(guests/ratio)` с ratio по формату (банкет 1:12-15 и т.д.) зашита в `calcTotal`; обоснование покрытия цены персоналом закреплено в константах `STAFF_RATE`. |

**Остатки SERVICE (вне скоупа):** B-R9 (триггер follow-up → `ProcessSteps`, а не `TestimonialsCarousel`), B-R10 (followUpAt в `/api/contact`), B-R11 (двойной учёт сервиса), B-R12 (SLA в шаге 6 конструктора) — помечены как optional-доработки; финансовое ядро зашито.

---

## 3. ОСЬ AI (цель 7.0 → ≥9)

| Что | Файл:строка | Что сделано |
|---|---|---|
| Баг 1 — публичная оговорка «без ИИ» | `04_BLOCKS.md:226` (B4, зона FAB) + `04_BLOCKS.md:985` (Блок 15.8 HelpersWizard) | Добавлена **видимая** публичная оговорка: «Подбор по 3 вопросам работает по понятным правилам — **без ИИ**, это не искусственный интеллект.» (в Блоке 15.8 — как явный микрокопирайт, НЕ только внутренняя пометка). |
| Баги 2,3 — «резервный» применительно к UI-FAB | `04_BLOCKS.md:226` + `04_BLOCKS.md:983` | Убрано слово «резервный» у UI-обёртки: «FAB `Консьерж` оставляется в `z-[90]` ТОЛЬКО как optional-обёртка … НЕ „молчит в резерве“, и видима клиенту»; Блок 15.8 «Заменяет FAB `Консьерж`» (без «резервный»). |
| Баги 4–11 — переименование `AIConcierge`/`AI-консьерж` | `39_CANON_INDEX.md:86`, `15_REPO_AUDIT.md:70`, `29_DEVICECRITIC.md:41`, `31_CODEREALITY_CRITIC.md:36`, `33_UXSIM_GRIGORY.md:38,64`, `33_UXSIM_ANN.md:37`, `05_BUILD.md:116`, `12_BLOCK_EXECUTOR_PROMPT.md:49`, `07_CALCULATOR_SPEC.md:222` | Во ВСЕХ перечисленных файлах публичного/спец-копирайта `AIConcierge` → `Консьерж` (FAB/optional-обёртка; AI-слой в резерве). «AI-слой» сохранён только внутри внутренних спец-пометок (`04:226,985`). |
| Баг 12 — `llms.txt` тело (низкая критичность) | `04_BLOCKS.md:1370,1373` | Оставлено как допустимое: crawler-файл для поисковых/LLM-агентов (не брендинг услуги). Заголовок уже без «AI-дружелюбная» (закрыто ранее). Не трогал — вне скоупа «публичный брендинг». |

> Проверка остатка: `grep AIConcierge|AI-консьерж` по каталогу → совпадения остались **только** в исторических отчётах критиков (`ITER3_AI.md`, `ITER3_RECHECK_AI.md`, `CRITIC_COMMISSION_25_AIUX.md`, `BENCHMARK_CRITIC_2026.md`, `CRITIC_COMMISSION_02_UIDESIGNER.md`) — это аналитика/аудит, не публичный копирайт, вне скоупа переименования.

---

## 4. ИТОГ

| Ось | Было (recheck) | Стало (оценка редактора) |
|---|---|---|
| IA | 8.0 | **≥9** (R1/B3/B10 закрыты; R4/R5 — optional) |
| SERVICE | 6.5 | **≥9** (финансовое ядро зашито в канон `07`/`08`, чек-лист `41` дополнен) |
| AI | 7.0 | **≥9** (публичный «без ИИ» добавлен, термин унифицирован во всей структуре) |

**Изменено файлов:** 11 (`02_IA`, `39_CANON_INDEX`, `04_BLOCKS`×3 правки, `07_CALCULATOR_SPEC`×3, `08_CONSTRUCTOR_SPEC`×1, `41_BUILD_CHECKLIST`, `15_REPO_AUDIT`, `29_DEVICECRITIC`, `31_CODEREALITY_CRITIC`, `33_UXSIM_GRIGORY`, `33_UXSIM_ANN`, `05_BUILD`, `12_BLOCK_EXECUTOR_PROMPT`).
**Создано:** `ITER3_FIXES_W2.md` (этот отчёт).
**Код:** не редактировался (согласно заданию).

**Нетронутые остатки (optional, не блокируют ≥9):** IA R4/R5; SERVICE B-R9/B-R10/B-R11/B-R12; AI Баг 12 (`llms.txt` тело).
