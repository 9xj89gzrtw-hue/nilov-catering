# ITER3_RECHECK_AI — Финальная микро-перепроверка оси AI-TRANSPARENCY (Итерация 3)

**Дата:** 2026-07-20 · **Режим:** финальная микро-перепроверка, strict / devil's-advocate
**Скоуп:** ТОЛЬКО структура `research/STRUCTURE/`
**Перечитаны (по заданию):** `04_BLOCKS.md`, `ITER3_AI.md`, `ITER3_FIXES_W2.md`
**Фактически прочитано:** `04_BLOCKS.md` (2337 строк, пост-фикс), `ITER3_AI.md`, `ITER3_FIXES_04TOKENS.md`, плюс целевой grep/reserve-проверка по `08_CONSTRUCTOR_SPEC.md`, `14_GAP_ANALYSIS.md` и всему каталогу `research/STRUCTURE/` на паттерны `AIConcierge|AI-консьерж|в резерве|Консьерж|Помощник|llms\.txt|без ИИ`.

> **⚠️ КРИТИЧЕСКОЕ РАБОЧЕЕ РАСХОЖДЕНИЕ.** Файл `ITER3_FIXES_W2.md`, указанный в задании как третий обязательный к перечитке, **в репозитории ОТСУТСТВУЕТ**. В каталоге `research/STRUCTURE/` есть `ITER3_FIXES_04TOKENS.md` (BRAND/TRUST/A11Y, **без AI-оси**), но **никакого фикс-отчёта по оси AI нет**. Это означает, что правки по AI-оси (переименование `AIConcierge`→`Консьерж`, единый статус резерва) были внесены прямо в `04_BLOCKS.md`/`08`/`14` **без специфицирующего отчёта об исправлениях** — они верифицируются только по диску, не по документу фиксов. См. Баг-0.

> **Смещение строк.** Номера строк из прошлого критика `ITER3_AI.md` (211 / 943 / 1325) **устарели** — `04_BLOCKS.md` вырос до 2337 строк. Актуальные позиции: резерв-пометка — `04:224` и `04:982`; `llms.txt` — `04:1368–1373`. Все ссылки ниже — по ТЕКУЩЕМУ состоянию файла.

---

## 1. Чек-лист из задания (4 явных пункта)

| # | Требование задания | Статус | Где в файле / факт |
|---|---|---|---|
| 1 | Устранено противоречие статуса резерва — **ОДИН вариант**: «AI-слой в резерве, модель не подключена» | ✅ **ИСПРАВЛЕНО** | `04:224` и `04:982` — дословно «**AI-слой — в резерве, модель не подключена**» (дважды, консистентно). `08:136` — «**AI-модель — в резерве, не подключена**» + «Единый статус: UI-обёртка Консьерж — видима …; AI-модель — в резерве». `14:17` — «Консьерж (AI-модель в резерве, не подключена …)». Старое самопротиворечие `08:136` («НЕ в резерве» + «резервным») — **закрыто**, теперь единый вариант. |
| 2 | Унифицирован термин: «Консьерж»/«Помощник» в публичном, «AI-слой» только в спеце | ⚠️ **ЧАСТИЧНО** (см. Баги 4–11) | Внутри `04`/`08`/`14` публично — «Консьерж»/`Помощник» (04:223,224,982), «AI-слой» — только во внутренней спец-пометке (04:224,982). Позитивного «AI-консьерж» в 04/08/14 нет. **НО** в других файлах `research/STRUCTURE/` термин `AIConcierge`/`AI-консьерж` **сохранился** (39_CANON_INDEX, 15_REPO_AUDIT, 29_DEVICECRITIC, 31_CODEREALITY, 33_UXSIM_GRIGORY×2, 33_UXSIM_ANN, 05_BUILD, 12_BLOCK_EXECUTOR_PROMPT) → термин НЕ унифицирован по всей структуре. |
| 3 | Публичная оговорка «без ИИ» у помощника | ❌ **НЕ ИСПРАВЛЕНО** (см. Баг 1) | В пользовательском копирайте НЕТ ни одной оговорки «без ИИ» / «работает по правилам». Hero CTA «Я не знаю, что нужно — помогите →» (`04:406`, `04:519`, `04:983`), Блок 15.8 `HelpersWizard` (`04:980–994`) — только «Помощник». Фраза «детерминированный wizard, НЕ «AI»» есть лишь во **внутренней** спец-пометке `04:982` (внутри `*…*`), не в публичном тексте. Исходный BUG 8 из `ITER3_AI.md` — **не закрыт**. |
| 4 | `llms.txt` без «AI-дружелюбная» в публичном брендинге | ✅ **ИСПРАВЛЕНО** (заголовок) | `04:1369` — «… **Машиночитаемая** карта сайта (для поисковых и LLM-агентов)». «AI-дружелюбная» удалена. *Оговорка:* в теле `llms.txt` осталось «для **AI**-поиска» (`04:1368`) и «для **AI**» (`04:1371`) — см. Баг 12 (низкая критичность, не нарушает строгую формулировку п.4). |

---

## 2. Скоринг оси AI-TRANSPARENCY

**Итоговая оценка: 7 / 10** (строго ≤ 9 по регламенту Волны 2; НЕ согласился автоматически с «всё исправлено»).

**Обоснование:**
- ✅ **+2 к исходным 6/10** — жёсткое перекрёстное противоречие резерва (`ITER3_AI` BUG 1,2) реально устранено: во всех трёх файлах теперь ОДИН вариант «AI-слой/модель в резерве, не подключена», самопротиворечие `08:136` снято.
- ✅ **+1** — «AI-дружелюбная» убрана из заголовка `llms.txt` (`04:1369`); нейтральные SEO-упоминания `llms.txt`/`helps AI systems` (`04:1179,1280,1308`) допустимы по `ITER3_AI` «Не баги».
- ✅ Термин унифицирован **внутри 04/08/14** (публично «Консьерж»/«Помощник», «AI-слой» — только в спеце).
- ❌ **НЕ закрыто (п.3 задания):** публичная оговорка «без ИИ» у помощника отсутствует в user-facing копирайте — клиент видит только «Помощник»/`/plan/helper` без понимания, что это правила, а не ИИ. Это ядро всей оси прозрачности (`ITER3_AI` BUG 8 не исправлен).
- ❌ **Частично не закрыто (п.2 задания):** термин `AIConcierge`/`AI-консьерж` жив в ≥9 файлах `research/STRUCTURE/` вне тройки 04/08/14 (Баги 4–11). Исполнитель, читающий `39_CANON_INDEX`/`15_REPO_AUDIT`/`29_DEVICECRITIC`/`33_UXSIM_*`, снова получит запрещённое имя → риск ресинхрона при сборке (исходный BUG 4 лишь наполовину закрыт).
- ⚠️ `04:224`/`04:981` возвращают слово «резервн**ый**» применительно к **UI-FAB** («резервным ТОЛЬКО как optional-обёртка», «резервный FAB Консьерж») — это мутит правило «резерв = модель, НЕ обёртка» (Баги 2,3).
- ⚠️ **Баг-0:** нет фикс-отчёта `ITER3_FIXES_W2.md` → правки AI-оси нетрассируемы.

**Вывод:** прогресс очевиден и значителен (резерв-противоречие убрано, `llms.txt` очищен), но 2 из 4 явных чек-пунктов задания закрыты не полностью. До «8–9» ось не дотягивает из-за отсутствия публичной оговорки «без ИИ» и остаточного `AIConcierge` за пределами 04/08/14.

---

## 3. Баги (file:line + альтернатива) — 12 шт. (≥8 требуемых)

| # | Файл:строка | Проблема | Альтернатива |
|---|---|---|---|
| 0 | `ITER3_FIXES_W2.md` (отсутствует) | Файл, названный в задании как обязательный фикс-отчёт AI-оси, **не существует**. Правки AI (переименование, единый резерв) внесены в 04/08/14 без документа-фикса, нетрассируемы и неверифицируемы по отчёту. | Создать `ITER3_FIXES_W2.md` по образцу `ITER3_FIXES_04TOKENS.md` со строками до/после для AI-оси (`04:224,982`, `08:102,136`, `14:17`). |
| 1 | `04_BLOCKS.md:980–994` (Блок 15.8 `HelpersWizard`, публичный копирайт) | Нет публичной оговорки «без ИИ» / «по правилам». Клиент видит «Помощник»/`/plan/helper`, не зная, что это rule-based, а не ИИ. Исходный `ITER3_AI` BUG 8 не закрыт. | Добавить в публичный микрокопирайт под кнопкой/в Блоке 15.8: «Подбор по 3 вопросам работает по понятным правилам — **без ИИ**» (видимый `disclaimer`, НЕ только внутренняя пометка). |
| 2 | `04_BLOCKS.md:981` | «Заменяет «**резервный**» FAB `Консьерж` (G5)» — слово «резервный» применено к UI-обёртке, хотя канон «резерв = модель, НЕ обёртка». Мутит единый вариант. | «Заменяет FAB `Консьерж` (G5)» (убрать «резервный»). |
| 3 | `04_BLOCKS.md:224` | «FAB `Консьерж` оставляется в `z-[90]` **резервным** ТОЛЬКО как optional-обёртка … НЕ «молчит в резерве»» — в одной фразе «резервным» + «НЕ молчит в резерве» самопротиворечиво; «резервным» снова относится к UI. | «FAB `Консьерж` оставляется в `z-[90]` ТОЛЬКО как optional-обёртка над `/plan/helper` (видимая, НЕ «молчит в резерве»)» — убрать «резервным». |
| 4 | `39_CANON_INDEX.md:74` | В глобальном индексе компонентов `app/layout.tsx` перечислен **`AIConcierge`** — запрещённое имя, противоречит унификации. | `Консьерж` (FAB, optional-обёртка над `/plan/helper`; AI-слой в резерве). |
| 5 | `15_REPO_AUDIT.md:70` | «ai: **`AIConcierge`**, `VoiceSearch` — В РЕЗЕРВ» — старое имя в аудите репозитория. | «ai: `Консьерж` (AI-слой в резерве, модель не подключена), `VoiceSearch` — В РЕЗЕРВ». |
| 6 | `29_DEVICECRITIC.md:41` | «**`AIConcierge`** (FAB …» — и здесь же `z-[50]`, тогда как `04:223` явно «НЕ `z-[50]`, `z-[90]`». Двойное расхождение: имя + z-index. | «`Консьерж` (FAB, `z-[90]`)» + привести z-index к `04:223`. |
| 7 | `31_CODEREALITY_CRITIC.md:36` | «…`AIConcierge`, `CustomCursor`» в масштабе `text-cream` — аудит кодреальности цитирует запрещённое имя. | `Консьерж` (FAB). |
| 8 | `33_UXSIM_GRIGORY.md:38,64` | Персона Григория дважды цитирует **`AIConcierge`** FAB («упомянут в 04 лишь как … помечен „в резерве“»; «B-GRIG-5. `AIConcierge` не „в резерве“…»). | `Консьерж` (FAB, optional-обёртка; AI-модель в резерве). |
| 9 | `33_UXSIM_ANN.md:37` | Персона Анны: «(опц.) **`AIConcierge`** FAB» — тот же остаток. | `Консьерж` (FAB). |
| 10 | `05_BUILD.md:116` | «AR-меню, **AI-консьерж/чат-бот**, голосовой поиск …» — в списке резерва build-плана сохранён префикс «AI». | «AR-меню, `Консьерж` (AI-модель в резерве, не подключена)/чат-бот, голосовой поиск …». |
| 11 | `12_BLOCK_EXECUTOR_PROMPT.md:49` | «без **AI-консьержа**, без AR» — исполнительский промпт инструктирует избегать «AI-консьержа», но сам термин называет. | «без `Консьерж`-AI-слоя, без AR» (или «без ИИ-консьержа» → «без подключённой AI-модели»). |
| 12 | `04_BLOCKS.md:1368,1371` (`llms.txt`, тело) | В заголовке «AI-дружелюбная» убрана (п.4 ✅), но в теле осталось «статичный файл для **AI**-поиска» (`04:1368`) и «структуру … для **AI**» (`04:1371`). Низкая критичность (crawler-файл, не брендинг), но дух «без AI в публичном» не до конца выдержан. | «статичный файл для поисковых и LLM-агентов»; «структуру услуг/меню для поисковых агентов (LLM-дружелюбно)». |

**Не баги (подтверждено):** `04:1179,1280,1308` (упоминания `llms.txt`/`helps AI systems`/ChatGPT/Perplexity/Claude) — относятся к SEO-индексации для поисковых LLM-агентов, не к брендингу сервиса; допустимы по `ITER3_AI` «Не баги». `04:1356` «ТОЛЬКО по правилу» — про `aggregateRating`, не про помощника.

---

## 4. Резюме верификации (по диску, не по вере в фикс-отчёт)

- **Резерв-статус (1 вариант):** ✅ консистентен в 04/08/14.
- **Термин в 04/08/14:** ✅ унифицирован.
- **Термин во ВСЕЙ структуре:** ❌ остаток `AIConcierge`/`AI-консьерж` в ≥9 файлах (Баги 4–11).
- **Публичная оговорка «без ИИ»:** ❌ отсутствует в user-facing копирайте (Баг 1).
- **`llms.txt` заголовок:** ✅ без «AI-дружелюбная».
- **Трассируемость фиксов AI:** ❌ `ITER3_FIXES_W2.md` отсутствует (Баг 0).

**Чек-лист для Iteration 4 (закрыть до ≥8/10):**
1. Добавить публичную оговорку «без ИИ / по правилам» в Блок 15.8 и Hero-CTA-хелпер (Баг 1).
2. Вычистить `AIConcierge`/`AI-консьерж` из всех файлов `research/STRUCTURE/` (39_CANON_INDEX, 15_REPO_AUDIT, 29_DEVICECRITIC, 31_CODEREALITY, 33_UXSIM_GRIGORY, 33_UXSIM_ANN, 05_BUILD, 12_BLOCK_EXECUTOR_PROMPT) → заменить на «Консьерж» (Баги 4–11).
3. Убрать «резервн**ый**» применительно к UI-FAB в `04:224,981` (Баги 2,3).
4. Создать `ITER3_FIXES_W2.md` с file:line до/после по AI-оси (Баг 0).

*Конец отчёта `ITER3_RECHECK_AI.md`. Связано: `04_BLOCKS.md`, `08_CONSTRUCTOR_SPEC.md`, `14_GAP_ANALYSIS.md`, `ITER3_AI.md`, `ITER3_FIXES_04TOKENS.md`.*
