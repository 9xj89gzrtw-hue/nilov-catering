# ITER3_RECHECK_SERVICE.md — ФИНАЛЬНАЯ микро-перепроверка оси SERVICE (Итерация 3)

**Ось:** SERVICE (операционный сервис день-X: персонал, холодовая цепь, координатор, SLA, пост-звонок).
**Объект проверки:** связность `SERVICE_DELIVERY_SPEC.md` ↔ `04_BLOCKS.md` ↔ `07_CALCULATOR_SPEC.md` ↔ `08_CONSTRUCTOR_SPEC.md` ↔ `41_BUILD_CHECKLIST.md`. ТОЛЬКО структура (не prose спецы).
**Метод:** независимая перепроверка без самооценки. Перечитаны: `04_BLOCKS.md` (calcTotal / DeliveryZonesMap / EventTypeSelector staffing), `08_CONSTRUCTOR_SPEC.md`, `14_GAP_ANALYSIS.md`, `ITER3_SERVICE.md`, `SERVICE_DELIVERY_SPEC.md`. **Файл `ITER3_FIXES_W2.md` НЕ НАЙДЕН** (см. B-R6).
**Дата:** 2026-07-20. ДНК: светлая (подтверждено `04_BLOCKS.md:8-77`, `06_TOKENS`).
**Критик:** независимый (diff-модель).

---

## Вердикт (скоринг)

**6.5 / 10** (строго ≤ 9; «связь восстановлена, но финансовое ядро сервиса не реализовано в каноне»).

По сравнению с `ITER3_SERVICE.md` (5.0/10) ось **существенно улучшена**: обратная связь `04_BLOCKS → SERVICE_DELIVERY_SPEC` теперь **присутствует** (предыдущий отчёт опирался на устаревшую версию `04_BLOCKS`, где этих ссылок не было). Маппинг staffing, холодовая цепь, SLA-пропсы и упоминание пост-звонка/TestimonialsCarousel — на месте.

**НО** центральное обязательство итерации — «сервис-норма учтена в смете» — **провалено в канонической реализации**: `04_BLOCKS.md` *утверждает*, что `calcTotal` возвращает строку `service = staffCost + coordinatorCost + setupCost` (+ `opts.serviceBreakdown`), однако **канонический `calcTotal` в `07` этого не делает**, `liveSummary` в `08` этого поля нет, а `41_BUILD_CHECKLIST` (куда `04` обещает продублировать §8) раздела «Service acceptance» **не содержит**. То есть связь *описана*, но *не зашита в код спеки* — при сборке строка «Сервис-норма» тихо не появится в расчёте.

---

## Что ЗАКРЫТО относительно `ITER3_SERVICE.md` (5.0)

- **Связь spec→blocks есть.** Биндинг-раздел `04_BLOCKS.md:922-930` + маппинг `637-646` + холодовая цепь `1944` + SLA-пропсы `331,334` + пост-звонок `885` + TestimonialsCarousel в таблице `929`. Ранее заявленные «0 упоминаний» (ITER3_SERVICE B1) **более не верны**.
- **Маппинг staffing по типам событий** (`04_BLOCKS.md:637-646`) — явная таблица event→format→ratio (Корпоратив→Фуршет 1:15-20, Свадьба→Банкет 1:12-15, …). Закрывает старый B4.
- **Холодовая цепь в доставке** (`04_BLOCKS.md:1944`) — реф-фургоны, логгеры ≤+6 °C, разрыв >+8 °C/30 мин → утилизация. Закрывает старый B3.
- **SLA в блоке** (`04_BLOCKS.md:331,334`) — `slaResponseHours=2` / `slaBookingConfirm='O3'` в Props `AvailabilityCalendar`. Закрывает старый B5.
- **Пост-звонок +24ч и TestimonialsCarousel упомянуты** (`04_BLOCKS.md:885,929`). Закрывает старые B6/B9 (с оговоркой — см. B-R9/R10).

---

## Таблица багов (file:line + альтернатива)

| # | Локус | Суть разрыва | Альтернатива (fix) |
|---|-------|--------------|--------------------|
| B-R1 | `04_BLOCKS.md:1062-1066` ↔ `07_CALCULATOR_SPEC.md:87-118` | **`calcTotal` канона НЕ возвращает строку `service`.** `04` утверждает «`calcTotal` добавляет line-item `service = staffCost + coordinatorCost + setupCost`», но каноническая сигнатура `07:93` = `{ base, discount, addonsTotal, total, perGuest }`. Поля `service`/`staffCost`/`coordinatorCost`/`setupCost` в функции **отсутствуют**. Разработчик, собирая по `07`, строку «Сервис-норма» не получит. | Либо дописать в `calcTotal` (`07:87-118`) возврат `service` + `serviceBreakdown`; либо убрать утверждение из `04:1062` (не объявлять реализованным то, чего нет в каноне). |
| B-R2 | `04_BLOCKS.md:1066` ↔ `07_CALCULATOR_SPEC.md:69-75` | **`opts.serviceBreakdown` не существует.** `04` ссылается на `opts.serviceBreakdown` для `ResultDisplay`/`SummaryCard`, но `CalcOpts` в `07:69-75` содержит только `discounts/bookingDays/items/extraZoneDelivery/childGuests`. Поле не объявлено → TS-ошибка при сборке по канону. | Расширить `CalcOpts` (`07:69`) и return-тип (`07:93`) полями `service` и `serviceBreakdown:{staffCount,ratio,coordinator,setupHours}`. |
| B-R3 | `04_BLOCKS.md:1066` ↔ `08_CONSTRUCTOR_SPEC.md:23,26` | **`liveSummary` конструктора не несёт сервис-норму.** `08:23` = `{ guests; format; tier; perGuest; total; savings }`; `08:26` = `subtotal; addOnsTotal; total; perGuest; savings`. Нет `service`. Даже если `calcTotal` вернёт `service`, конструктор его не прокинет в `SummaryCard`. | Добавить `service` и `serviceBreakdown` в `liveSummary` (`08:23`) и в блок `SummaryCard` (08 шаг 5, `51`). |
| B-R4 | `04_BLOCKS.md:930` ↔ `41_BUILD_CHECKLIST.md` (весь файл) | **Ложное утверждение о чек-листе.** `04:930`: «Чек-лист приёмки (`41_BUILD_CHECKLIST`) дублирует §8 этой спецы как раздел „Service acceptance (SERVICE_DELIVERY_SPEC)“». Реальный `41_BUILD_CHECKLIST.md` (168 строк) раздела «Service acceptance»/сервис/холодовая цепь/координатор/staffing **НЕ содержит** (grep → 0 совпадений). Критерии приёмки сервиса НЕ попадут в build-гейт. | Либо добавить в `41_BUILD_CHECKLIST.md` раздел «Service acceptance (SERVICE_DELIVERY_SPEC)» с чек-листом из §8; либо убрать claim из `04:930`. |
| B-R5 | `04_BLOCKS.md:1063-1065` | **Сервис-норма не имеет чисел.** `staffCost = f(format, guests)` (без тарифа FTE/час), `coordinatorCost` (flat-позиция без суммы), `setupCost` (7 ч без ставки). Line-item заявлен, но **неисчислим** — разработчик не может посчитать сумму. | Задать явные ставки: `STAFF_RATE[format]` ₽/FTE-час, `COORDINATOR_FLAT` ₽, `SETUP_RATE` ₽/ч; прописать в `07` рядом с `PRICE_PER_GUEST`. |
| B-R6 | отсутствует `ITER3_FIXES_W2.md` | **Файл фиксов W2 не найден.** Задание требовало перечитать `ITER3_FIXES_W2.md`, но он отсутствует в репозитории (поиск по дому: `ITER3_FIXES*` → только `ITER3_FIXES_04TOKENS.md`; `FIXES_W*` → 0; `ITER3_FIXES_W2*` → 0). Фиксы «волны 2» **неверифицируемы** — нельзя подтвердить, что заявленные правки применены. | Создать/локализовать `ITER3_FIXES_W2.md` ИЛИ явно перечислить применённые правки в `04_BLOCKS` (с пометками «закрыто Wave-X» уже есть, но метка «W2» не резолвится в файл). |
| B-R7 | `04_BLOCKS.md:646` ↔ `07_CALCULATOR_SPEC.md:162` | **Мин. гостей vs норма персонала не сверены с ценой** (старые B7/B11 не закрыты числом). Chef-at-home `MIN_GUESTS=10` + норма 1:8-10 → ~1–2 FTE + обязательный координатор (2 FTE) на 10 гостей при «от 4 500». Банкет `MIN_GUESTS=15`, норма 1:12-15 + координатор → 3 FTE на 15 гостей. Ни в `04`, ни в `07` нет подтверждения, что эти цены покрывают персонал. | Либо поднять `MIN_GUESTS` (банкет ≥20, chef-at-home ≥12); либо в `07` обосновать, что персонал уже в базе (с цифрой), устранив двусмысленность. |
| B-R8 | `04_BLOCKS.md:1944` ↔ `07_CALCULATOR_SPEC.md:73,116` | **Холодовая цепь «включена в КАД» без стоимости в смете.** `04:1944` показывает «Холодовая цепь (реф-фургон + логгеры) — включена в КАД». Но `calcTotal` (`07:116`) учитывает только `extraZoneDelivery` (надбавка ЗА КАД), а стоимость реф-логистики внутри КАД нигде не заложена. Либо она «в базе еды» (не доказано числом), либо недоучтена. | Либо вынести `COLD_CHAIN_COST` в базу `calcTotal` явно; либо задокументировать, что реф-логистика входит в `pricePerGuest` (со ссылкой), убрав двусмысленность «включена». |
| B-R9 | `04_BLOCKS.md:929` | **Ошибочная атрибуция блока.** Таблица биндинга приписывает `TestimonialsCarousel` (БЛОК 9) связь «§2 +24 ч пост-звонок координатора». Но триггер follow-up — `ProcessSteps` шаг 5 (`04:885`), а не карусель отзывов. Карусель — соц-доказательство, не оркестратор звонка. | Исправить строку таблицы: `ProcessSteps` → §2 +24ч пост-звонок; `TestimonialsCarousel` оставить только как «сервис-блок (доказательство)», без триггера follow-up. |
| B-R10 | `04_BLOCKS.md:885` ↔ `08_CONSTRUCTOR_SPEC.md:25` | **Пост-звонок «вне сайта», но назван частью «контракта качества сайта».** `04:885` явно: follow-up — CRM-процесс, поля `followUpAt` в `/api/contact` нет. При этом `04:885` именует его «обязательной процедурой контракта качества сайта». `08:25` `contact = { name; phone; date; comment }` — `followUpAt` отсутствует. Противоречие в ярлыке. | Либо добавить `followUpAt` (или `needsCoordinatorCall:true`) в payload `/api/contact` и `08:25`; либо убрать из формулировки «контракта качества сайта» (оставить «CRM-процесс»). |
| B-R11 | `04_BLOCKS.md:1065` | **Двойной учёт / двусмысленность сервиса.** `04:1065`: «сервис уже в базе цены (… НЕ противоречит явному line-item: здесь он виден клиенту как честная расшифровка»). Одновременно сервис «в базе» И «отдельной строкой» — при реализации это либо двойной счёт (база уже включает + ещё раз line-item), либо пустая расшифровка. Старый B2 не закрыт по существу. | Явно выбрать модель: (а) сервис ОТКРЫТО вынесен отдельным line-item и исключён из `pricePerGuest`; ИЛИ (б) сервис в базе, line-item — только read-only расшифровка без суммы. Задокументировать однозначно. |
| B-R12 | `04_BLOCKS.md:334` ↔ `08_CONSTRUCTOR_SPEC.md:53` | **SLA не продублирован в шаг 6 конструктора.** `04:334` требует «продублировать [slaBookingConfirm='O3'] в `08` шаг 6». Шаг 6 «Контакт» (`08:53`) несёт только `minBookingDays` + загрузку брифа; SLA-полей/статуса «подтверждено» там нет. | Добавить в шаг 6 / `ContactForm` отображение SLA-статуса (≤15 мин / «подтверждено»), синхронно с `AvailabilityCalendar` (`04:331`). |

---

## Дополнительные замечания (не баги, влияют на связность)

- **`14_GAP_ANALYSIS.md`** не упоминает ось SERVICE вообще — это нормально (gap-анализ про исходники → структура, не про операционку), но означает, что SERVICE не прошёл через «честные пропуски» и не имеет независимого coverage-чека. Рекомендую добавить строку «Операционный сервис (SERVICE_DELIVERY_SPEC) — покрыт биндингами в 04» для полноты.
- **Старый `ITER3_SERVICE.md` (5.0)** фактически отменяется этим отчётом: его ключевой тезис «0 упоминаний SERVICE в 04» ложен для текущей версии `04_BLOCKS`. Новый баланс: связь есть (зачтено), реализация в каноне — нет (B-R1/R2/R3/R4).

---

## Итог

Ось SERVICE **структурно связана** с блоками (биндинги `04→SERVICE_DELIVERY_SPEC` присутствуют: маппинг staffing `637-646`, холодовая цепь `1944`, SLA `331/334`, пост-звонок `885`, TestimonialsCarousel `929`). Это поднимает ось выше прежних 5.0.

**НО финансовое ядро итерации не зашито в канон:** `calcTotal` в `07` не возвращает строку «Сервис-норма» (B-R1/R2), `liveSummary` в `08` её не несёт (B-R3), `41_BUILD_CHECKLIST` не дублирует §8 (B-R4, ложный claim в `04:930`), стоимость персонала/холодовой цепи не исчислима (B-R5/R8), а мин. гости vs норма не сверены с ценой (B-R7). Плюс файл фиксов `ITER3_FIXES_W2.md` отсутствует (B-R6) — верификация правок невозможна.

**Рекомендация перед сборкой:** закрыть B-R1, B-R2, B-R4 (критичны — иначе «сервис в смете» останется декларацией) и B-R5/B-R7 (числовое обоснование). Без них независимый критик не может подтвердить, что «премиум-сервис» реально учтён в цене, а не только описан.

**Скоринг: 6.5 / 10** (связь восстановлена, реализация в каноне — нет).

*Конец отчёта `ITER3_RECHECK_SERVICE.md`. Связано: `SERVICE_DELIVERY_SPEC.md`, `04_BLOCKS.md`, `07_CALCULATOR_SPEC.md`, `08_CONSTRUCTOR_SPEC.md`, `41_BUILD_CHECKLIST.md`, `14_GAP_ANALYSIS.md`, `ITER3_SERVICE.md`.*
