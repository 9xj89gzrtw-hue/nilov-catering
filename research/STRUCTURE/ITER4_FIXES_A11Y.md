# Итерация 4 — ИСПРАВЛЕНИЯ оси ACCESSIBILITY / CONTRAST (gold-token binding)

**Ось:** accessibility • contrast (золото на светлой ДНК)
**Правились ТОЛЬКО:** `04_BLOCKS.md`, `06_TOKENS.md`
**Опоры:** `ITER4_RECHECK_A11Y.md` (F1–F12), `06_TOKENS.md`, `04_BLOCKS.md`
**Дата:** 2026-07-20 (Итерация 5, исправляющий редактор)
**Не тронуто:** код (`app/`, `components/`).

> Примечание: цитируемые в `ITER4_RECHECK_A11Y.md` номера строк (181/455/614/1113/1509/1656)
> незначительно смещены относительно текущего состояния файлов после предыдущих волн.
> Ниже привязка выполнена по **реальному содержимому** — каждый голый «золотой»/gold-применение
> найден живым поиском и привязан к токену.

## 1. Что исправлено в `04_BLOCKS.md` (specificity-debt устранён)

Латентные gold-применения без токена → привязаны к контраст-безопасному токену:

| Блок (применение) | Было | Стало | Контраст |
|---|---|---|---|
| Стайки/Sticky CTA (≈ `04:212`) | «золотая» | `bg-primary` #B08D57 + `text-primary-foreground` ink 5.7:1 ✅ AA (заливка, НЕ текст) | 5.7:1 |
| HeroExtras вторичник (≈ `04:479`) | «отдельная золотая кнопка-вторичник» | `border-gold-text` #8A6D3B (outline, ≥3:1) ИЛИ `bg-primary`+ink 5.7:1 | ≥3 / 5.7 |
| EventTypeSelector CTA-кнопка (≈ `04:638`) | «золотая/вторичная» | `bg-primary`#B08D57+ink / outline `border-gold-text` | 5.7 / ≥3 |
| MenuCatalog рекомд. тариф (≈ `04:1048`) | «золотую рамку» | `border-gold-text` #8A6D3B (4.129:1 на ivory ≥3:1; НЕ `--color-gold` #B08D57 FAIL) | ≥3:1 |
| ReviewCard «Ответ NiloV» (≈ `04:1137`) | «золотой акцент» | `border-l-2 border-gold-text` #8A6D3B (4.129:1; НЕ `--color-gold` FAIL) | ≥3:1 |
| GallerySection focus (≈ `04:1533`) | «focus-visible золотая обводка» | `--color-ring` #8A6D3B (≥3:1; НЕ `--color-gold` FAIL) | ≥3:1 |
| PackageGrid recommended (≈ `04:1656` + `1680`) | «Золотая рамка» / «золотой рамкой» | `border-gold-text` #8A6D3B (синхрон `04:1657`) | ≥3:1 |

Текстовые «золотые» применения уже были на `var(--color-gold-text)` (`04:170` телефон, `04:205` ScrollProgress, `04:614/683` EventTypeSelector) — подтверждены, без изменений.

## 2. Что исправлено в `06_TOKENS.md`

- **§5 PriceTag (`06:181`)** — `border-b border-gold` → **`border-b border-gold-text`** #8A6D3B (4.129:1 на ivory ≥3:1; явно НЕ `border-gold` #B08D57 = 2.893:1 FAIL). Закрывает F3.
- **§1 gold-text — добавлено ограничение (ХРУПКИЙ AA-запас):**
  - на ivory #8A6D3B = **4.537:1**, запас **0.037** — НЕ использовать gold-text на тексте **<16px без font-weight ≥ 600**;
  - на secondary #8A6D3B = **4.129:1** — **ПРЯМОЙ FAIL AA** → gold-text на secondary **ЗАПРЕЩЁН**, только `--color-gold-text-on-secondary` #6E5631 (5.885/6.467);
  - не-текст → `--color-ring` #8A6D3B (≥3:1) или заливка `--color-gold` #B08D57 (НЕ как текст).

## 3. Итог

- Устранён specificity-debt: все 8 голых gold-применений в 04 + 1 в 06 привязаны к токену.
- Риск латентного 1.4.11-FAIL (`#B08D57` вместо `#8A6D3B`) при сборке снят явной пометкой токена в каждом месте.
- Документирована хрупкость gold-text (запас 0.037) и прямой FAIL на secondary — исполнитель получает явные границы.
- Код не редактировался.

**Остаток (вне объёма этой итерации, см. F1/F2/F12 в RECHECK):** системная хрупкость ring/gold-text (запас 0.037) — кандидат на введение единого тёмно-золотого токена `#6E5631` для всех контекстов.
