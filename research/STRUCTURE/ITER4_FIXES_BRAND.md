# ITER4_FIXES_BRAND.md — Исправления оси BRAND (Итерация 5)

**Дата:** 2026-07-20 · Режим: правка ТОЛЬКО спец-файлов `40_BRAND_VOICE.md` и `04_BLOCKS.md`. Код не трогал.

Важно: файлы `04_BLOCKS.md`/`40_BRAND_VOICE.md` были переписаны ПОСЛЕ `ITER4_RECHECK_BRAND.md`, поэтому указанные там строки (632/533/743/276/13/61-65/86) смещены. Правки применялись по РЕАЛЬНОМУ текущему содержимому (найдено через grep), а не по номерам из речека.

## Что исправлено

1. **Бан-лист + регекс (`40`)** — добавил в §3.1 строку `роскошн*` (`роскошный`/`роскошь`) и включил `роскошн` (и `vip`/`premium`) в grep-gate (§86). Закрывает R-19: слово, запрещённое §3.4, теперь ловится гейтом.
2. **«роскошный банкет» (`04:656`)** — заменено на «банкет верхнего тарифа» (синоним без запрещённого слова). Закрывает R-20.
3. **«доступная премиальность» (`40:13`)** — перефразировано в «доступное качество мирового класса» (слово «премиальность» убрано, противоречие с «НЕ называем себя премиум» снято). Закрывает R-22.
4. **Капшены Эрмитаж (`04:557`, `04:767`)** — в саму строку капшена вшит 🟡-disclaimer: «Свадьба · Эрмитаж 🟡 · 120 гостей (пример ДЕМО-данных, не клиент)». Теперь правило `40:73` («рядом 🟡») соблюдено внутри строки, а не только в сноске. Закрывает R-23.
5. **aria-пример «Премиум» (`04:300`)** — заменён на «Максимальный» (совпадает с публичным лейблом верхнего тарифа `40:184`/`04:1643`). Закрывает R-24.
6. **grep-gate путь (`40:86`)** — вместо несуществующего `content/` указан реальный путь исходников `app/ components/ lib/ --include="*.ts" --include="*.tsx" --include="*.css"`. Команда исполнима прямо из корня репо (подтверждено `ls -d app components lib`).

## Найденные проблемы

- **Резидуал в КОДЕ (вне scope этой задачи):** реальные `app/components/lib` содержат публичные нарушения бан-листа — `lib/data.ts` alt-тексты «премиум-кейтеринг» (строки 185/249/353/402/488/655/943/1029/1614), публичные имена пакетов «Премиум»/`Люкс`/`VIP` и «Роскошный банкет» (`lib/data.ts:1237/1264/1349`), `app/pricing/layout.tsx:5` meta «…от эконом до премиум». Спека-гейт теперь смотрит в эти пути, но сам код не исправлен (задача запрещает правку кода) — это отдельный дефект для оси SERVICE/CODE-REALITY.
- `content/` реально отсутствует в репо (подтверждено), поэтому старый gate был физически неисполним (R-21a).

## Статус приёмки (по `40` §8)

- [x] Бан-лист покрывает «роскошный» (п.1).
- [x] Регекс исполним, целится в реальные `app/components/lib` (п.6).
- [x] Tone без «премиальности» (п.3); aria = «Максимальный» (п.5).
- [x] Эрмитаж-капшены несут 🟡 в самой строке (п.4).
- [~] Фактический копирайт в коде (`lib/data.ts` и др.) всё ещё содержит запрещённый лексикон — требует отдельного прохода вне этой итерации.

*Конец отчёта `ITER4_FIXES_BRAND.md`.*
