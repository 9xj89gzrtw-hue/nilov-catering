# ITER4_FIXES_TRUST — правки ось TRUST (Итерация 5, исправляющий редактор)

**Дата:** 2026-07-20 · **Скоуп:** ТОЛЬКО структура — `04_BLOCKS.md` + `02_IA.md` (без кода). База: `ITER4_RECHECK_TRUST.md`.

## Что изменено

### 1. Исполнимость fact-gate → блок «РЕАЛИЗАЦИЯ»
`04_BLOCKS.md` (после строки 105, перед моделью `Dish`): добавлен подблок
`### 🔧 РЕАЛИЗАЦИЯ FACT-GATE`. Описан механизм исполнения гейта как работающего:
- **Точки внедрения `renderFactItem`** при сборке страниц из блоков: `ReviewCard`,
  `TrustBar`, `TrustProof` (блок 57, п. 7–10), `WhyUs` (блок 8), `HeroSection` (блок 1, Sub).
  Указано, что guard из `04:96-103` внедряется в каждый компонент, рендерящий сущность с факт-полем.
- **Контракт** (default `pending` fail-closed → `verified` рендер; `pending`+видимый `disclaimer` →
  рендер с пометкой; `pending` БЕЗ `disclaimer` → `null`, не попадает в DOM; `disclaimer`/`status` обязательны).
- **CI-шаг `scripts/fact-gate`**: файл `scripts/fact-gate.mjs` (grep/lint) вешается на `pre-commit`
  (husky) и шаг в `.github/workflows/`; проверяет (1) отсутствие числового/годового claim без
  `verified`/видимого `disclaimer`, (2) grep-гейт на безусловные бренды-клиенты, (3) обязательность
  `disclaimer` у любого `pending`-claim в рендере; при провале — блокировка деплоя.
- **Приёмка (DoD)**: реализация `renderFactItem` мин. в 5 компонентах + рабочий CI-шаг.
Код не писался — механизм описан в спеке так, чтобы быть исполнимым (закрывает ITER4_RECHECK баг #1).

### 2. «18 лет» → единая честная формулировка «более 18 лет / с 2007 года»
Унифицировано во ВСЕХ упоминаниях обоих файлов; конфликт «19 лет» снят.
- `04_BLOCKS.md:115,321,429,500,609,833,837,838,842,853,938,1250,1255,2346` — «18 лет» → «более 18 лет».
- `02_IA.md:64` («🟡 18 лет [с 2007…]») → «🟡 более 18 лет [с 2007…]».
- `02_IA.md:273` («🟡 19 лет») → «🟡 более 18 лет (с 2007 года)» — **удалён конфликт «19 лет»** (был единственный остаток рассогласования с `04`).
- Вспомогательные пояснения (FACTCHECK-статус `04:500`, TODO `04:2346`) приведены к «более 18 лет (с 2007)» с сохранением честной оговорки об арифметике 2007→19.

### 3. Голые факты → унификация «3500+» и проверка 🟡
- `04_BLOCKS.md:1251` (TrustProof п.8): «3000+ проведённых событий … (на сайте клиента — фактически 3 500+)»
  → **«3 500+ проведённых событий»** 🟡 с единым disclaimer «НЕ публиковать как подтверждённый факт до
  сверки — статус `pending` по FACT-GATE». Убран вложенный голый «3 500+» внутри дисклеймера.
- `02_IA.md:64` («🟡 3 500+ мероприятий [подтверждено живым сайтом, независимый критик C6…]»)
  → **убрана фабрикованная ссылка-подтверждение** «независимый критик C6 2026-07-20» (прямо противоречила
  FACTCHECK_REPORT), заменена на честную «НЕ публиковать как подтверждённый факт до сверки — статус `pending`».
  Теперь `02_IA:64` и `04:1251` несут **одинаковое число «3 500+»** и одинаковый статус-маркер.
- «Более 3000» (`04:429` Hero) и «кофе от 950» (`04:413`/`04:543` HeroExtras) — **подтверждено**: уже несут
  🟡 + видимый disclaimer («(на проверке)»; «точная цена подтверждается при расчёте; на сайте указано 390 ₽»).
  Дополнительно в РЕАЛИЗАЦИИ (п.1) закреплено, что эти claim'ы оборачиваются в `renderFactItem` с `pending`
  + disclaimer — то есть механизм теперь требует видимую пометку, а не только 🟡.
- «Участник и финалист отраслевых премий» (`04:1252`) — **подтверждено**: уже 🟡 + disclaimer «мелким шрифтом
  снизу, НЕ якорь доверия» + удалённые факт-призраки. Оставлено как есть (честно помечено), без вранья.

### 4. Межфайловая сверка
`02_IA:64` (3 500+) = `04:1251` (3 500+) ✅ — число и статус совпадают. Конфликт «19 лет» (`02_IA:273`)
с «18 лет» (`04`) устранён — везде «более 18 лет / с 2007 года».

## Что НЕ правилось (вне скоупа / не требовало)
- Код (`app/`, `components/`, `scripts/`, `.github/`) — по заданию не трогался.
- Прочие критик-отчёты (`CRITIC_*`, `21_LOGIC_AUDIT`, `C6_TRUST_FACTCHECK_CRITIC`) — вне скоупа задачи.
- Цены кофе 950/390 и анонимные плейсхолдеры клиентов — их 🟡-статус и disclaimer уже корректны, только
  закреплены в РЕАЛИЗАЦИИ как подлежащие `renderFactItem`.

## Остатки / что остаётся открытым
- **Гейт реально не исполнен**: `renderFactItem` и `scripts/fact-gate.mjs` по-прежнему НЕ существуют в коде
  (подтверждено в ITER4_RECHECK_TRUST поиском по `app/`/`components/`/`.github/`). Блок РЕАЛИЗАЦИЯ описывает
  ТРЕБОВАНИЕ, но закрытие оси = отдельная задача инженера (написать код + CI-шаг).
- **«3 500+» остаётся неподтверждённым** самим по себе (FACTCHECK ❓ no-data) — помечен 🟡 `pending`, что честно,
  но финальную цифру всё ещё надо сверить с заказчиком до прод-релиза (TODO `04:2346` п.2).
- **Арифметика 2007→19** документирована как известная; выбрана формулировка «более 18 лет (с 2007)»,
  повторяющая лейбл живого сайта — компромисс «честно, без точного N». Точное число лет всё ещё ждёт
  подтверждения заказчика.
- Другие файлы структуры (`13_BENCHMARK_SYNTHESIS:23`, `16_BLOCK_BENCHMARKS:103/148`, `19_*`) по-прежнему
  держат устаревшие «19 лет»/«Роспотребнадзор как клиент» — вне скоупа этой итерации, но мешают глобальной
  сведённости (отмечено в ITER4_RECHECK_TRUST).

## file:line сводка правок
| Файл | Строки | Суть |
|---|---|---|
| `04_BLOCKS.md` | 105–129 (новый блок) | РЕАЛИЗАЦИЯ FACT-GATE (внедрение + контракт + CI) |
| `04_BLOCKS.md` | 115,321,429,500,609,833,837,838,842,853,938,1250,1255,2346 | «18 лет» → «более 18 лет» |
| `04_BLOCKS.md` | 1251 | «3000+ … (фактически 3 500+)» → «3 500+» 🟡 единый disclaimer |
| `02_IA.md` | 64 | «18 лет» → «более 18 лет»; убрана фабрикация «критик C6» у 3 500+ |
| `02_IA.md` | 273 | «19 лет» → «более 18 лет (с 2007 года)» (снят конфликт) |
