# ИТЕРАЦИЯ 4 — ФИНАЛЬНЫЙ РЕЧЕК оси TRUST/FACTS (recheck)

**Дата:** 2026-07-20 · **Режим:** финальная перепроверка, strict/devil's-advocate · **Скоуп:** ТОЛЬКО структура — `04_BLOCKS.md` (+ `02_IA.md` по касательной), `ITER3_FIXES_W3.md`, `ITER3_RECHECK_TRUST.md`.
**Метод:** перечитаны все 3 заданных файла; РЕАЛЬНЫЕ строки `04_BLOCKS.md` сверены поиском по файлу (номера строк актуальны для текущей версии Итерации 4, НЕ копируются из устаревших отчётов). Код `app/`/`components/`/`.github/` проверен на наличие реального гейта.

---

## 0. Три вопроса задания — короткие ответы

1. **РЕАЛЬНО ли fact-gate исполним (без самопротиворечий)?** — Механизм **задан как контракт и теперь ВНУТРЕННЕ НЕПРОТИВОРЕЧИВ** (ITER3_FIXES_W3 §6 переписал битый CI-assert: `04:104` больше НЕ требует «0 вхождений брендов», а грейпает на *безусловные* формулировки «обслуживали Эрмитаж» + разрешает disclaimer-контекст). НО он **НЕ ЗАКРЕПЛЁН ИСПОЛНЕНИЕМ**: `renderFactItem` не реализован ни в одном компоненте; `scripts/fact-gate.*` и CI-шаг отсутствуют (проверено: `app/`, `components/`, `.github/workflows/`). Гейт «спроектирован, внутренне согласован, но не запущен».
2. **Нет ли голых фактов в прод-копирайте без disclaimer/verified?** — **ЕСТЬ голые/слабые факты, которые рендерятся пользователю**: Hero «Более 3000 событий» (405), «Кофе-брейк от 950 ₽» ведущим якорем (413/519 — реальная цена 390), вложенный «3 500+» ВНУТРИ дисклеймера (1227), «Участник премий» в proof-ряду (1228). Они помечены 🟡/disclaimer, т.е. проходят через УСЛОВИЕ-2 гейта — но само условие 2 превращает гейт в «любой числовой claim рендерится, стоит добавить тег». Голых в строгом смысле гейта мало, но слабых фактов много, и ведущий якорь (950) объективно вводит.
3. **Консистентна ли «18 лет»?** — ВНУТРИ `04` едино «18 лет (с 2007)» (403/1226/2322). НО: (а) **арифметически ложно** (2026−2007=19), спека ЗНАЕТ и сознательно копирует ошибку клиента (2322); (б) **МЕЖФАЙЛОВО НЕ СВЕДЕНО**: `02_IA.md:273` до сих пор «19 лет · 3000+ · Эрмитаж/Мариинский/Armani», `02_IA.md:64` — «3 500+ [подтверждено живым сайтом, независимый критик C6]» (фабрикованная ссылка, противоречит FACTCHECK_REPORT). Ось глобально НЕ закрыта.

> **Критическое методологическое замечание:** `ITER3_RECHECK_TRUST.md` (пред. итерация) цитирует баги #2/#10 («CI-assert `04:102` сам себе противоречит») и опирается на строки `04:102`/`04:564`/`04:1228`/`04:531`, которые в ТЕКУЩЕМ файле **УЖЕ НЕ СООТВЕТСТВУЮТ реальности**: битый CI-assert был переписан в ITER3_FIXES_W3 §6 и теперь живёт в `04:104`. ITER3_RECHECK_TRUST описывает СТАРУЮ версию гейта → его вывод «гейт невыполним из-за самопротиворечия» **УСТАРЕЛ**. Ниже — перепроверка на актуальном файле.

---

## 1. Fact-gate как МЕХАНИЗМ (вопрос 1)

Текущий `04:88-104` содержит:
- default `status:'pending'` (fail-closed) — `04:91`;
- псевдокод `renderFactItem` с ветками verified→рендер, pending+disclaimer→рендер с дисклеймером, иначе `null` — `04:96-103`;
- CI-assert-описание (бренды грейпуются на *безусловные* формулировки, разрешён disclaimer-контекст) — `04:104`.

**Внутренняя непротиворечивость ВОССТАНОВЛЕНА** (баг #2/#10 из ITER3_RECHECK_TRUST ЗАКРЫТЫ переписыванием).

**НО гейт НЕ является работающим механизмом в смысле исполнения (подтверждено поиском по коду/CI):**
- `renderFactItem` не встречается ни в одном `.ts/.tsx` файле проекта (`app/`, `components/` пусты по запросу);
- `scripts/fact-gate.*` отсутствует (есть только `nilov-catering/scripts`, не относящийся к гейту);
- ни в одном `.github/workflows/*.yml` нет шага, грейпающего `Эрмитаж|premium|Armani` или проверяющего `status:'verified'`. Три существующих CI-файла: `vercel-deploy.yml` (деплой), `block-force-push.yml` (защита ветки), `external-critics.yml` (визуальный критик по скриншотам, НЕ факт-чек).

**Вывод по вопр. 1:** контракт **задан и внутренне согласован**, но **не закреплён исполнением** (нет кода, нет теста/линта/хука). Для «закрытого гейта» этого недостаточно — описание CI-assert остаётся ПРОЗОЙ-инструкцией, а не запускаемой проверкой.

---

## 2. Голые/слабые факты в прод-копирайте (вопрос 2)

- `04:405` Hero Sub: «Более 3000 событий (на проверке) за 18 лет (с 2007) 🟡». Крупный видимый числовой claim первого экрана. Гейт пропускает по условию 2 (есть «(на проверке)»+🟡), но это ровно паттерн «подписанный голый факт». Слабость, НЕ баг гейта.
- `04:1227` TrustProof п.8: внутри дисклеймера — «(на сайте клиента — фактически 3 500+)». Второе неподтверждённое число (FACTCHECK ❓ no-data) протащено **ВНУТРЬ дисклеймера**. Дисклеймер, несущий свежий голый факт — не дисклеймер.
- `04:413`/`04:519` «Кофе-брейк от 950 ₽/гость» — **ВЕДУЩИЙ якорь** строки. Реальная цена живого сайта 390 (FACTCHECK ❌ refuted, `04:427`/`04:113`/`04:671`). 950 помечен 🟡, но стоит первым и крупным; 390 спрятан в скобку. Гейт-комплаент, но вводящий: первый якорь = вымышленная цена.
- `04:1228` «Участник и финалист отраслевых премий» 🟡 — 0 подтверждений (FACTCHECK: `Гастрономический Оскар 2024`/`Event Awards Russia 2023` удалены как факт-призраки). Смягчено («мелким шрифтом снизу») — приемлемо, но claim всё ещё в proof-ряду.

---

## 3. Консистентность «18 лет» (вопрос 3)

- ВНУТРИ `04`: едино «18 лет (с 2007)» — `04:403`, `04:1226`, `04:2322`. ✓ формально.
- **АРИФМЕТИКА ЛОЖНА:** 2026−2007 = 19. Спека ЗНАЕТ (`04:2322`: «арифметически для 2026 года — 19, но прод клиента показывает 18») и сознательно фиксирует неверное число «во избежание противоречия». Перенос чужой ошибки как факта.
- **МЕЖФАЙЛОВО НЕ СВЕДЕНО** (правки scope=только `04`):
  - `02_IA.md:273` — «🟡 **19 лет** · 🟡 **3000+** событий · 🟡 Эрмитаж · 🟡 Мариинский · 🟡 Armani» → всё ещё «19 лет» и открытый перечень брендов-клиентов (без disclaimer-оговорки об анонимности).
  - `02_IA.md:64` — «🟡 3 500+ мероприятий [подтверждено живым сайтом, независимый критик C6 2026-07-20; финальную цифру уточнить у заказчика]» → фабрикованная цитата-подтверждение. FACTCHECK_REPORT (стр. 19, 51) прямо: «3000/3500 = ❓ no-data», «ссылка на отчёт FactChecker невоспроизводима». То есть `02_IA:64` утверждает «подтверждено», опираясь на невоспроизводимый отчёт.
  - Три числа по файлам: `04` = «3000»+«18 лет»; `02_IA:273` = «3000+»+«19 лет»+бренды; `02_IA:64` = «3 500+ подтверждено». Несведены.

---

## 4. BUG-LIST (file:line + альтернатива) — ≥8

**🔴 CRITICAL**

1. **`04:96-103` / `04:104` — fact-gate спроектирован и внутренне согласован, но НЕ закреплён исполнением.** `renderFactItem` НЕ реализован ни в одном компоненте (поиск по `app/`/`components/` — 0 совпадений); `scripts/fact-gate.*` отсутствует; ни один CI-шаг (`vercel-deploy`/`block-force-push`/`external-critics`) не грейпает факты. CI-assert — проза-инструкция, не запускаемая проверка.
   → *Альтернатива:* либо написать реальный `scripts/fact-gate.mjs` (grep-хук: 0 безусловных «обслуживали X» в публичных строках + 0 числовых claim без `status:'verified'`/`disclaimer`) и повесить его на `pre-commit` + шаг в `.github/workflows/`; либо в `04` явно записать «гейт считается закрытым ТОЛЬКО когда добавлен соответствующий тест в репо».

2. **`04:405` — «Более 3000 событий» крупным видимым claim в Hero.** Гейт пропускает по условию 2 («(на проверке)»+🟡), но условие 2 — дыра: любой числовой claim рендерится, стоит добавить тег. Реальное число не подтверждено (FACTCHECK ❓ no-data).
   → *Альтернатива:* для класса «количественный claim» (события, годы, клиенты, награды, цены) рендерить ТОЛЬКО при `status='verified'`; дисклеймер-путь оставить для нейтральных «пример из архива», не для чисел. Либо в Hero заменить на «сотни событий под ключ с 2007 года» без конкретного числа до верификации.

3. **`04:413` / `04:519` — «Кофе-брейк от 950 ₽» ВЕДУЩИЙ якорь; реальная цена 390 (FACTCHECK ❌ refuted, `04:427`/`04:113`/`04:671`).** 950 помечен 🟡, но стоит первым и крупным; 390 спрятан в скобку. Гейт-комплаент, но первый якорь = вымышленная цена → вводит.
   → *Альтернатива:* до сверки НЕ делать 950 ведущим числом — либо вести строку от подтверждённого «от 390 ₽», либо показывать «цена по расчёту» без конкретной цифры. `pending` должен блокировать ВЫВОД самого числа как якоря, а не только сопровождать его.

4. **`04:1227` — вложенный неподтверждённый факт «фактически 3 500+» ВНУТРИ дисклеймера TrustProof п.8.** FACTCHECK помечает 3500+ как ❓ no-data. Дисклеймер сам протаскивает свежее непроверенное число.
   → *Альтернатива:* удалить «(на сайте клиента — фактически 3 500+)»; оставить только «уточнить у заказчика». Число 3500+ не должно появляться ни в claim, ни в дисклеймере до верификации.

5. **`02_IA.md:64` — фабрикованная ссылка-подтверждение «3 500+ [подтверждено живым сайтом, независимый критик C6 2026-07-20]».** Точная копия бага, который был честно исправлен в `04:476` («БЕЗ ПОДТВЕРЖДЕНИЯ … ❓ no-data»), но `02_IA` остался нетронутым (scope правок = только `04`). FACTCHECK прямо опровергает «подтверждено».
   → *Альтернатива:* привести `02_IA:64` к виду `04:476`: «🟡 3000+/3500+ — НЕ подтверждено независимо (FACTCHECK ❓ no-data), статус pending, НЕ публиковать как факт». Убрать «подтверждено живым сайтом / критик C6».

6. **`02_IA.md:273` — «19 лет» + «3000+» + открытый перечень «Эрмитаж/Мариинский/Armani» не сведены с `04`.** Правки тронули только `04`, поэтому межфайловое противоречие 18/19, 3000/3500 и статус брендов сохраняется. При этом в `04` бренды уже анонимизированы (1229), а в `02_IA:273` они всё ещё названы как proof-лента.
   → *Альтернатива:* зафиксировать ОДНО число («18 лет (с 2007)») и ОДИН claim («3000+ 🟡») во ВСЕХ файлах структуры; в `02_IA:273` убрать прямой перечень брендов (заменить на «статусные площадки 🟡, анонимно»), синхрон с `04:1229`. `04:2322` должен явно указать, что правка `02_IA` — часть закрытия оси.

**🟠 MEDIUM**

7. **`04:2322` — «18 лет (с 2007)» арифметически ложно (2026−2007=19), спека знает и копирует ошибку клиента.** Документированная ложность ради «консистентности с живым сайтом».
   → *Альтернатива:* либо запросить у заказчика актуальное число и поставить верное (19 лет с 2007), либо честно писать «с 2007 года» БЕЗ числа «N лет» до подтверждения; не публиковать заведомо неверное «18 лет» как факт.

8. **`04:1228` — «Участник и финалист отраслевых премий» 🟡 в proof-ряду при 0 подтверждений (факт-призраки «Гастрономический Оскар 2024»/`Event Awards Russia 2023» удалены).** Смягчено («мелким шрифтом снизу»), но claim остаётся в блоке доверия.
   → *Альтернатива:* до получения реального списка наград с годами — убрать пункт из proof-ряда целиком, а не держать неподтверждённый claim в блоке доверия.

9. **`04:1229` (и грейп-контекст `04:104`) — имена брендов Эрмитаж/Armani/Газпром/Forbes всё ещё НАЗВАНЫ в публичном копирайте `04`** (566 TrustBar, 794/1229 TrustProof, 1410 `/reviews`), пусть и в disclaimer-оговорке. Гейт это допускает (условие: рядом 🟡/фраза «пример-площадка»), НО назвать 4 конкретных бренда в proof-блоке = реальный репутационный/юрриск, если кто-то из них возразит («мы не клиенты»).
   → *Альтернатива:* в TrustProof/TrustBar НЕ перечислять имена брендов даже в disclaimer — писать «статусные площадки и бренды 🟡 (примеры категорий, не подтверждённые клиенты)». Называть конкретный бренд ТОЛЬКО после письменного договора (как и требует бан-лист 40 §3.2).

10. **`04:476` vs `04:405`/`04:1226` — статус FACTCHECK помечен «🟡 БЕЗ ПОДТВЕРЖДЕНИЯ» (честно внутри `04`), НО сам claim «18 лет / 3000+ / отраслевые премии» ВСЁ РАВНО рендерится пользователю как видимый факт с 🟡.** Гейт формально соблюдён (есть дисклеймер), но на практике отличается от «pending БЕЗ дисклеймера → null» (04:101) только мелкой пометкой — пользователь видит бренд-якорь «18 лет / 3000+» как есть.
    → *Альтернатива:* для класса «громкий репутационный claim» (годы опыта, число событий) трактовать 🟡 НЕ как «рендер рядом с пометкой», а как «рендер ТОЛЬКО в честном блоке „что мы знаем/не знаем“», без выноса числа в Hero/TrustProof-якоря до `verified`.

---

## 5. Что ЗАКРЫТО (баланс — подтверждено перечитыванием актуального файла)

- ✅ **FACT-GATE стал контрактом И теперь ВНУТРЕННЕ СОГЛАСОВАН** (`04:88-104`): default `pending`, псевдокод `renderFactItem`, CI-assert переписан под реальные строки (баг #2/#10 из ITER3_RECHECK_TRUST ЗАКРЫТЫ переписыванием в ITER3_FIXES_W3 §6).
- ✅ Модели `Dish`/`Review`/`Event`/`Testimonial`/`AwardsStrip`/`TrustProofItem`/`ClientLogo` несут `status:'verified'|'pending'` — `04:107,111,119-122` (полнота модели гейта).
- ✅ `04:476` СТАТУС FACTCHECK внутри `04` честен («БЕЗ ПОДТВЕРЖДЕНИЯ … ❓ no-data»).
- ✅ Бренды-клиенты в `04` анонимизированы плейсхолдерами (`Корпоратив «А»` … `04:1229`); Роспотребнадзор исключён из ряда клиентов (`04:1229`).
- ✅ Реальная цена 390 задокументирована рядом с 950 (`04:427`,`04:113`,`04:671`) — вводящий якорь помечен, но известен.

---

## 6. Скоринг оси TRUST/FACTS (0–10, строго; потолок ≤9)

| Критерий | Балл | Обоснование |
|---|---|---|
| Наличие работающего механизма гейта | 4 | Контракт задан и внутренне согласован (94-104), НО не реализован в коде, нет теста/линта/CI (баг 1). «Working» — НЕТ. Улучшение vs ITER3 (там гейт был и самопротиворечив), но исполнение 0. |
| Отсутствие голых фактов в prod-копирайте | 4 | Hero «3000» (405), вложенный «3 500+» (1227), кофе 950 ведущим (413/519), премии (1228) рендерятся как видимые якоря. Гейт-комплаент по условию 2, но условие 2 — дыра (баги 2,3,4,8). |
| Консистентность фактов (18 лет, цены, клиенты) | 4 | Внутри `04` «18 лет» ок формально; арифметика ложна (баг 7); межфайлово `02_IA:273` «19 лет» + `02_IA:64` «3500+ подтверждено» (фабрикация) не сведены (баги 5,6). |
| Честность цитирования источника | 5 | Внутри `04` честно (476); НО `02_IA:64` повторяет исправленную фабрикацию «подтверждено критиком C6» (баг 5) — цепочка верификации broken в другом файле. |
| Полнота модели гейта (все сущности) | 7 | Все заявленные сущности несут `status` (04:107-122); остаётся gap «гейт задан, не исполнен» (баг 1). |

**ИТОГОВЫЙ БАЛЛ: 5 / 10** (усреднённо-строго; потолок ≤9 соблюдён).

**Вердикт: ⚠️ ось TRUST/FACTS НЕ закрыта, продвинута vs Итерация 3, но с существенным откатом в оценке из-за проверки ИСПОЛНЕНИЯ.** Главный блокер сместился: раньше (ITER3) гейт был прозой/самопротиворечив — теперь он **спроектирован и внутренне согласован, но не закреплён кодом/CI** (баг 1). Второй блокер — **межфайловая несведённость** `02_IA.md:64`/`02_IA.md:273` («19 лет», «3500+ подтверждено») прямо противоречит FACTCHECK и честному `04:476`. Третий — арифметически ложное «18 лет» копируется сознательно (баг 7). Четвёртый — ведущий якорь «950» (реально 390) и вложенный «3 500+» внутри дисклеймера всё ещё рендерятся (баги 3,4).

> **Почему 5, а не 6 (ITER3):** ITER3_RECHECK_TRUST оценивал гейт как «спроектирован, но битый» (6/10). При перепроверке Итерации 4 выяснилось, что гейт **бОльше НЕ битый** (исправлено ITER3_FIXES_W3), НО при этом выяснилось, что он **вообще не исполняется** (ни кода, ни CI), и что слабые факты (950/3500/Hero-3000) продолжают рендериться как якоря, а межфайловая фабрикация в `02_IA` сохраняется. Честная проверка ИСПОЛНЕНИЯ, а не только намерения в спеке, снижает балл: «задумано хорошо, работает — нет».

---

## 7. Приоритет исправлений (для след. итерации / закрытия оси)

1. **Закрепить гейт исполнением** — написать `scripts/fact-gate.mjs` + повесить на pre-commit и CI-шаг; `renderFactItem` реализовать в компонентах либо явно пометить гейт «не закрыт без теста» (баг 1).
2. **Свести оси во ВСЕХ файлах** — `02_IA.md:64` (убрать «подтверждено критиком C6»), `02_IA.md:273` («19 лет»→«18 лет (с 2007)», убрать прямой перечень брендов) (баги 5,6).
3. **Убрать вложенный «3 500+»** из дисклеймера TrustProof п.8 (баг 4).
4. **Не делать 950 ведущим якорем** до сверки; честно с «390» или «цена по расчёту» (баг 3).
5. **Решить арифметику «18/19 лет»** — запросить у заказчика актуальное число, не публиковать заведомо ложное (баг 7).
6. **Не называть 4 конкретных бренда в proof-блоках даже в disclaimer** (баг 9).

---

*Метод: номера строк в этом отчёте сверены с РЕАЛЬНЫМ текущим `04_BLOCKS.md` (Итерация 4) поиском по файлу. `ITER3_RECHECK_TRUST.md` опирался на СТАРУЮ версию CI-assert (`04:102`) и его баги #2/#10 УСТАРЕЛИ после ITER3_FIXES_W3 §6. Любые будущие правки должны обновлять номера строк в отчётах, иначе критика устаревает мгновенно. Связано: `04_BLOCKS.md`, `02_IA.md`, `ITER3_FIXES_W3.md`, `ITER3_RECHECK_TRUST.md`.*
