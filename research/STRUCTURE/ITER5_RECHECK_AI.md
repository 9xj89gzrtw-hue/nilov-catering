# ITER5_RECHECK_AI — Финальный речек оси AI-TRANSPARENCY (Итерация 5)

**Дата:** 2026-07-20 · **Режим:** независимый речек (НЕ соглашаюсь автоматически) · ТОЛЬКО структура `research/STRUCTURE/` + `public/llms.txt` + живой код.
**Объекты:** `ITER4_FIXES_AI.md` (отчёт-фиксер) + `08_CONSTRUCTOR_SPEC.md` + `07_CALCULATOR_SPEC.md` + `04_BLOCKS.md` + `public/llms.txt` + `components/ai/AIConcierge.tsx` (живой код, вне скоупа правки, но в зоне проверки оси).

**Скоринг оси AI-TRANSPARENCY (Итерация 5): `6 / 10`** — строго ≤9, как требовалось. Не «закрыто 8/10» (см. обоснование в конце).

---

## ЖИВЫЕ проверки по диску (grep + построчное чтение)

| № | Требование задания | Факт на диске | Статус |
|---|---|---|---|
| 1 | «резервный» убран из `08` | `grep резервный` по `08_CONSTRUCTOR_SPEC.md` → **0 совпадений**. Подтверждено. | ✅ |
| 1б | …консистентно с `04` (оговорка «без ИИ») | `04` НЕ содержит слова «резервный» вообще (`grep резервный` → 0). Консьерж-FAB-обёртка реально живёт в `04:250` (и `04:1008`), НЕ в `04:226`. | ⚠️ контент ок, ссылка в отчёте врёт |
| 2 | «AI-слой» в `07` помечен внутренним | `07:260`: `*Внутренняя спец-пометка: НЕ добавлять слой «Консьерж»; AI-слой (внутренняя спецификация, не публично) — держать ТОЛЬКО во внутренних спецификациях (в резерве, не подключен), в публичный копирайт не выносить. Итерация 5: «AI-слой» явно помечен как внутренний, НЕ публичный.*` — публичный копирайт `07:260` чист («без упоминания ИИ»). | ✅ (но строка в отчёте названа неверно) |
| 3 | `llms.txt` нейтрален (без «для AI»-брендинга) | `public/llms.txt` (12 строк): **нет** «AI»/«ИИ»/«для AI». Чистый маркдаун-буктрейл. | ✅ |

**Вывод по 1–3:** текстовые цели на диске ДОСТИГНУТЫ. НО сопровождающий отчёт `ITER4_FIXES_AI.md` описывает правки с ложными номерами строк и сфабрицированным diff → целостность аудит-трейла нарушена (Баги 1–4). А главное — живой код (`AIConcierge.tsx`) всё ещё публично брендирован «AI-консьерж», что прямо противоречит запрету оси (Баг 5).

---

## БАГИ (file:line + альтернатива) — 10 шт.

### Баг 1 — `ITER4_FIXES_AI.md:14` — ложная ссылка «консистентно с `04:226`»
**Проблема:** утверждается, что `08:136` «идентичен формулировке `04:226` («ТОЛЬКО как optional-обёртка», без «резервный»)». Но живой `04:226` — это строка z-index иерархии: `AnnouncementBar z-[110] (над всеми, если видна) ·`. К Консьерж-обёртке она отношения не имеет.
**Альтернатива:** ссылаться на реальную строку — `04:250` (`FAB Консьерж оставляется в z-[90] ТОЛЬКО как optional-обёртка над /plan/helper … НЕ «молчит в резерве»`) или `04:1008` (внутренняя спека «AI-слой — в резерве»).

### Баг 2 — `ITER4_FIXES_AI.md:10-12` — сфабрицированный before/after для `08:136`
**Проблема:** «Было» цитирует `…оставлен резервным ТОЛЬКО как optional-обёртка…`. Живой `08:136` сейчас чист (без «резервный»), но утверждение консистентности с `04:226` ложно (см. Баг 1). Контент `08:136` совпадает с `04:250`, а не с `04:226`. Сам diff мог быть реален, но обоснование консистентности указывает на несуществующую строку → трейл разъезжается.
**Альтернатива:** переписать обоснование: «`08:136` идентичен `04:250`»; удалить упоминание `04:226`.

### Баг 3 — `ITER4_FIXES_AI.md:16-18` — неверная строка `07:246` вместо `07:260`
**Проблема:** «`07_CALCULATOR_SPEC.md:246` — «AI-слой» помечен как внутренняя спец-пометка». Живой `07:246` — это заголовок `## Волна 4 — синхронизация с 04 (Елена/Света)`. Реальная пометка «AI-слой (внутренняя спецификация…)» — в `07:260`.
**Альтернатива:** исправить номер на `07:260`.

### Баг 4 — `ITER4_FIXES_AI.md:22-26` — сфабрицированный diff `04:1371,1373` (правка НЕ применялась)
**Проблема:** заявлено «Было `04:1371` статичный файл для AI-поиска (ChatGPT/Perplexity/Claude)» → «Стало … для поисковых систем и ИИ-ассистентов». Но:
- Живой `04:1371` — строка таблицы `| /404 | Organization (noindex, HTTP 404) |`. К llms.txt не относится.
- Блок llms.txt в `04` живёт в `04:1395-1402`. Там УЖЕ написано «статичный машинойтаемый файл для поисковых систем и ИИ-ассистентов» и «структуру услуг/меню для поисковых систем и ИИ-ассистентов».
- Текста «для AI-поиска» / «для AI:» в `04` **никогда не было** (grep `для AI|AI-поиск` по `04` → 0; единственный «AI-краулеров» — в `04:1206`, см. Баг 8).
Итог: описанная в отчёте правка либо не применялась, либо «Было» выдумано. Цель (нейтральный llms.txt) достигнута СУЩЕСТВУЮЩИМ текстом `04:1395/1398` + самим файлом `public/llms.txt`, а НЕ описанным edit'ом.
**Альтернатива:** удалить §3 как ложный, либо заменить на «нейтральность подтверждена живым `public/llms.txt` (12 строк, без AI/ИИ) и `04:1395/1398` (уже «ИИ-ассистентов», не «AI-поиска»)».

### Баг 5 — `components/ai/AIConcierge.tsx:25,36` + `app/layout.tsx:150` — ЖИВОЙ код публично брендирован «AI-консьерж» (нарушает ядро оси)
**Проблема:** ось AI-TRANSPARENCY (04:250, 08:136, 08:102) императивна: «публично НЕ называть «AI-консьерж» — использовать «Консьерж»/«Помощник»». Но живой компонент, рендерящийся site-wide (`app/layout.tsx:150 <AIConcierge/>`), содержит:
- `AIConcierge.tsx:25`: `const [messages, setMessages] = useState<Message[]>([{ role: 'bot', text: 'Здравствуйте! Я AI-консьерж NiloV. Чем помочь?' }]);`
- `AIConcierge.tsx:36`: `aria-label={open ? 'Закрыть' : 'AI-консьерж'}`
- `AIConcierge.tsx:43`: `<p …>AI-консьерж</p>`
Это прямое публичное нарушение запрета оси. `ITER4_FIXES_AI.md:33` пишет «Код проекта — не редактировался» — но тогда заявление оси «публично НЕ называть AI-консьерж» НЕ выполнено на проде. Самый тяжёлый баг оси.
**Альтернатива:** переименовать публичный копирайт в `AIConcierge.tsx:25,36,43` на «Консьерж»/«Помощник» (внутренняя пометка AI-слоя в резерве сохраняется в spec, но не в публичном UI); либо снять компонент из `layout.tsx`, пока не приведён к канону. Это вне скоупа правки STRUCTURE, но обязано быть зафиксировано как открытый блокер оси.

### Баг 6 — `ITER4_FIXES_AI.md:38` — ложное заявление «оценка ITER4_RECHECK_AI (8/10) по трём пунктам теперь закрыта»
**Проблема:** `ITER4_RECHECK_AI.md` УЖЕ выставил 8/10 и отметил пункты 1–3 как ✅ на Итерации 4 (табл. строки 18-19). Заявлять, что Итерация 5 «закрыла» их — избыточно и вводит в заблуждение. Реальные скидки к 8/10 в `ITER4_RECHECK_AI` — Баги 3/7 (трассируемость старых отчётов) и Баг 5 (z-index в `29_DEVICECRITIC`) — НЕ входят в 04/07/08 и остаются открытыми.
**Альтернатива:** переформулировать: «текстовые цели 04/07/08 подтверждены живым grep'ом; до ≥9/10 мешают: (а) целостность настоящего отчёта (Баги 1–4), (б) живой код AI-консьерж (Баг 5), (в) трассируемость ITER3-отчётов (вне скоупа)».

### Баг 7 — `ITER4_FIXES_AI.md:38` — игнорирование Бага 5 (код) в расчёте «до ≥9/10»
**Проблема:** список «что мешает ≥9/10» перечисляет только z-index и трассируемость, полностью игнорируя живое публичное нарушение «AI-консьерж» в коде (Баг 5). Самооценка оси завышена, потому что самый очевидный для пользователя дефект (бренд в UI) не учтён.
**Альтернатива:** включить Баг 5 в перечень блокеров ≥9/10.

### Баг 8 — `04_BLOCKS.md:1206` — остаточное «AI-краулеров» рядом с llms.txt
**Проблема:** `public/llms.txt` нейтрален ✅, но spec-строка `04:1206` пишет «`public/llms.txt` перечисляет `/blog` и ключевые посты для AI-краулеров (ChatGPT/Perplexity)». Термин «AI-краулеров» — пограничный: это SEO-индексация (ITER4_RECHECK_AI:60 допускает «для поисковых LLM-агентов» как NOT-баг), но в свете требования «llms.txt без „для AI“-брендинга» это единственное место в `04`, где «AI» привязан к llms.txt. Низкая критичность, но противоречит духу нейтральности.
**Альтернатива:** заменить на «для поисковых систем и LLM-агентов (ChatGPT/Perplexity)» — синхрон с уже очищенным `04:1395`.

### Баг 9 — `ITER4_FIXES_AI.md:10` — ложный тезис «кросс-файловый drift устранён»
**Проблема:** слово «резервный» никогда не фигурировало в `04` (grep → 0). Значит, «drift» между `08` и `04` по термину «резервный» — выдуманная предпосылка: `04` не мог содержать «резервный», с которым `08` был бы неконсистентен. Правка в `08` (если была) не устраняла кросс-файловый drift, а лишь убирала слово внутри `08`.
**Альтернатива:** убрать формулировку «кросс-файловый drift устранён»; написать «слово „резервный“ удалено из 08 (grep → 0); 04 никогда не использовал этот термин, канон — 04:250/1008».

### Баг 10 — `ITER4_FIXES_AI.md:4` — неточная ссылка на «правила 04/07/08»
**Проблема:** заголовок утверждает «Правил: 04 / 07 / 08», но тело опирается на `ITER4_RECHECK_AI.md` и на строки `04:226/1371` (ложные), `07:246` (ложная). Правила (реальные строки) не названы точно, что ломает воспроизводимость речек-а.
**Альтернатива:** дать точный реестр: `04:250`, `04:1008`, `04:1395/1398`, `07:260`, `08:136`, `08:102` + `public/llms.txt`.

---

## Что РЕАЛЬНО сделано (позитив)
- `08_CONSTRUCTOR_SPEC.md` — слово «резервный» отсутствует (grep 0); Консьерж-FAB описан как `optional-обёртка над /plan/helper` ✅.
- `07_CALCULATOR_SPEC.md:260` — «AI-слой» явно помечен внутренним, публичный копирайт калькулятора чист ✅.
- `public/llms.txt` — нейтрален, без «AI»/«ИИ»-брендинга ✅.
- `04:250` и `04:1008` — несут «без ИИ» оговорку и внутреннюю пометку AI-слоя в резерве ✅.

## Что НЕ сделано / сломано
- Отчёт `ITER4_FIXES_AI.md` содержит ложные номера строк (`04:226`, `07:246`, `04:1371/1373`) и сфабрицированный diff (Баги 1–4, 9) → аудит-трейл оси невоспроизводим.
- Живой код `AIConcierge.tsx` публично брендирован «AI-консьерж» вопреки запрету оси (Баг 5) — главный блокер.
- `04:1206` сохраняет «AI-краулеров» (Баг 8, низкая критичность).

---

## Скоринг: `6 / 10` (строго ≤9)
**Обоснование (не соглашаюсь на 8/10):**
- +3 — три текстовые цели на диске достигнуты (08 без «резервный», 07 AI-слой внутренний, llms.txt нейтрален).
- −1 — остаточное «AI-краулеров» в `04:1206` (Баг 8).
- −1,5 — живая публичная брендировка «AI-консьерж» в коде (`AIConcierge.tsx`), противоречащая ядру оси (Баг 5). Для пользователя это единственное видимое нарушение.
- −1,5 — целостность отчёта `ITER4_FIXES_AI.md`: 4 ложных ссылки на строки + сфабрицированный diff `04:1371/1373` (Баги 1–4, 9). Авторитет речек-а подорван: нельзя доверять «закрыто по живым строкам», когда живые строки другие.

**Итог:** ось близка к цели по документации, но (а) отчёт-фиксер сам содержит неточности, и (б) прод-код всё ещё нарушает запрет публичного «AI-консьерж». До ≥9/10: исправить Баги 1–4 (переписать отчёт по живым строкам), закрыть Баг 5 (ребренд `AIConcierge.tsx`), прибрать Баг 8.

*Связано: `ITER4_FIXES_AI.md`, `ITER4_RECHECK_AI.md`, `04_BLOCKS.md`, `07_CALCULATOR_SPEC.md`, `08_CONSTRUCTOR_SPEC.md`, `public/llms.txt`, `components/ai/AIConcierge.tsx`.*
