# ITER5_RECHECK_TRUST — ФИНАЛЬНЫЙ речек ось TRUST (Итерация 5)

**Дата:** 2026-07-20 · **Скоуп:** ТОЛЬКО структура `research/STRUCTURE/`
(прежде всего `04_BLOCKS.md` + `02_IA.md`, плюс сверка с остальными файлами папки).
**Метод:** прямой grep по ЖИВЫМ строкам файлов + проверка реальности кода/CI (`app/`, `components/`, `.github/workflows/`, `scripts/`). Без автосогласия с ITER4_FIXES_TRUST.

---

## ВЕРДИКТ: СКОРИНГ 5/10 (строго ≤9)

Ось TRUST **не закрыта**. Из 4 заявленных проверок:
- (1) гейт исполним — **FAIL** (проза, не код/CI);
- (2) «18/19 лет» → «с 2007» — **PARTIAL** (целевые 04/02_IA — да; глобально и по count-up — нет);
- (3) голые факты имеют 🟡 — **FAIL** (JSON-LD и FAQ без 🟡);
- (4) 02_IA и 04 консистентны по 3500+ — **PARTIAL/FAIL** (64↔1251 да; 273 и внутр. 04 — нет).

Ни одна из 4 проверок не проходит полностью. Блокер (1) признан незакрытым даже в самом ITER4_FIXES_TRUST:55-57.

---

## ЧТО ПОДТВЕРЖДЁННО ЗАКРЫТО (не просто баги)

✅ **C1. «19 лет» удалён из целевых файлов.** `02_IA.md:64` теперь «🟡 более 18 лет [с 2007…]»; `02_IA.md:273` теперь «🟡 более 18 лет (с 2007 года)». В 04/02_IA нет «19 лет» как клиент-факта. Конфликт 18↔19 в целевой паре снят.

✅ **C2. Фабрикованная ссылка «независимый критик C6 2026-07-20» в 02_IA:64 удалена.** Живая строка 64 несёт честное «НЕ публиковать как подтверждённый факт до сверки — статус `pending`». ITER4_FIXES_TRUST:33-35 подтверждено фактом.

✅ **C3. 02_IA:64 «3 500+» = 04:1251 «3 500+»** — число и маркер статуса совпадают (оба 🟡 pending). Частичная межфайловая сверка выполнена.

✅ **C4. Кофе 950 в Hero/price-якорях 04:437 и 04:543 несёт 🟡 + видимый disclaimer** («(на проверке)» / «на живом сайте указано 390 ₽»). Заявление ITER4_FIXES_TRUST:37 подтверждено для ЭТИХ двух строк.

⚠️ Выше — единственные реально закрытые пункты. Всё ниже — живые баги.

---

## БАГИ (file:line + альтернатива)

### B1 — Гейт НЕ исполним (блок РЕАЛИЗАЦИЯ = проза, не код/CI) — КРИТ
`04_BLOCKS.md:107-129` описывает «РЕАЛИЗАЦИЯ FACT-GATE», но:
- `renderFactItem` отсутствует во ВСЕХ `.ts/.tsx` (`grep -rln renderFactItem app components` → 0 совпадений, проверено в терминале);
- каталога `scripts/` **не существует** → `scripts/fact-gate.mjs` нет;
- в `.github/workflows/` только `block-force-push.yml`, `external-critics.yml`, `vercel-deploy.yml` — ни один не грейпает факты (grep `fact-gate|renderFactItem` по `.github` → NONE);
- сам 04:127 гласит: «гейт считается закрытым ТОЛЬКО когда этот шаг реально присутствует в репо и зелёный в CI (иначе — статус „спроектирован, не исполнен“)».
- ITER4_FIXES_TRUST:55-57 САМ признаёт: «Гейт реально не исполнен … по-прежнему НЕ существуют в коде».

→ **Альтернатива:** либо написать реальный `scripts/fact-gate.mjs` (grep-хук: 0 безусловных «обслуживали X» в публичных строках + 0 числовых claim без `status:'verified'`/`disclaimer`) и повесить на `pre-commit` + шаг в `external-critics.yml`; реализовать `renderFactItem` мин. в 5 компонентах из 04:112-115; либо в 04:127 и ITER4_FIXES явно переклассифицировать гейт в «описан, НЕ исполнен» и НЕ считать ось закрытой.

### B2 — Кофе 950 БЕЗ 🟡 в JSON-LD (нарушение FACT-GATE)
`04_BLOCKS.md:1381` — `priceRange … ИЛИ явно «от 950 ₽/гость»` (в блоке LocalBusiness JSON-LD). Нет 🟡, нет disclaimer, нет статуса. Прямо противоречит 04:105 («цены кофе-брейка „950“ — 🟡 pending с пометкой сверки») и FACT-GATE 04:88-104. Это структурированные данные, индексируемые как факт.

→ **Альтернатива:** в `priceRange` либо поставить 🟡 + `(pending-verification, на сайте клиента 390 ₽)`, либо вынести число в fact-гейт-поле с `status:'pending'` + disclaimer, НЕ публиковать 950 как подтверждённый якорь.

### B3 — Кофе 950 БЕЗ 🟡 в FAQTeaser
`04_BLOCKS.md:1581` — «Кофе-брейк — от 950 ₽/гость. Цена за гостя фиксирована…» — видимый пользователю копирайт, 🟡 отсутствует (в отличие от 04:437/543). Пользователь читает 950 как утверждение.

→ **Альтернатива:** добавить 🟡 `pending-verification` + «точная цена подтверждается при расчёте; на сайте клиента 390 ₽» рядом с 950.

### B4 — Серия голых якорей «950» без 🟡 (FormatShowcase / тарифы)
`04_BLOCKS.md:695` («Кофе-брейк от 950 ₽»), `04:703`, `04:1034`, `04:1045`, `04:1085` («Кофе-брейк — economy 950…») — ценовые якоря без 🟡. ITER4_FIXES_TRUST:37 утверждает «подтверждено: уже несут 🟡» — это **ложно** для перечисленных строк.

→ **Альтернатива:** каждый публичный якорь 950 обернуть в `renderFactItem`/`pending`-disclaimer (едино с 04:437/543), либо глобально в 04 ввести правило «любой публичный ₽/гость-якорь несёт 🟡 до сверки с прайсом».

### B5 — Фрагментация числа событий 3000 ↔ 3500 (нет единого источника)
Одна и та же сущность («проведённых событий») в разных блоках 04:
- `04:429` Hero: «Более 3000 событий (на проверке)»;
- `04:833` / `04:837` / `04:839` WhyUs + count-up: «3000 событий» / «3000+ событий» / count-up `3000+`;
- `04:1251` TrustProof: «3 500+ проведённых событий» 🟡;
- `04:500` FACTCHECK: «3000+/3500+ событий»;
- `04:2348` TODO: «3000+ проведённых событий».

Нет SSOT. Заявление ITER4_FIXES_TRUST:29 «унификация „3500+“» выполнено ТОЛЬКО в TrustProof (1251), но Hero/WhyUs продолжают 3000.

→ **Альтернатива:** выбрать одно каноническое число (3500+ согласно заказчику) и заменить 3000/3000+ на «3 500+ 🟡» во ВСЕХ блоках 1/8/57 + count-up 04:839 + TODO 04:2348; либо сознательно зафиксировать «3000+ (как на сайте клиента)» и привести 04:1251 к 3000+.

### B6 — 02_IA:273 противоречит 02_IA:64 и 04:1251 (внутрифайловая + межфайловая рассогласовка 3500)
`02_IA.md:273` (AwardsStrip на /why-us): «🟡 3000+ событий». Но `02_IA.md:64` (та же страница /why-us): «🟡 3 500+ мероприятий», и `04:1251` (тот же AwardsStrip = TrustProof facts): «3 500+». Одна IA-страница даёт два разных числа; IA расходится с каноном 04.

→ **Альтернатива:** привести 02_IA:273 к «3 500+ 🟡» (или к единому выбранному в B5 числу), чтобы 64/273 и 04:1251 совпали.

### B7 — Count-up анимирует расходящиеся числа + сохранён самоконфликт арифметики 18/19
`04_BLOCKS.md:839` count-up: `18` 🟡 (лет) · `3000+` 🟡 (событий). Числа count-up (18 лет, 3000+) расходятся с каноном «с 2007» (2026−2007=19, признано в 04:2346) и с 3500 (B5). Более того, 04:2346 сам явно пишет «арифметически для 2026 года — 19, но прод клиента показывает 18» — спека сознательно копирует заведомо неверное число.

→ **Альтернатива:** запросить у заказчика актуальное число лет и поставить верное (19 с 2007) ИЛИ честно «с 2007 года» БЕЗ «N лет» до подтверждения; привести count-up 04:839 к единому каноническому числу событий из B5.

### B8 — Глобальная папка STRUCTURE НЕ унифицирована по «18/19 лет» и «Роспотребнадзор как клиент»
Вне скоупа ITER4_FIXES (только 04/02_IA), но ось TRUST глобально открыта. Живые «19 лет»/«Роспотребнадзор как клиент» в других STRUCTURE-файлах:
- `21_LOGIC_AUDIT.md:16, 117, 128, 154, 208` — «19 лет / с 2007», «Роспотребнадзор» как клиент (стр. 119);
- `13_BENCHMARK_SYNTHESIS.md:23` — «19 лет», «Роспотребнадзор» в client-фрейме;
- `18_CRITIC_SYSTEM.md:61` — «19 лет» в перечне claim'ов FactChecker;
- `17_IA_CRITIC.md:55` — «19 лет, 3000, су-вид, фермеры»;
- `22_COHERENCE_REPORT.md:87, 134, 189` — «19 лет/3000+»;
- `36_DESIGNCRITIC_B_BLOCKS.md:65, 182, 210` — «19 лет» vs «18 лет»;
- `39_CANON_INDEX.md:67` — «SocialProofBar (статы 19 лет/3000+/98%)»;
- `20_CRITIC_RESULTS.md:58, 114` — «19 лет» = «18 лет»;
- `16_BLOCK_BENCHMARKS.md:103` — «Роспотребнадзор 🟡» в client/geometric frames.

ITER4_FIXES_TRUST:63-65 САМ отмечает эти остатки, но они не закрыты.

→ **Альтернатива:** одноразовый sweep всех `research/STRUCTURE/*.md` — заменить «19 лет»→«более 18 лет 🟡 (с 2007)», убрать «Роспотребнадзор» из client-контекста (оставить только как регулятор). Либо официально сузить «ось TRUST» до 04/02_IA и не заявлять глобальную сведённость.

### B9 — ITER4_FIXES_TRUST завышает статус закрытия (самопротиворечие отчёта)
ITER4_FIXES_TRUST:7 озаглавливает §1 «Исполнимость fact-gate → блок „РЕАЛИЗАЦИЯ“» и подразумевает закрытие бага #1, НО в том же файле (стр. 55-57) написано «Гейт реально не исполнен … НЕ существуют в коде». То есть отчёт об «исправлении» сам фиксирует, что исправление не выполнено. Заявление «закрывает ITER4_RECHECK баг #1» (04:20) ложно по собственному признанию.

→ **Альтернатива:** переписать ITER4_FIXES_TRUST:7-20 как «гейт СПРОЕКТИРОВАН в спеке, НЕ исполнен в коде/CI (баг открыт)»; не помечать баг #1 закрытым.

### B10 — Заказчик-домен `odaeda.ru` vs ИП Нилов — потенциальный рассинхрон идентичности (trust)
`02_IA.md:10` фиксирует прод-домен `odaeda.ru` («РЕАЛЬНЫЙ прод-домен NiloV Catering (ИП Нилов)»), но живой сайт заказчика, на который ссылаются ВСЕ фактчек-ссылки (04:429/500/651/695 «на живом сайте NiloV указано…»), — `nilov-catering.ru`. Если `odaeda.ru` ещё не поднят, то claim'ы «на живом сайте NiloV пишет …» ссылаются на СТАРЫЙ домен, а канон уже `odaeda.ru` — доверие к «живому сайту» как источнику подтверждения рассинхронно с каноном.

→ **Альтернатива:** явно зафиксировать статус `odaeda.ru` (поднят/на стадии) и привязать фактчек-ссылки к актуальному домену; либо временно не ссылаться на «живой сайт» как на подтверждение до редиректа 301.

---

## СВОДКА ПРОВЕРОК (живой grep)

| # | Проверка | Результат | Док-во |
|---|---|---|---|
| 1 | Гейт исполним (РЕАЛИЗАЦИЯ + renderFactItem + CI) | **FAIL** | renderFactItem: 0 в app/components; scripts/ нет; workflows без fact-gate (B1, B9) |
| 2 | «18/19 лет» → «с 2007» унифицированы | **PARTIAL** | 02_IA:64/273 + 04 целевые — да; 04:839 count-up `18` + глобально «19 лет» в 21/13/18/17/22/36/39 — нет (B7, B8) |
| 3 | Голые факты имеют 🟡/disclaimer | **FAIL** | 04:1381 (JSON-LD), 04:1581 (FAQ), 04:695/703/1034/1045/1085 — голые 950 без 🟡 (B2, B3, B4) |
| 4 | 02_IA и 04 консистентны по 3500+ | **PARTIAL/FAIL** | 02_IA:64=04:1251 (3500) ✓; 02_IA:273=«3000+» и 04 Hero/WhyUs=«3000/3000+» — расходятся (B5, B6) |

---

## ИТОГОВЫЙ СКОРИНГ: 5/10

**Обоснование:** 4 проверки — 0 полных прохождений (1 FAIL, 2 PARTIAL, 1 FAIL). Критический блокер (1) исполнимость гейта не устранён даже по признанию самого fix-отчёта. «Голые факты 🟡» нарушен в машиночитаемом JSON-LD и в FAQ — именно там, где поисковик/пользователь видит утверждение без пометки. Единое число событий (3500 vs 3000) и единая формулировка лет (18 vs 19) разъезжаются между блоками и файлами. Закрыты только 4 узких пункта (C1-C4).

Ось TRUST требует отдельной инженерной задачи (код + CI, B1) и сплошного sweep'а папки STRUCTURE (B8), прежде чем может быть заявлена закрытой. До того — статус «спроектировано, НЕ исполнено».
