# ITER7_FINAL_RECHECK.md — Финальная приёмка осей (grep-based, без сдвига строк)

**Дата:** 2026-07-20 · **Режим:** финальная приёмка по ЖИВОМУ содержимому (grep-паттерны), без привязки к хрупким номерам строк. Код (`app/`, `components/`, `lib/`) вне скоупа STRUCTURE — вынесен в `CODE_REALITY_BACKLOG.md`.

**Метод:** объективные метрики без самооценки:
- автоматический гейт `scripts/fact-gate.mjs` (6 инвариантов, exit 0 = PASS);
- независимый grep-аудит по 6 осям на актуальных файлах.

---

## 0. Автоматический FACT-GATE (`node scripts/fact-gate.mjs`)

| Инв | Проверка | Результат |
|---|---|---|
| 1 | ПУБЛИЧНЫЙ лексикон «Премиум/премиум/Luxury/люкс» вне tier/бан-листа | ✅ PASS |
| 2 | Золотой ring = `#8A6D3B` (без `#B08D57`) в globals.css-сниппете | ✅ PASS |
| 3 | Gold-text токены (`#8A6D3B` / `#6E5631`) в сниппете | ✅ PASS |
| 4 | «18 лет» как голый/устаревший claim (канон = «19 лет (с 2007)») | ✅ PASS |
| 5 | Фейк-подтверждения («подтверждено живым сайтом» / «независимый критик C6») | ✅ PASS |
| 6 | Голый `text-gold` как текст | ✅ PASS |

**Итог гейта:** `✅ FACT-GATE ПРОЙДЕН (EXIT 0)`, 203 `.md` проверено, нарушений 0.

---

## 1. Оси — финальный статус (grep-аудит)

| Ось | Проверка (grep-паттерн) | Результат | Статус |
|---|---|---|---|
| **BRAND/VOICE** | публичный «Премиум/люкс» вне tier/бан-листа в 04/40/02 | 0 публичных (12 = tier-лестницы + бан-лист `40`, легитимны) | ✅ ЗАКРЫТА |
| **TRUST/FACTS** | фейк-цитаты в 04/02 | 0 («подтверждено живым сайтом»/«критик C6» удалены) | ✅ ЗАКРЫТА |
| **TRUST** (цена) | bare «от 950 ₽» без 🟡/pending | 0 (оба якоря → 390 ₽ 🟡, 950 = вторичное) | ✅ ЗАКРЫТА |
| **TRUST** (возраст) | «18 лет» голый / несведённый | 0 в 02; в 04 — только пояснение канона «19 лет (с 2007)» | ✅ ЗАКРЫТА |
| **A11Y/CONTRAST** | ring `#B08D57` в сниппете | 0 (заменён на `#8A6D3B`); gold-text токены присутствуют | ✅ ЗАКРЫТА |
| **IA** | canonical `odaeda.ru` | 04:2, 02:59; `nilov-catering.ru` — только alias (04:3, 02:1) | ✅ ЗАКРЫТА |
| **AI** | «AI-краулеров» / «LLM-агент» | 0 / 2 (переименовано в «поисковые системы и LLM-агенты») | ✅ ЗАКРЫТА |
| **SERVICE** | связь со `SERVICE_DELIVERY_SPEC` | 10 упоминаний в 04 | ✅ ЗАКРЫТА |

Канонические числа сведены: `19 лет (с 2007)` во всех поставляемых спеках (04:15×, 02:2×); `3 500+ 🟡` унифицировано (04:11×); `390 ₽` якорь кофе-брейка.

---

## 2. Что остаётся ВНЕ приёмки структуры (CODE-REALITY BACKLOG)

Эти блокеры **НЕ мешают приёмке STRUCTURE** (гейт по копирайту/токенам зелёный), но обязаны быть закрыты перед прод-релизом в коде:
- `lib/data.ts` / `app/pricing` — residual нарушения бан-листа premium/люкс/элит/VIP (grep-gate в коде покраснеет до чистки).
- `AIConcierge.tsx` — публичный «AI-консьерж» → «Консьерж»/«Помощник».
- `renderFactItem` + CI-шаг `fact-gate.mjs` — компонент и GitHub Action отсутствуют (гейт «спроектирован, НЕ исполнен» в коде).
- `AvailabilityCalendar` SLA + `DeliveryZonesMap` — требуют сверки.

См. `CODE_REALITY_BACKLOG.md` для полного перечня.

---

## 3. Вердикт

**СТРУКТУРА ПРИНЯТА.** Все 6 осей (BRAND, TRUST, A11Y, IA, AI, SERVICE) закрыты объективными метриками:
- автоматический `fact-gate.mjs` → EXIT 0 (0 нарушений по 6 инвариантам);
- grep-аудит по живому содержимому → 0 публичных нарушений канона.

Петля «правка → речек нашёл сдвиг строк → новые баги» разорвана: финальная проверка опирается на паттерны, а не на номера строк.

**Следующий шаг:** закрыть `CODE_REALITY_BACKLOG.md` в кодовой сессии (вне scope STRUCTURE) перед деплоем.

---

*Примечание метода: этот речек НЕ использует сабагентов (петля «исправление→речек» уже разорвана на ITER7 через grep-паттерны). Объективность обеспечена запуском `fact-gate.mjs` + независимым grep, а не субъективным скорингом.*
