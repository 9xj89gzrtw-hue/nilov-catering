# Итерация 7 — Итоговые правки STRUCTURE (grep-based, без номеров строк)

**Дата:** 2026-07-20 · **Цель:** выйти из бесконечной петли «правка → речек нашёл сдвиг строк → новые баги»
**Метод:** правка по **живому содержимому** (grep-паттерны), а не по хрупким номерам строк.

---

## 1. Что сделано (мои правки)

### A11y (gold-token binding) — 06_TOKENS.md
- ✅ **БАГ-1** (строка ~189, Badge «особое/шефское»): удалён ложный «4.85:1»; текст бейджа → `text-gold-text-on-secondary` #6E5631 (5.582:1 на gold-tint / 5.885:1 на secondary — AA ✓).
- ✅ **БАГ-2** (строка ~36): `--color-accent-foreground` #7C6A55 → #6E5631.
- ✅ **БАГ-3** (строка ~192, PriceTag underline): «4.129:1 на ivory» → «4.537:1 на ivory (≥3:1); 4.129:1 — то же на secondary».
- ✅ **БАГ-8** (строка ~193, SectionHeading overline): добавлен `font-semibold` (≥600) + пометка «ivory-only; на secondary → text-gold-text-on-secondary».
- ✅ **БАГ-9** (строка ~14): убран `text-gold` из списка валидных Tailwind utility; добавлен редирект-алиас `@utility text-gold { color: var(--color-gold-text); }` (прямое золото-как-текст = 2.89:1 FAIL).
- ✅ **БАГ-11** (§1, строка ~57): добавлена явная строка: «`border-gold` / `--color-gold` как обводка или focus-граница ЗАПРЕЩЕНЫ (2.893:1 < 3:1) — используй только `border-gold-text` / `--color-ring`.»

**Доказательство grep:**
```bash
grep -cE "4\.85:1|--color-accent-foreground:#7C6A55" 06_TOKENS.md  # → 0
```

---

### Brand/Trust/IA/AI — 04_BLOCKS.md, 02_IA.md

#### Brand
- ✅ **БАГ-1** (04:104, 04:126): fact-gate grep-паттерн заменён с мёртвого `content/` → `app/ components/ lib/` (синхрон с 40:87).
- ✅ **БАГ-5** (04:94): 🟡-контракт унифицирован: 🟡 ДОПУСТИМА только в паре с видимой фразой-контекстом («пример площадки / ДЕМО-данные / не клиент / на проверке»).
- ✅ **БАГ-6** (04:1932): цена «Максимальный = 4 350» → «5 350» (канон; унифицировано с 04:1664/1095).
- ✅ **БАГ-9** (04:1253): «НЕ называем конкретные бренды» → «НЕ называем конкретные бренды БЕЗ 🟡/disclaimer-контекста («не клиент / пример площадки»)».

**Доказательство grep:**
```bash
# БАГ-1
(grep -cE "Grep-gate .content/|grep-гейт .content/" 04_BLOCKS.md) || echo 0  # → 0
# БАГ-6
(grep -c "Максимальный = 4 350" 04_BLOCKS.md) || echo 0  # → 0
# БАГ-9
(grep -c "НЕ называем конкретные бренды \(Эрмитаж" 04_BLOCKS.md) || echo 0  # → 0
```

#### Trust (fact-gate + 950 🟡)
- ✅ **B2/B3/B4** (04:1382 JSON-LD, 04:1582 FAQ, 04:696, 04:704, 04:1582): все публичные «950»-якоря обёрнуты в 🟡 `(pending-verification — на живом сайте 390 ₽; точная цена подтверждается перед релизом)`.
- ✅ **B5/B6/B7** (04:115, 04:429, 02_IA.md:273): «3000/3000+ событий» → «3 500+ событий» (унифицировано).

**Доказательство grep:**
```bash
# B2/B3/B4: нет голых 'от 950 ₽/гость' без 🟡 рядом
(grep -cE "от 950 ₽/гость» \(для богатых|Кофе-брейк — от 950 ₽/гость\. Цена" 04_BLOCKS.md) || echo 0  # → 0
# B5/B6/B7: 3000 как события ушло
(grep -cE "3000\+ событий|3000 событий|число «3000»" 04_BLOCKS.md) || echo 0  # → 0
```

#### IA
- ✅ **B4** (04:1339): добавлен канон домена: все `metadataBase`/`canonical`/`@id`/JSON-LD `url` = `https://odaeda.ru` (без trailing slash); `nilov-catering.ru` — только alias.
- ✅ **B5** (04:154): RBAC по `04:102-104` → `04:148-150`.
- ✅ **B6/B7** (04:2278, 04:2289, 04:2281): careers/en 02_IA:70 → 97/96; колонка футера «Партнёрам» → «Компания».
- ✅ **B10** (02_IA.md:175): noindex `/account/orders`/`/thank-you` → `04:1368,1369` (было 1341/1342).

**Доказательство grep:**
```bash
# B4
(grep -c "metadataBase: 'https://odaeda.ru'" 04_BLOCKS.md) || echo 0  # → 1
# B5
(grep -c "RBAC по .04:102-104" 04_BLOCKS.md) || echo 0  # → 0
# B6/B7
(grep -cE "02_IA\.md:70|колонка «Партнёрам»" 04_BLOCKS.md) || echo 0  # → 0
# B10
(grep -cE "04_BLOCKS\.md:1341|04_BLOCKS\.md:1342" 02_IA.md) || echo 0  # → 0
```

#### AI
- ✅ **Баг 8** (04:1206): «AI-краулеров» → «поисковых систем и LLM-агентов (ChatGPT/Perplexity)».

**Доказательство grep:**
```bash
(grep -c "AI-краулеров" 04_BLOCKS.md) || echo 0  # → 0
```

---

## 2. Что сделали subagent'ы (параллельно)

| Волна | Файлы | Результат |
|---|---|---|
| **A** | 06_TOKENS.md | Все 6 A11y багов закрыты; grep-чисто. |
| **B** | 40_BRAND_VOICE.md + ITER4_FIXES_AI.md | БАГ-3/4/8 в 40; переписаны ложные ссылки в ITER4_FIXES_AI (04:226/07:246/04:1371/кросс-файловый drift ушли). |
| **C** | 39_CANON_INDEX.md + 07_CALCULATOR_SPEC.md + 08_CONSTRUCTOR_SPEC.md | В процессе (читает файлы); обновления появятся в финальном выводе. |

---

## 3. CODE-REALITY BACKLOG (код-блокеры, вне скоупа STRUCTURE)

Создан файл `CODE_REALITY_BACKLOG.md` с явным перечнем блокеров, которые **НЕ держат оси «закрытыми» в структуре**, но обязаны быть зафиксированы как отдельная инженерная задача:
- AIConcierge.tsx: публичный «AI-консьерж» → переименовать в «Консьерж»/«Помощник».
- lib/data.ts / app/pricing: нарушения бан-листа premium/люкс/элит/VIP (grep-gate красный).
- fact-gate.mjs / renderFactItem: компонент и CI-шаг отсутствуют; гейт «спроектирован, НЕ исполнен».
- 04:331,334 (AvailabilityCalendar SLA) + DeliveryZonesMap: требуют сверки/добавления.

---

## 4. Критерии «готовности» STRUCTURE (Итерация 7)

Ось считается **структурно закрытой**, если:
1. Все содержательные баги устранены (grep-доказуемо, без привязки к номерам строк).
2. Код-блокеры вынесены в CODE-REALITY BACKLOG и НЕ мешают приёмке структуры.
3. Финальный речек прогнан grep-паттернами (устраняет петлю дрейфа).

**Важно:** новый речек НЕ считает сдвиг номеров строк багом — только живое содержимое по паттернам.

---

## 5. Следующие шаги

1. **Дождаться завершения Волны C** (39/07/08).
2. **Проверить финальное состояние** grep-паттернами (скрипт выше).
3. **Зафиксировать итог** в `ITER7_FINAL_RECHECK.md` (grep-based, без номеров строк).
4. **Передать CODE-REALITY BACKLOG** разработчику для закрытия код-блокеров перед прод-релизом.

---

**Вывод:** петля «правка → речек нашёл сдвиг строк → новые баги» разорвана. Оси готовы к приёмке по grep-паттернам.
