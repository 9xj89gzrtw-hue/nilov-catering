# ITER8_RECHECK_BRAND.md — Независимый пересмотр оси BRAND/VOICE

**Дата:** 2026-07-21
**Критик:** независимый (diff-модель, самооценка исключена)
**Объект:** только живое содержимое `40_BRAND_VOICE.md`, `04_BLOCKS.md` (копирайт), `06_TOKENS.md`
**Метод:** поиск по живому тексту (НЕ по номерам строк старых отчётов ITER3/4/5/7). Сквозной grep по `04_BLOCKS.md` на премиум-лексикон, статус-бренды, лейблы тарифов + выборочное чтение Hero / PackageGrid / TrustProof / TrustBar / Venues.

---

## ИТОГОВЫЙ ВЕРДИКТ

**4 обязательные оси ЗАКРЫТЫ.** Публичного премиум/люкс-лексикона в видимом копирайте `04` нет; статус-бренды присутствуют только как 🟡 / DEMO / «запрещено»; voice DO/DONT соблюдён; публичные лейблы тарифов = Эконом / Стандарт / Расширенный / Максимальный.

**Найдено остатков:** 2 минорных (не gate-breaking) нарушения правила «жаргон с расшифровкой на первом экране» в Hero. 8 тяжёлых багов НЕ обнаружено — не выдумываю под квоту.

**СКОРИНГ: 8 / 10** (потолок ≤9 соблюдён; балл снижен за 2 резидуальных жаргон-гэпа на первом экране + мета-оговорку об исполнении grep-гейтов, см. ниже).

---

## ПРОВЕРКА ПО ОСЯМ

### Ось 1 — нет ли публичного «Премиум/премиум/люкс/luxury» в видимом копирайте 04
**СТАТУС: ЗАКРЫТО.**

Сквозной поиск по `04_BLOCKS.md` на `премиум|Премиум|люкс|Люкс|luxury|Luxury|VIP|элит|Элит|роскош|exclusive|Exclusive` — все совпадения попадают в ДОПУСТИМЫЕ категории:

- **Внутренние `tier`-идентификаторы** (разрешены per `40:87`):
  - `04:132` `tier ('economy'|'standard'|'premium'|'luxury' — внутренний)`
  - `04:136` `tier (... — внутренний, НЕ видимый премиум-лейбл, T7)`
  - `04:1084` `внутренний tier: 'economy'|'standard'|'premium'|'luxury'`
  - `04:1085-1087`, `04:1097` `pricePerGuest['luxury']` (код формулы)
  - `04:1666`, `04:1935` — внутренний `tier` сохраняет `economy|standard|premium|luxury`
- **Текст правил/бан-листа** (не копирайт): `04:143` «Люкс» НЕ в публичном заголовке; `04:1043-1045`, `04:1664-1671`, `04:1934-1935` — инструкции маппинга tier→лейбл.
- **Контраст/ретроспектива** (не claim): `04:594` «это про меня, не только про VIP» (приземляющий якорь — ХОРОШО); `04:1084` «старые лейблы «Классик/VIP/Лёгкий» выведены из обращения»; `04:949` ««элитный сервис» только описан, но не простроен»; `04:1254` «акцент … не на «элитности»».
- **Deep-link query-параметр** `?tier=premium` (`04:471`, `04:621`, `04:457` и др.) — внутренний идентификатор в URL, НЕ публичный лейбл (разрешён per `40:87`).

**НЕТ** ни одного standalone публичного заголовка/лейбла «Премиум»/«Люкс»/«VIP»/«Элитный»/«Роскошный». Токен `roскош*` в `04` отсутствует полностью.

### Ось 2 — Эрмитаж/Armani/Газпром/Forbes только как 🟡/DEMO, не как доказательство уровня
**СТАТУС: ЗАКРЫТО.**

Все вхождения статус-брендов несут 🟡 / DEMO / «запрещено» контекст, НЕТ голых claim «обслуживали Эрмитаж/Armani/Газпром»:

- `04:502` (FACTCHECK): «логотипы Эрмитаж/Мариинский/Armani/Газпром/Forbes как «клиенты» — 0 веб-подтверждений … исключить».
- `04:561` «Примеры площадок («Эрмитаж» и др.) — ДЕМО-дАННЫЕ слота … не публиковать как подтверждённого клиента без 🟡-сверки».
- `04:820` «Фото реальных площадок (Эрмитаж/Мариинский 🟡 — пример-площадки, не подтверждённые клиенты)».
- `04:145` `Event.venue`: «примеры площадок типа «Эрмитаж/Мариинский» НЕСУТ status:'pending'».
- `04:2098` «Эрмитаж/Мариинский 🟡 — упоминаются ТОЛЬКО как примеры ДЕМО-данных слота, НЕ как подтверждённые клиенты и НЕ как доказательство уровня».
- `04:2122` `venue`: «реальные бренды (Эрмитаж/Мариинский) ЗАПРЕЩЕНЫ как клиенты без письменного договора».
- `04:2259` «Площадки — ПРИМЕР-категории (Эрмитаж/Мариинский 🟡 — НЕ подтверждены как клиенты …)».

`Forbes` в `04:1437` — цитата **исследования** («Forbes 2024: сбалансированный рейтинг вызывает БОЛЬШЕ доверия») как источник аргументации для RT-6 (честный микс ± отзывов), НЕ как клиент/статус NiloV. Допустимо.

### Ось 3 — voice DO/DONT соблюдён
**СТАТУС: ЗАКРЫТО (с 2 минорными резидуальными жаргон-гэпами, см. БАГИ 1–2).**

- Архетип «Мастер-хозяин» (`40:19-26`) → в копирайте `04`: Hero-sub «под ключ для любого бюджета» (`04:475-499`), «без скрытых доплат», «су-вид (без наценки за бренд)», «Доставка в КАД включена», «Маркировка по 14 аллергенам». Честность/конкретика/доступность на месте.
- Tone-матрица `40:32-52`: снобизм/пафос/фейк-авторитет в видимом копирайте `04` отсутствуют. Клиентский ряд `04:581-595`/`04:1255` — анонимные плейсхолдеры с 🟡 + приземляющая подпись «от семейного ужина … до банкета на 500 — один стандарт качества».
- **БАГ 1 (минорный):** `04:499` Hero-микрокопирайт первого экрана: «Кухня су-вид (без наценки за бренд)» — `су-вид` дан **без** plain-language перевода (пояснение в скобках касается «без наценки за бренд», а не того, что такое су-вид). Нарушает `40:52` DON'T #6: «КАД, су-вид — только с пояснением в скобках в первом экране».
  - *Альтернатива:* «Кухня су-вид (медленная варка при низкой t, без наценки за бренд)» ИЛИ вынести расшифровку из `04:448-449` на Hero.
- **БАГ 2 (минорный):** `04:545` ценовой якорь первого экрана (HeroExtras, НИЖЕ fold, но всё ещё «первый экран» по смыслу цены): «Доставка в КАД включена» — `КАД` без расшифровки. `40:52` требует пояснения в первом экране.
  - *Альтернатива:* «Доставка в пределах КАД (граница города) включена» — синхронно с расшифровкой в `04:448`.
- Примечание: расшифровка КАД/су-вид прописана в спеке ТОЛЬКО для `FormatShowcase` (`04:448-449`), но Hero (первый экран) её не дублирует → внутренняя нестыковка правила и фактического копирайта Hero.

### Ось 4 — публичные лейблы тарифов = Эконом/Стандарт/Расширенный/Максимальный
**СТАТУС: ЗАКРЫТО.**

- `04:1043` `PackageGrid` — «Публичные лейблы: «Эконом / Стандарт / Расширенный / Максимальный»».
- `04:1044-1045`, `04:1084`, `04:1666-1671`, `04:1934-1935` — единый канон: внутренний `luxury` → публично «Максимальный», `premium` → «Расширенный»; «Люкс» НЕ в публичном заголовке; старые «Классик/VIP/Лёгкий/Imperial/Royal» выведены из обращения.
- `04:1097` dirty-check savings считается «от ВЕРХНЕГО тарифа `luxury` (публично «Максимальный»)» — корректный маппинг, без скрытого «Люкса».
- Dish/Review/PricingConfig несут внутренний `tier` — разрешено (`40:87`, `04:132/136`).

---

## РЕЗИДУАЛЬНЫЕ НАХОДКИ (не gate-breaking)

| # | Файл:строка | Проблема | Альтернатива |
|---|-------------|----------|--------------|
| 1 | `04_BLOCKS.md:499` | `су-вид` на первом экране Hero без plain-language расшифровки (нарушение `40:52` DON'T #6) | «су-вид (медленная варка при низкой t, без наценки за бренд)» |
| 2 | `04_BLOCKS.md:545` | `КАД` на первом экране ценового якоря без расшифровки (нарушение `40:52` DON'T #6) | «в пределах КАД (границы города)» |

## МЕТА-ОГОВОРКА (не баг копирайта, но влияет на итоговый балл)

`40:87` и `04:105-128` описывают grep-гейты (`grep -rinE … → 0 совпадений`) и `scripts/fact-gate.mjs` как **спроектированный** механизм; статус исполнения в CI явно помечен «закрыт ТОЛЬКО когда шаг реально присутствует в репо и зелёный в CI» (`04:128`). Настоящая проверка 0-совпадений — это аудит **живого кода** (`app/ components/ lib/`), который вне зоны `research/STRUCTURE/`. В спеках (копирайт) правила прописаны корректно и самодостаточно; фактическое отсутствие премиум-лексикона в `04` подтверждено сквозным grep выше. Риск: если `scripts/fact-gate.mjs` ещё не в репо/CI, защита держится только на дисциплине копирайта, а не на автоматическом гейте.

---

## ЧЕК-ЛИСТ ПРИЁМКИ (из `40:181-189`) — статус по живому содержимому

- [x] В публичном копирайте `04` 0 совпадений с бан-листом §3 (проверено сквозным grep: все `премиум/люкс/VIP/элит` — внутренние tier / правила / контраст).
- [x] Hero/H1/H2 не содержат «премиум/люкс/элит/VIP» как публичный лейбл.
- [x] Лейблы тарифов: публично — «Эконом / Стандарт / Расширенный / Максимальный»; «Премиум» только внутренний `tier`.
- [x] Цена видна на первом экране, с мин. чеком и надбавкой вне КАД (`04:437-445`, `04:545`).
- [x] Нет фейк-лого и упоминаний Эрмитаж/Armani/Газпром как клиентов (все 🟡/DEMO/запрещено).
- [x] Tone соответствует «Мастер-хозяин»: тепло, честно, конкретно, доступно.
- [x] Отзывы — реальные голоса про еду/сервис, `approved`, без статус-лексикона (`04:1107-1133`, `04:1433-1442`).
- [!] Жаргон КАД/су-вид на Hero — расшифровка есть в `FormatShowcase` (`04:448-449`), но НЕ на первом экране Hero (`04:499`,`04:545`) → см. БАГИ 1–2.

---

## ЗАКЛЮЧЕНИЕ

Ось BRAND/VOICE в текущих спеках **закрыта по всем 4 обязательным пунктам**. «Премиум/люкс/элит/VIP/роскошь» в публичном копирайте `04` отсутствуют (только внутренние `tier`-идентификаторы и текст правил); статус-бренды — исключительно 🟡/DEMO/«запрещено»; voice DO/DONT отражён в копирайте; лейблы тарифов корректно смаппены. Остатки — 2 минорных жаргон-гэпа на первом экране Hero (КАД/су-вид без расшифровки) — не ломают бренд-позицию, но формально нарушают `40:52` DON'T #6 и должны быть закрыты правкой копирайта Hero. 8 тяжёлых багов не обнаружено — квота не удовлетворяется искусственно.

**СКОРИНГ: 8 / 10.**
