# ITER8 — НЕЗАВИСИМЫЙ РЕЧЕК оси TRUST/FACTS

**Дата:** 2026-07-21 · **Роль:** независимый критик (занижение, без само-скоринга) · **Объект:** `04_BLOCKS.md`, `02_IA.md`, `FACTCHECK_REPORT_2026-07-20.md` (фактически лежит в `research/`, не `research/STRUCTURE/` — см. примечание).

> Метод: старые номера строк НЕ доверялись — каждый claim перепроверен живым `grep`/`read` по текущему содержимому. Дополнительно прогнан реальный `node scripts/fact-gate.mjs` и просканированы `app/`, `components/`, `.github/workflows/`, `package.json`.

---

## 0. ЧТО ПРОВЕРЯЛОСЬ (6 чеков) — КРАТКИЙ ВЕРДИКТ

| # | Чек | Результат на уровне СПЕЦЫ | Реальный остаточный дефект |
|---|---|---|---|
| 1 | fact-gate реален (механизм, не проза) | ⚠️ ЧАСТИЧНО | скрипт есть и зелёный, но НЕ в CI/pre-commit и `renderFactItem` отсутствует в коде |
| 2 | нет фейк-подтверждений («живым сайтом»/«критик C6») | ✅ в 04/02 — ЧИСТО | но живёт фейк в критик-файле C6 (вне скопа гейта) |
| 3 | канон возраста «более 19 лет (с 2007)» везде, нет голого «18 лет» | ✅ проза чиста | count-up анимирует **«18»** (04:841) — противоречие канону |
| 4 | кофе-брейк якорь = 390 ₽ 🟡, 950 = вторичное | ⚠️ в Hero — ДА | в 4 местах 950 — ведущий, 390 — в скобках |
| 5 | «3 500+» помечено 🟡/pending | ✅ везде | — |
| 6 | Эрмитаж/Armani и др. = НЕ клиенты (0 подтверждений, 🟡) | ✅ везде | — |

**Итоговый балл оси TRUST/FACTS: 7.5 / 10** (строго ≤ 9). Обоснование ниже.

---

## 1. ДЕТАЛЬНАЯ ПРОВЕРКА

### Чек 1 — fact-gate: МЕХАНИЗМ ИЛИ ПРОЗА?
**Найдено (объективно):**
- `scripts/fact-gate.mjs` **существует** (13.7 KB) и **реально исполняется**: `node scripts/fact-gate.mjs` → `EXIT=0`, 6 инвариантов PASS, проверено 207 `.md`-файлов STRUCTURE. Это честный механизм, не проза.
- **НО он НЕ закреплён в пайплайне:** нет `.husky/`, нет git-хуков (`ls .git/hooks` — только sample), в `package.json` нет ни `prepare`/`husky`, ни скрипта `fact-gate`; в `.github/workflows/` (`block-force-push`, `external-critics`, `vercel-deploy`) **ни одного** шага `fact-gate`/`renderFactItem`. Спека `04:125-130` требует «повешен на pre-commit + CI-шаг» — **не выполнено**.
- **`renderFactItem` НЕ реализован в коде сайта:** `grep -rln renderFactItem app components` → **0 совпадений**. Спека `04:112-116` описывает его как «применяется в ReviewCard/TrustBar/TrustProof/WhyUs/HeroSection» — на деле это только псевдокод в `04:96-103`. Гейт на уровне ПРОДУКТА отсутствует.
- **Скоп гейта не совпадает со спекой:** `04:105,127` обещает «grep-гейт `app/ components/ lib/`», а реальный скрипт (см. его же header) сканирует **только `research/STRUCTURE/*.md`**. То есть гейт линтит сами спец-доки (которые УЖЕ корректны), а не поставляемый код. Покрытие фактически пустое по продукту.

**Вердикт чека 1:** fact-gate — реальный lint-скрипт спец-доков, но **НЕ рабочий механизм защиты живого сайта** (нет CI, нет `renderFactItem`, скоп ≠ заявленному). См. баги B1, B6, B7.

### Чек 2 — Фейк-подтверждения
- В `04_BLOCKS.md` и `02_IA.md`: фразы «подтверждено живым сайтом» и «независимый критик C6» — **0 совпадений** (grep чист). Старые фабрикации удалены. ✅
- `04:502` ссылается на «независимый критик D» и корректно пишет «🟡 БЕЗ ПОДТВЕРЖДЕНИЯ … НЕ подтверждено независимо» — это **честная** пересылка на реальный `FACTCHECK_REPORT_2026-07-20`, не фейк. ✅
- **НО** в критик-файле `C6_TRUST_FACTCHECK_CRITIC.md:50` до сих пор написано: «B2 закрыт … замечён на «подтверждено живым сайтом 3 500+»». Это прямо противоречит каноническому `FACTCHECK_REPORT_2026-07-20` (там 3500+ = ❓ no-data). Файл находится **вне скопа** `fact-gate.mjs` (инварианты 4/5 намеренно исключают ~200 критик-отчётов), поэтому гейт его не ловит. См. баг B5.

### Чек 3 — Канон возраста
- Проза во всех точках = «более 19 лет (с 2007)»: `02_IA.md:64,273`; `04_BLOCKS.md:430,502,611,835,839`. Голого «18 лет» в прозе — **0** (grep чист). ✅
- **Противоречие в count-up:** `04_BLOCKS.md:841` — «Цифры count-up … `18` 🟡 (лет)». Анимируемое число = **18**, тогда как весь остальной копирайт = «более 19 лет». Пользователь увидит счётчик «18», конфликтующий с Hero/Story. См. баг B2. (Гейт INV 4 пропускает, т.к. там «18» в обратных кавычках, не «18 лет».)

### Чек 4 — Кофе-брейк якорь 390 ₽ 🟡 vs 950
- Правило применено корректно в Hero/ценовых строках: `04:438` «от 390 ₽/гость (Эконом, минимум) 🟡 *(…ведущий якорь = 390 ₽; канон 950 — вторичное уточнение…)*», а также `04:447,545,712`. ✅
- **Нарушено в 4 местах**, где 950 — ведущий, а 390 — лишь в скобках/примечании:
  - `04:697` FormatShowcase-карточка: «Кофе-брейк `от 950 ₽/гость` 🟡 *(…на живом сайте NiloV 390 ₽…)*» — первое число = 950.
  - `04:705` таблица EventTypeSelector: «Кофе-брейк | Эконом | от 950 ₽ 🟡».
  - `04:1087` мат-функция конструктора: «Кофе-брейк — economy 950».
  - `04:1384` JSON-LD `priceRange`: «от 950 ₽/гость» 🟡.
  См. баг B3.

### Чек 5 — «3 500+» 🟡/pending
Все вхождения несут 🟡/pending и пометку сверки: `02_IA.md:64`; `04_BLOCKS.md:322,430,502,611,835,839,841,1253`. ✅ Чисто.

### Чек 6 — Эрмитаж/Armani и др. = НЕ клиенты
Все вхождения с 🟡 и директивой «не публиковать как факт до сверки»: `02_IA.md:64,273`; `04_BLOCKS.md:127,145,561,820,2098,2122,2259`. Роспотребнадзор явно исключён («госрегулятор, не клиент»). ✅ Чисто.

---

## 2. BUG-LIST (≥ 8 багов, file:line + альтернатива)

**B1 · [КРИТ] fact-gate НЕ закреплён в пайплайне + renderFactItem отсутствует в коде.**
- `04_BLOCKS.md:125-130` (требует pre-commit + CI-шаг и `renderFactItem` в 5 компонентах) vs реальность: `.husky` — нет, git-хуков нет, `package.json` без `fact-gate`, `.github/workflows/*` без шага, `grep renderFactItem app components` → 0.
- *Альтернатива:* либо (а) добавить шаг в `.github/workflows/vercel-deploy.yml` (`node scripts/fact-gate.mjs` → fail CI при exit≠0) + `husky` pre-commit + реализовать `renderFactItem` мин. в `WhyUs`/`HeroSection`/`TrustProof`; либо (б) в `04:127-130` честно переклассифицировать гейт в «описан, НЕ исполнен в коде/CI» и НЕ считать ось закрытой.

**B2 · [СРЕД] count-up анимирует «18», канон — «более 19 лет».**
- `04_BLOCKS.md:841` «`18` 🟡 (лет)» против `04:430,839` «более 19 лет». Видимое анимированное число = 18, конфликтует с прозой.
- *Альтернатива:* заменить count-up-число на «19» (или убрать точное число из count-up, оставив «более 19 лет» как статичный текст 🟡), либо синхронизировать прозу обратно на «18 лет 🟡» — но канон уже «более 19 лет», значит править count-up.

**B3 · [СРЕД] 950 — ведущий якорь в 4 местах вопреки правилу «390 ведущий».**
- `04:697,705,1087,1384` (FormatShowcase / EventTypeSelector-таблица / мат-функция / JSON-LD) показывают 950 первым, 390 — в скобках. Hero (`04:438,447,545,712`) соблюдает обратное.
- *Альтернатива:* в `04:697/705/1087/1384` вынести 390 ₽ 🟡 как ведущий якорь, 950 оставить вторичным уточнением в скобках (едино с Hero); в JSON-LD `priceRange` писать «от 390 ₽/гость» 🟡.

**B4 · [НИЗ] «независимый критик D» — неразрешимая атрибуция.**
- `04_BLOCKS.md:502` ссылается на «независимый критик D», но заголовок `FACTCHECK_REPORT_2026-07-20.md:2` называет автора просто «независимый FactChecker» (без «D»). Фамилии/идентификатора нет.
- *Альтернатива:* заменить «независимый критик D» на точную цитату из отчёта: «см. FACTCHECK_REPORT_2026-07-20, табл. #6 (❓ no-data)» — без вымышленного буквенного индекса.

**B5 · [КРИТ] Живой фейк-подтверждения «3 500+ подтверждено живым сайтом» в критик-файле.**
- `C6_TRUST_FACTCHECK_CRITIC.md:50`: «B2 закрыт … замечён на «подтверждено живым сайтом 3 500+»». Прямо противоречит канону `FACTCHECK_REPORT_2026-07-20` (3500+ = ❓ no-data) и `04:502`. Файл вне скопа `fact-gate.mjs` (инв. 4/5 исключают критик-отчёты), поэтому гейт его не ловит.
- *Альтернатива:* исправить строку на «3500+ НЕ подтверждено независимо (FACTCHECK ❓ no-data) → статус pending, 🟡»; либо удалить утверждение о «подтверждено».

**B6 · [ВЫС] Скоп `fact-gate.mjs` не соответствует заявленному в спеке.**
- `04_BLOCKS.md:105,127` обещает «grep-гейт `app/ components/ lib/`», а реальный скрипт (header + `SHIPPABLE_SPECS`) сканирует **только `research/STRUCTURE/*.md`**. Продукт не проверяется вовсе.
- *Альтернатива:* либо расширить скрипт на `app/ components/ lib/` (как заявлено в 04), либо в `04:105,127` честно написать «гейт сканирует поставляемые спецы 04/02, НЕ код» и убрать ложное обещание проверки `app/`.

**B7 · [ВЫС] `renderFactItem` описан как «применяется» в 5 компонентах, но не написан.**
- `04_BLOCKS.md:112-116` («Где применяется renderFactItem … ReviewCard/TrustBar/TrustProof/WhyUs/HeroSection») vs 0 реализаций в `app/`/`components/`. Спека подаёт неисполненное как сделанное.
- *Альтернатива:* в `04:112` добавить дисклеймер «(псевдокод — реализация в коде вне scope структуры; гейт считается закрытым ТОЛЬКО при наличии компонента + CI-шага)»; либо реализовать.

**B8 · [НИЗ] 04:116 включает «кофе 950» в список claim'ов под renderFactItem, хотя 950 — вторичное.**
- `04_BLOCKS.md:116`: «числовые claim'ы (3 500+/более 19 лет/кофе 950)». По канону ведущий якорь кофе-брейка = 390, 950 — вторичное уточнение. Упоминание «кофе 950» как основного claim легитимизирует нарушение B3.
- *Альтернатива:* заменить на «кофе-брейк 390 ₽ 🟡 (канон 950 — вторичное, pending-verification)».

**B9 · [ИНФО] Два FACTCHECK-отчёта противоречат друг другу по 3500+.**
- `FACTCHECK_REPORT_2026-07-20.md:19` (табл. #6) = ❓ no-data; `FACTCHECK_REPORT.md` (2026-07-18):16 = ✅ «сами пишут 3 500+, подтверждается». `04:502` корректно цитирует 2026-07-20, но старый отчёт остаётся в репо и сбивает (см. B5/C6).
- *Альтернатива:* пометить `FACTCHECK_REPORT.md` (07-18) как «устаревший, заменён 07-20» или удалить; оставить единым источником 2026-07-20.

---

## 3. СКОРИНГ (0–10, строго ≤ 9)

**Ось TRUST/FACTS = 7.5 / 10.**

Обоснование (занижение):
- **+** Спецы 04/02 на уровне ПРОЗЫ чисты: нет голого «18 лет», нет фраз «подтверждено живым сайтом»/«критик C6»; «3 500+», Эрмитаж/Armani и др. везде с 🟡/pending; Роспотребнадзор исключён; кофе-брейк в Hero ведёт с 390 ₽ 🟡. `fact-gate.mjs` реально существует и зелёный.
- **−** Механизм защиты живого сайта НЕ собран: `renderFactItem` отсутствует в коде (B1/B7), гейт не в CI/pre-commit (B1), скоп скрипта ≠ заявленному (B6). Count-up «18» бьёт канон «более 19 лет» (B2). 950 лидирует в 4 местах вопреки правилу (B3). Живой фейк-подтверждения 3500+ остаётся в критик-файле вне скопа гейта (B5). Противоречивые отчёты (B9).

Балл 7.5 (а не 9), потому что формально-документальная ось вычищена, но **исполнение механизма и внутренняя консистентность (числа/цены/count-up) ещё не закрыты**.

---

## 4. ВЕРДИКТ

**⚠️ ЧАСТИЧНО ЗАКРЫТА (7.5/10).** Чеки 2, 3(проза), 5, 6 — закрыты на уровне спец-документов. Чек 4 — закрыт в Hero, НЕ закрыт в 4 вторичных блоках. Чек 1 — **фундаментально не закрыт**: fact-gate существует как линтер спец-доков, но не защищает продукт (нет CI, нет `renderFactItem`, неверный скоп). Требуется до сборки: B1 (закрепить гейт в CI + реализовать `renderFactItem` ИЛИ честно переклассифицировать), B2 (count-up 18→19), B3 (390 ведущий везде), B5 (убрать фейк в C6-критике).

---

## 5. ПРИМЕЧАНИЕ ПО ПУТЯМ

- `FACTCHECK_REPORT_2026-07-20.md` в задании указан как `research/STRUCTURE/...`, фактически лежит в `research/FACTCHECK_REPORT_2026-07-20.md` (вне `STRUCTURE/`). Использован реальный путь.
- Прогон `node scripts/fact-gate.mjs` выполнен вживую: `EXIT=0`, 6 инвариантов PASS (объективное подтверждение, что механизм как файл существует и зелёный).
